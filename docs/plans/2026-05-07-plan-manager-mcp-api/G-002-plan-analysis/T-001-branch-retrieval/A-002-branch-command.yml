format: atomic_step
status: pending
step_id: A-002
name: branch-command
title: PlanBranchCommand class
parent_step_id: T-001
parent_step_name: branch-retrieval
parent_global_id: G-002
parent_global_name: plan-analysis
technical_specification_excerpt: 'Plan Manager MCP API - plan_branch command:

  plan_branch(plan_id, node_id) -> list of {node_id, file_path, content, lock_status}

  - plan_id: string, required

  - node_id: string, required

  - Returns ordered ancestor chain: spec first, requested node last

  - Disk-read only; no session required

  - plans_root from server config; never from MCP command params

  '
global_step_context: 'G-002: Plan analysis.

  plan_branch is the primary context assembly command for AI executors.

  Ancestor chain builder provided by G-002/T-001/A-001 in plan_manager/commands/plan_branch.py.

  '
tactical_step_context: "T-001: Branch retrieval.\nThis atomic step A-002 implements\
  \ PlanBranchCommand in plan_manager/commands/plan_branch.py.\nA-001 (same file)\
  \ provides build_ancestor_chain.\n\nConsumed contracts:\n  build_ancestor_chain\
  \ from A-001 (same file)\n\nexecute() workflow:\n  1. Resolve plans_root from server\
  \ config\n  2. chain = build_ancestor_chain(plans_root, plan_id, node_id)\n  3.\
  \ Return {chain: chain, depth: len(chain)}\n"
atomic_operation: Implement PlanBranchCommand in plan_manager/commands/plan_branch.py
intent: 'Add PlanBranchCommand to plan_manager/commands/plan_branch.py using

  build_ancestor_chain from A-001.

  '
responsibilities:
- Implement PlanBranchCommand with name plan_branch, get_schema, validate_params,
  execute, get_metadata
- get_schema requires plan_id and node_id
- execute calls build_ancestor_chain and returns chain list and depth
- No session required
code_file_target: plan_manager/commands/plan_branch.py
estimated_lines: 55
max_context_for_qwen63_8k: This atomic step is self-contained
depends_on:
- G-002/T-001/A-001 build_ancestor_chain in plan_manager/commands/plan_branch.py
deliverables:
- PlanBranchCommand class with complete MCP command interface
- get_schema with required plan_id and node_id
- Returns chain list ordered spec-first and depth count
constraints:
- total file including A-001 helper must stay under 300 lines
- Disk-read only; no session
acceptance_criteria:
- plan_branch for G node returns 2-item chain
- plan_branch for A node returns 4-item chain
- content field contains actual file content
- lock_status present in every chain record
observations_template:
- Did plan_branch return the correct ancestor chain for each node level?
- Is spec always the first element and the requested node always the last?
- Is lock_status present in all chain records?

format: tactical_step
status: pending
step_id: T-002
name: git-health
title: Git repository health collector
parent_step_id: G-005
parent_step_name: service-diagnostics
source_documents:
- docs/plans/2026-05-07-plan-manager-mcp-api/README.yaml
- docs/plans/2026-05-07-plan-manager-mcp-api/G-005-service-diagnostics/README.yaml
intent: 'Implement git repository health collector: repo size, loose object count,

  last commit timestamp per plan. Pure data-access module; no MCP command.

  '
context_from_technical_spec:
  metrics_per_plan: 'plan_id, repo_size_bytes, loose_object_count, last_commit_ts
    (ISO 8601)

    repo_size_bytes and loose_object_count: from git count-objects -v

    last_commit_ts: from git log -1 --format=%cI

    '
  plans_root_convention: 'plans_root from server config; never from MCP params.

    '
depends_on:
- G-001/T-001
responsibilities:
- collect_git_health(plans_root) -> list[GitHealthRecord]
- Per plan: repo_size_bytes, loose_object_count, last_commit_ts
- Disk-read only via git commands; no writes; no MCP command
owned_code_paths:
- plan_manager/diagnostics/git_health.py
owned_test_paths:
- tests/test_diag_git_health.py
owned_docs_paths: []
forbidden_paths:
- mcp_proxy_adapter/
- plan_manager/commands/
- .venv/
atomic_steps:
- id: A-001
  name: repo-metrics
  summary: GitHealthRecord and collect_git_health in git_health.py
model_assignment_spec:
  task: 'Implement plan_manager/diagnostics/git_health.py.

    collect_git_health(plans_root) -> list[GitHealthRecord].

    Per plan: repo_size_bytes, loose_object_count, last_commit_ts.

    Uses git count-objects -v and git log. No writes; no session.

    '
  allowed_edit_paths:
  - plan_manager/diagnostics/git_health.py
  - tests/test_diag_git_health.py
  forbidden_edit_paths:
  - mcp_proxy_adapter/
  - plan_manager/commands/
  - .venv/
  deliverables:
  - GitHealthRecord dataclass with plan_id, repo_size_bytes, loose_object_count, last_commit_ts
  - collect_git_health(plans_root) -> list[GitHealthRecord]
verification:
- collect_git_health returns correct repo size for a known plan
- Loose object count reflects actual git state
- last_commit_ts is ISO 8601 string
acceptance_criteria:
- All required git health fields present per plan
- No MCP command created in this step
- No writes to git repos
observations_template:
- Did collect_git_health report correct repo size?
- Was the loose object count accurate?
- Was last_commit_ts in ISO 8601 format?

format: atomic_step
status: pending
step_id: A-001
name: repo-metrics
title: GitHealthRecord and collect_git_health in git_health.py
parent_step_id: T-002
parent_step_name: git-health
parent_global_id: G-005
parent_global_name: service-diagnostics
technical_specification_excerpt: "Per plan git health metrics:\n  repo_size_bytes:\
  \ from git count-objects -v (size-pack field * 1024)\n  loose_object_count: from\
  \ git count-objects -v (count field)\n  last_commit_ts: from git log -1 --format=%cI\
  \ (ISO 8601)\nplans_root from config; never from MCP params.\nNo session; disk-read\
  \ via git commands only.\n"
global_step_context: 'G-005: Service diagnostics. git_health.py provides git metrics
  for plan_service_status.

  '
tactical_step_context: "T-002: Git health collector. Single atomic step owning all\
  \ of git_health.py.\n\nGitHealthRecord dataclass:\n  plan_id: str\n  repo_size_bytes:\
  \ int\n  loose_object_count: int\n  last_commit_ts: str  # ISO 8601 or empty string\
  \ if no commits\n\ncollect_git_health(plans_root) -> list[GitHealthRecord]:\n  Walk\
  \ plan directories under plans_root.\n  For each:\n    Run: git -C plan_dir count-objects\
  \ -v\n    Parse: count (loose objects) and size-pack (kilobytes -> bytes * 1024)\n\
  \    Run: git -C plan_dir log -1 --format=%cI\n    Build GitHealthRecord.\n  Return\
  \ list.\n"
atomic_operation: Implement GitHealthRecord and collect_git_health in plan_manager/diagnostics/git_health.py
intent: 'Create plan_manager/diagnostics/git_health.py with GitHealthRecord dataclass

  and collect_git_health function.

  '
responsibilities:
- Implement GitHealthRecord with plan_id, repo_size_bytes, loose_object_count, last_commit_ts
- Implement collect_git_health(plans_root) -> list[GitHealthRecord]
- Use subprocess git calls
- No writes; no MCP command
code_file_target: plan_manager/diagnostics/git_health.py
estimated_lines: 70
max_context_for_qwen63_8k: This atomic step is self-contained
depends_on: []
deliverables:
- GitHealthRecord dataclass with 4 fields
- collect_git_health(plans_root) -> list[GitHealthRecord]
constraints:
- Read-only git commands; no writes
- No MCP command in this file
acceptance_criteria:
- collect_git_health returns correct repo size and object count
- last_commit_ts is ISO 8601
observations_template:
- Did collect_git_health report correct repo size?
- Was loose_object_count accurate?
- Was last_commit_ts in correct ISO 8601 format?

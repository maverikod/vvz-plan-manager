format: atomic_step
status: pending
step_id: A-003
name: nav-tree-command
title: PlanTreeCommand in plan_tree.py
parent_step_id: T-003
parent_step_name: nav-list-tree
parent_global_id: G-001
parent_global_name: mcp-api-surface
technical_specification_excerpt: 'plan_tree(plan_id, node_id?, depth?) -> nested tree
  of node records

  plan_id required. node_id optional (default spec). depth optional.

  Every node record includes lock_status.

  No session required. Disk-read only.

  Tree structure: {node_id, title, level, lock_status, children: [subtree...]}

  '
global_step_context: 'G-001: MCP API surface. nav_shared from G-001/T-003/A-001.

  '
tactical_step_context: "T-003: Navigation list and tree.\nA-003 owns plan_manager/commands/plan_tree.py.\n\
  A-001 (nav_shared.py) provides build_node_record and list_children.\n\nexecute()\
  \ workflow:\n  1. Resolve plans_root and editor_lock_dir from config\n  2. Start\
  \ from node_id (default spec)\n  3. Recursively build tree using build_node_record\
  \ and list_children\n  4. Respect depth limit if provided\n  5. Return nested dict\
  \ structure\n"
atomic_operation: Implement PlanTreeCommand in plan_manager/commands/plan_tree.py
intent: 'Create plan_manager/commands/plan_tree.py with PlanTreeCommand.

  Renders the plan node tree recursively from root or a given node_id.

  '
responsibilities:
- Implement PlanTreeCommand with name plan_tree
- get_schema: required plan_id, optional node_id and depth
- Recursive tree rendering with depth limit support
- lock_status in every node record
code_file_target: plan_manager/commands/plan_tree.py
estimated_lines: 65
max_context_for_qwen63_8k: This atomic step is self-contained
depends_on:
- G-001/T-003/A-001 build_node_record, list_children from plan_manager/commands/nav_shared.py
deliverables:
- PlanTreeCommand with complete MCP command interface
- Recursive tree with depth limit
- lock_status in every node record
constraints:
- Disk-read only; no writes
- Do not modify nav_shared.py (owned by A-001)
acceptance_criteria:
- plan_tree returns correct hierarchy from root
- depth=1 returns only root and direct children
- lock_status present in every node
observations_template:
- Did plan_tree render the correct tree structure?
- Does depth limiting work correctly?
- Was lock_status present in every returned node record?

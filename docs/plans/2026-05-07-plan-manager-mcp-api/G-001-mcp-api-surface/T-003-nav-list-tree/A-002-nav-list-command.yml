format: atomic_step
status: pending
step_id: A-002
name: nav-list-command
title: PlanListCommand in plan_list.py
parent_step_id: T-003
parent_step_name: nav-list-tree
parent_global_id: G-001
parent_global_name: mcp-api-surface
technical_specification_excerpt: 'plan_list() -> list of plan metadata dicts

  No required params. plans_root from server config.

  Returns list of {plan_id, ...metadata} for all plans found in plans_root.

  No session required. Disk-read only.

  '
global_step_context: 'G-001: MCP API surface. plan_manager owns tree structure only.

  nav_shared utilities provided by G-001/T-003/A-001 (plan_manager/commands/nav_shared.py).

  '
tactical_step_context: "T-003: Navigation list and tree.\nA-002 owns plan_manager/commands/plan_list.py.\n\
  A-001 (nav_shared.py) provides scan_plans and build_node_record.\n\nexecute() workflow:\n\
  \  1. Resolve plans_root from config\n  2. plans = scan_plans(plans_root)\n  3.\
  \ For each plan_id build summary dict {plan_id}\n  4. Return list of summaries\n"
atomic_operation: Implement PlanListCommand in plan_manager/commands/plan_list.py
intent: 'Create plan_manager/commands/plan_list.py with PlanListCommand.

  Enumerates all plans in plans_root and returns plan metadata list.

  '
responsibilities:
- Implement PlanListCommand with name plan_list
- get_schema with no required params
- execute calls scan_plans and returns list of plan metadata dicts
- No session required
code_file_target: plan_manager/commands/plan_list.py
estimated_lines: 50
max_context_for_qwen63_8k: This atomic step is self-contained
depends_on:
- G-001/T-003/A-001 scan_plans from plan_manager/commands/nav_shared.py
deliverables:
- PlanListCommand with complete MCP command interface
- Returns list of plan metadata dicts including plan_id
constraints:
- Disk-read only; no writes
- Do not import from mcp_proxy_adapter internals
acceptance_criteria:
- plan_list returns all plan_ids from plans_root
- No session required
observations_template:
- Did plan_list find all plans in plans_root?
- Is the command callable without a session_id?

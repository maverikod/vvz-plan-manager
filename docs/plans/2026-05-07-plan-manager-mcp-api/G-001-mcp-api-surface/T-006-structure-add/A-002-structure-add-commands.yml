format: atomic_step
status: pending
step_id: A-002
name: structure-add-commands
title: PlanAddGlobalCommand, PlanAddTacticalCommand, PlanAddAtomicCommand
parent_step_id: T-006
parent_step_name: structure-add
parent_global_id: G-001
parent_global_name: mcp-api-surface
technical_specification_excerpt: 'plan_add_global(plan_id, slug) -> {new_node_id,
  created_path, commit_sha}

  plan_add_tactical(plan_id, parent_node_id, slug) -> {new_node_id, created_path,
  commit_sha}

  plan_add_atomic(plan_id, parent_node_id, slug) -> {new_node_id, created_path, commit_sha}

  NODE_NOT_FOUND if parent does not exist.

  Exactly one git commit per call on plan main.

  No lock check. plans_root from config.

  '
global_step_context: 'G-001: MCP API surface.

  Helpers from G-001/T-006/A-001 in same file.

  '
tactical_step_context: "T-006: Structure add.\nA-002 owns three command classes in\
  \ plan_manager/commands/plan_add.py.\nA-001 (same file) provides allocate_next_prefix,\
  \ generate_*_stub, commit_addition.\n\nplan_add_global.execute():\n  g_num = allocate_next_prefix(plan_dir,\
  \ \"G\")\n  stub_path, _ = generate_global_stub(plan_dir, g_num, slug)\n  new_node_id\
  \ = f\"G-{g_num:03d}\"\n  sha = commit_addition(plan_dir, stub_path, new_node_id)\n\
  \  return {new_node_id, created_path, commit_sha: sha}\n\nplan_add_tactical.execute():\n\
  \  Validate parent_node_id is G-level; raise NODE_NOT_FOUND if G dir missing.\n\
  \  Resolve G dir. t_num = allocate_next_prefix(g_dir, \"T\")\n  stub_path, _ = generate_tactical_stub(...)\n\
  \  sha = commit_addition(...)\n\nplan_add_atomic is analogous using T-level parent.\n"
atomic_operation: Implement three PlanAdd*Command classes in plan_manager/commands/plan_add.py
intent: 'Add PlanAddGlobalCommand, PlanAddTacticalCommand, PlanAddAtomicCommand

  to plan_manager/commands/plan_add.py using helpers from A-001.

  '
responsibilities:
- Implement all three command classes with full MCP interface
- Validate parent node existence before creating child
- Return new_node_id, created_path, commit_sha
code_file_target: plan_manager/commands/plan_add.py
estimated_lines: 130
max_context_for_qwen63_8k: This atomic step is self-contained
depends_on:
- G-001/T-006/A-001 allocate_next_prefix, generate_*_stub, commit_addition in same
  file
- G-001/T-001/A-004 resolve_node_id from plan_manager/path_resolver.py
deliverables:
- PlanAddGlobalCommand, PlanAddTacticalCommand, PlanAddAtomicCommand
- NODE_NOT_FOUND on missing parent
- Exactly one commit per call on plan main
constraints:
- total file including A-001 helpers under 300 lines
- No lock check (new nodes cannot be locked)
- No session required
acceptance_criteria:
- plan_add_global creates G-NNN-<slug>/README.md and commits
- plan_add_tactical creates T-NNN-<slug>/index.md under correct G dir
- plan_add_atomic creates A-NNN-<slug>.md under correct T dir
- Missing parent returns NODE_NOT_FOUND
observations_template:
- Did plan_add_global create the correct directory and stub file?
- Did plan_add_tactical find and use the correct parent G directory?
- Was exactly one commit produced per call?

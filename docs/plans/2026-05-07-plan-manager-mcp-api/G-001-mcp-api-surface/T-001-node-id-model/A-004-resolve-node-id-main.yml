format: atomic_step
status: pending
step_id: A-004
name: resolve-node-id-main
title: Main resolve_node_id function
parent_step_id: T-001
parent_step_name: node-id-model
parent_global_id: G-001
parent_global_name: mcp-api-surface
technical_specification_excerpt: "Plan Manager MCP API implements structured-tree\
  \ plan editor. Plans are git repositories on disk.\nNode IDs are stable string identifiers\
  \ mapped to files/directories by numeric prefix.\n\nPath Resolution Rules:\n- spec:\
  \ \"README.md\" (at plan root)\n- G-NNN: \"G-NNN-*/README.md\" (numeric prefix only,\
  \ ignore slug)\n- G-NNN/T-NNN: \"G-NNN-*/T-NNN-*/index.md\" \n- G-NNN/T-NNN/A-NNN:\
  \ \"G-NNN-*/T-NNN-*/A-NNN-*.md\"\n\nResolution uses numeric prefix only. Slug after\
  \ dash ignored for matching.\nplans_root configured in server config, NOT passed\
  \ as MCP command parameter.\nresolve_node_id always receives plans_root as resolved\
  \ Path from caller.\n"
global_step_context: 'G-001: MCP API surface

  Define and implement the complete public MCP command surface for plan_manager.

  Node IDs are stable string identifiers mapped to files and directories by numeric
  prefix.

  All other G-001 tactical steps depend on this foundation module for path resolution.

  '
tactical_step_context: 'T-001: Node ID model

  Implement deterministic mapping between public node_id strings and files/directories.


  T-001 Deliverables:

  - NodeId dataclass with level, g_num, t_num, a_num fields (A-001)

  - parse_node_id function returning NodeId (A-002)

  - Foundation path utilities (A-003)

  - resolve_node_id main function (A-004)


  Available from previous atomic steps in same file:

  - NodeId dataclass (A-001)

  - parse_node_id function (A-002)

  - find_directories_with_prefix function (A-003)

  - check_duplicate_prefixes function (A-003)

  '
atomic_operation: Implement main resolve_node_id function using NodeId parsing and
  path utilities
intent: 'Implement the main resolve_node_id function in plan_manager/path_resolver.py.

  This function combines NodeId parsing with path resolution to convert

  node_id strings into concrete file paths within plan repositories.

  '
responsibilities:
- Implement resolve_node_id main function with three parameters
- Use parse_node_id to parse the node_id string
- Apply appropriate path resolution logic for each node level
- Validate file existence and return concrete Path
- Handle all four node_id forms with proper error reporting
code_file_target: plan_manager/path_resolver.py
estimated_lines: 90
max_context_for_qwen63_8k: This atomic step is self-contained
depends_on:
- A-001 (NodeId dataclass from plan_manager/node_id.py)
- A-002 (parse_node_id function from plan_manager/node_id.py)
- A-003 (Foundation utilities in same file)
resolution_logic_by_level:
  spec: Return plans_root / plan_id / 'README.md'
  global: Find G-{g_num}-* directory, return its README.md
  tactical: Find G-{g_num}-*/T-{t_num}-* directory, return its index.md
  atomic: Find G-{g_num}-*/T-{t_num}-*/A-{a_num}-*.md file
deliverables:
- resolve_node_id function with correct signature
- Integration with parse_node_id and foundation utilities
- Proper error handling for missing files/directories
- Import statements for NodeId and parse_node_id
- Path existence validation before return
constraints:
- Must use parse_node_id for input validation
- Must use foundation utilities from A-003
- plans_root always comes from caller, never from MCP params
- Pure utility function - no MCP dependencies
acceptance_criteria:
- Function signature matches specification exactly
- All four node_id forms resolve to correct paths
- Missing files/directories raise appropriate errors
- Function integrates cleanly with parsing and utilities
observations_template:
- Does resolve_node_id correctly handle "spec" -> README.md?
- Does "G-001" resolve to correct G-001-*/README.md path?
- Does "G-001/T-002/A-003" resolve through full path correctly?
- Are missing directories detected and reported properly?

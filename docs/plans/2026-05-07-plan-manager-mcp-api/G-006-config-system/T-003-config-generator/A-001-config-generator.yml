format: atomic_step
status: pending
step_id: A-001
name: config-generator
title: PlanManagerConfigGenerator extending SimpleConfigGenerator
parent_step_id: T-003
parent_step_name: config-generator
parent_global_id: G-006
parent_global_name: config-system
technical_specification_excerpt: 'Plan Manager MCP API - config generator:

  - PlanManagerConfigGenerator extends SimpleConfigGenerator

  - Call super().generate() first to get adapter config dict

  - Inject plan_manager section with all five fields and their defaults

  - Return JSON-serializable dict

  - Config format is JSON only

  - Do not reimplement SimpleConfigGenerator internals

  - Do not add YAML or TOML output formats

  '
global_step_context: 'G-006: Config system.

  PlanManagerConfigGenerator produces a config template for deployment.

  Depends on T-001 (PlanManagerConfigSection) and T-002 (PlanManagerConfigValidator).

  '
tactical_step_context: "T-003: Config generator.\nThis is a single atomic step owning\
  \ the entire config_generator.py file.\n\nConsumed contracts:\n  PlanManagerConfigSection\
  \ from plan_manager/config/config_section.py (G-006/T-001)\n  SimpleConfigGenerator\
  \ from core/config/simple_config_generator.py (do not modify)\n\nPlanManagerConfigGenerator(SimpleConfigGenerator):\n\
  \  generate() -> dict:\n    config = super().generate()\n    config[\"plan_manager\"\
  ] = PlanManagerConfigSection().dict()\n    return config\n\nNote: embed_url has\
  \ no default; the generated template should include a placeholder\nstring like \"\
  http://localhost:11434/api/embed\" to make the template usable.\n"
atomic_operation: Implement PlanManagerConfigGenerator in plan_manager/config/config_generator.py
intent: 'Create plan_manager/config/config_generator.py with PlanManagerConfigGenerator

  extending SimpleConfigGenerator to produce a JSON config template with plan_manager
  section.

  '
responsibilities:
- Implement PlanManagerConfigGenerator extending SimpleConfigGenerator
- generate() calls super().generate() and injects plan_manager section
- Return JSON-serializable dict with all five fields and defaults
- embed_url placeholder must be present in generated config
code_file_target: plan_manager/config/config_generator.py
estimated_lines: 30
max_context_for_qwen63_8k: This atomic step is self-contained
depends_on:
- G-006/T-001/A-001 PlanManagerConfigSection from plan_manager/config/config_section.py
- G-006/T-002/A-001 PlanManagerConfigValidator from plan_manager/config/config_validator.py
deliverables:
- PlanManagerConfigGenerator class extending SimpleConfigGenerator
- generate() returning dict with plan_manager section
- Output is JSON-serializable
constraints:
- Do not reimplement SimpleConfigGenerator internals
- Do not modify adapter config components
- JSON-only output
acceptance_criteria:
- generate() returns dict containing plan_manager section with all five fields
- SimpleConfigGenerator called first via super().generate()
- Output is JSON-serializable
observations_template:
- Did generate() return the plan_manager section with correct defaults?
- Was the adapter config from super().generate() preserved?
- Was the output JSON-serializable?

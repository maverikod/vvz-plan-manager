step_id: A-014
parent_tactical_step: T-003
name: relation-store-remove-relation
target_file: plan_manager/domain/relation_store.py
operation: modify_file
priority: 3
depends_on: [A-013]
concepts: [C-004, C-003, C-035]
prompt: |
  Modify the existing Python source file at exact project-relative path plan_manager/domain/relation_store.py.

  Operation: modify_file. The current full content of the file (state after all lower-priority AS on this file have been applied) is exactly the following:

  ```python
  """Persistence functions for the Relation domain entity (MRS concept
  C-004).


  This function writes Relation rows in table `relation`, scoped to one
  plan via the plan_uuid column. Field validation and endpoint-existence
  checks are delegated to plan_manager.domain.relation before any row is
  written.

  """


  import uuid

  import psycopg

  from plan_manager.domain.relation import Relation, validate_relation, check_relation_endpoints_exist
  from plan_manager.domain.concept_store import list_concept_ids


  def insert_relation(
      conn: psycopg.Connection, plan_uuid: uuid.UUID, relation: Relation
  ) -> uuid.UUID:
      """Validate and insert a new Relation row scoped to one plan.

      Args:
          conn: Open psycopg database connection.
          plan_uuid: UUID of the owning plan.
          relation: The Relation instance to persist.

      Returns:
          The generated uuid primary identity of the inserted row.

      Raises:
          RelationValidationError: propagated unmodified from
              validate_relation or check_relation_endpoints_exist if
              relation fails field validation or either endpoint is not
              among the plan's existing concept_id values.
      """
      validate_relation(relation)
      check_relation_endpoints_exist(relation, list_concept_ids(conn, plan_uuid))
      row_uuid = uuid.uuid4()
      with conn.cursor() as cur:
          cur.execute(
              "INSERT INTO relation (uuid, plan_uuid, from_concept, to_concept, type) "
              "VALUES (%s, %s, %s, %s, %s)",
              (row_uuid, plan_uuid, relation.from_concept, relation.to_concept, relation.type),
          )
      return row_uuid


  def list_relations(
      conn: psycopg.Connection, plan_uuid: uuid.UUID
  ) -> list[tuple[str, str, str]]:
      """List every (from_concept, to_concept, type) triple for one plan.

      Args:
          conn: Open psycopg database connection.
          plan_uuid: UUID of the owning plan.

      Returns:
          List of (from_concept, to_concept, type) tuples for the given
          plan_uuid, ordered ascending by (from_concept, to_concept, type).
      """
      with conn.cursor() as cur:
          cur.execute(
              "SELECT from_concept, to_concept, type FROM relation "
              "WHERE plan_uuid = %s ORDER BY from_concept, to_concept, type",
              (plan_uuid,),
          )
          rows = cur.fetchall()
      return [(row[0], row[1], row[2]) for row in rows]
  ```

  MRS excerpt (concept definitions relevant to this AS):
  - C-004 Relation: typed directed edge between two concepts restricted to seven allowed types; a Relation depends on the existence of the Concepts it connects.
  - C-003 Concept: identifier pattern is C-NNN, unique within the plan.
  - C-035 Database: entities are stored as rows; accessed via psycopg 3 with plain SQL, no ORM; runtime code never emits DDL.

  Required change: append the following function definition to the end of the file, after `list_relations`, separated from it by exactly two blank lines. Do not change any existing line in the file shown above.

  ```python



  def remove_relation(
      conn: psycopg.Connection,
      plan_uuid: uuid.UUID,
      from_concept: str,
      to_concept: str,
      type: str,
  ) -> uuid.UUID:
      """Delete the Relation row matching from_concept, to_concept, and type.

      Note: the parameter named `type` shadows the Python builtin `type`;
      this is kept because it matches the domain vocabulary (Relation.type
      in plan_manager.domain.relation).

      Args:
          conn: Open psycopg database connection.
          plan_uuid: UUID of the owning plan.
          from_concept: concept_id of the source concept.
          to_concept: concept_id of the target concept.
          type: Relation type of the row to delete.

      Returns:
          The uuid primary identity of the deleted row.

      Raises:
          ValueError: If no row matches all three of from_concept,
              to_concept, and type for the given plan_uuid (message is
              exactly "relation not found").
      """
      with conn.cursor() as cur:
          cur.execute(
              "SELECT uuid FROM relation WHERE plan_uuid = %s AND from_concept = %s "
              "AND to_concept = %s AND type = %s",
              (plan_uuid, from_concept, to_concept, type),
          )
          row = cur.fetchone()
      if row is None:
          raise ValueError("relation not found")
      row_uuid = row[0]
      with conn.cursor() as cur:
          cur.execute(
              "DELETE FROM relation WHERE plan_uuid = %s AND from_concept = %s "
              "AND to_concept = %s AND type = %s",
              (plan_uuid, from_concept, to_concept, type),
          )
      return row_uuid
  ```

  Constraints:
  - The function signature must be exactly `def remove_relation(conn: psycopg.Connection, plan_uuid: uuid.UUID, from_concept: str, to_concept: str, type: str) -> uuid.UUID:`. The parameter is named `type`, matching the domain vocabulary of Relation.type in plan_manager.domain.relation; this shadows the Python builtin `type` and must be kept exactly as named, with the shadowing noted in the function's docstring exactly as shown in the code above.
  - Use only names already defined or imported in this file (`psycopg`, `uuid`). Add no new imports.
  - First, open a cursor via `conn.cursor()` as a context manager and execute exactly the SQL string "SELECT uuid FROM relation WHERE plan_uuid = %s AND from_concept = %s AND to_concept = %s AND type = %s" with parameter tuple `(plan_uuid, from_concept, to_concept, type)`, then call `cur.fetchone()` and bind the result to `row`.
  - If `row is None`, raise `ValueError("relation not found")`. Otherwise bind `row_uuid = row[0]`.
  - Then open a second cursor via `conn.cursor()` as a context manager and execute exactly the SQL string "DELETE FROM relation WHERE plan_uuid = %s AND from_concept = %s AND to_concept = %s AND type = %s" with the same parameter tuple `(plan_uuid, from_concept, to_concept, type)`.
  - Return `row_uuid` as the function's return value.
  - Do not catch or translate any exception raised by psycopg during either `cur.execute` call; let it propagate unmodified.
  - Do not emit any DDL statement (no CREATE, ALTER, or DROP).
  - Do not modify, reorder, or remove any part of the file content shown above as the current state. Only append the new function.
  - Do not add any code beyond what is shown above. Do not add logging. Do not add a module-level connection or any module-level mutable state. Do not add any function other than `remove_relation`.

verification:
  type: static_analysis
  target: plan_manager.domain.relation_store.remove_relation
  expected: >
    The function remove_relation accepts a psycopg connection, a plan_uuid, from_concept, to_concept, and type (the parameter named type shadows the builtin, matching Relation.type vocabulary), and returns a uuid.UUID. It raises ValueError with message 'relation not found' when no row in table relation matches all three of from_concept, to_concept, and type for the given plan_uuid. Otherwise it deletes the matching row and returns its uuid primary identity as read before deletion.
status: draft

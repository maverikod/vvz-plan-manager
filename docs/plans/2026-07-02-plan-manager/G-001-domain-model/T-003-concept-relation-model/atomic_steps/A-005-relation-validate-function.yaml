step_id: A-005
parent_tactical_step: T-003
name: relation-validate-function
target_file: plan_manager/domain/relation.py
operation: modify_file
priority: 2
depends_on: [A-004]
concepts: [C-004, C-003]
prompt: |
  Modify the existing Python source file at exact project-relative path
  plan_manager/domain/relation.py.


  Operation: modify_file. The current full content of the file (state after
  all lower-priority AS on this file have been applied) is exactly the
  following:


  ```python

  """Relation domain entity (MRS concept C-004).


  A Relation is a typed directed edge from one Concept (C-003) to another,
  restricted to exactly seven types: uses, owns, implements, extends,
  depends_on, produces, consumes. Free-form types are rejected at write
  time. A Relation depends on the existence of the Concepts (identified by
  concept_id, pattern C-NNN) it connects. Self-referencing relations (a
  concept related to itself) are permitted.

  """


  from dataclasses import dataclass

  from typing import List


  from plan_manager.domain.concept import CONCEPT_ID_PATTERN


  RELATION_TYPES = frozenset(
      {"uses", "owns", "implements", "extends", "depends_on", "produces", "consumes"}
  )


  class RelationValidationError(ValueError):
      """Raised when a Relation fails write-time validation."""


  @dataclass
  class Relation:
      """A single typed directed edge between two concepts.

      Attributes:
          from_concept: concept_id (pattern C-NNN) of the source concept.
          to_concept: concept_id (pattern C-NNN) of the target concept.
          type: Relation type; must be one of RELATION_TYPES.
      """

      from_concept: str
      to_concept: str
      type: str

  ```


  MRS excerpt (concept definitions relevant to this AS):

  - C-004 Relation: types are uses, owns, implements, extends, depends_on,
    produces, consumes; free-form types rejected at write time.

  - C-003 Concept: identifier pattern is C-NNN.


  Required change: append the following function definition to the end of
  the file, after the `Relation` dataclass, separated from it by exactly
  two blank lines. Do not change any existing line in the file shown above.


  ```python

  def validate_relation(relation: Relation) -> None:
      """Validate a Relation's fields at write time.

      Args:
          relation: The Relation instance to validate.

      Raises:
          RelationValidationError: If any of the following hold:
              - relation.type is not a member of RELATION_TYPES (the set
                {"uses", "owns", "implements", "extends", "depends_on",
                "produces", "consumes"}).
              - relation.from_concept does not match CONCEPT_ID_PATTERN
                (pattern "^C-\\d{3}$", e.g. "C-003").
              - relation.to_concept does not match CONCEPT_ID_PATTERN
                (pattern "^C-\\d{3}$", e.g. "C-003").
      """
      if relation.type not in RELATION_TYPES:
          raise RelationValidationError(
              f"type '{relation.type}' is not one of {sorted(RELATION_TYPES)}"
          )
      if not CONCEPT_ID_PATTERN.match(relation.from_concept):
          raise RelationValidationError(
              f"from_concept '{relation.from_concept}' does not match "
              f"pattern '{CONCEPT_ID_PATTERN.pattern}'"
          )
      if not CONCEPT_ID_PATTERN.match(relation.to_concept):
          raise RelationValidationError(
              f"to_concept '{relation.to_concept}' does not match pattern "
              f"'{CONCEPT_ID_PATTERN.pattern}'"
          )
  ```


  Constraints:

  - The function signature must be exactly `def validate_relation(relation:
    Relation) -> None:` using the `Relation` type already defined in this
    file.

  - Use only names already defined in the file (`RELATION_TYPES`,
    `RelationValidationError`, `CONCEPT_ID_PATTERN`, `Relation`). Add no
    new imports.

  - Validation order must be exactly: type membership, from_concept
    pattern, to_concept pattern, as shown in the code above.

  - Do not add a check comparing from_concept to to_concept; equal values
    are valid (self-referencing relations are permitted per C-005 to C-005
    in the MRS).

  - Every raised exception must be an instance of `RelationValidationError`
    with the exact message text shown in the code above (f-strings as
    written).

  - Do not modify, reorder, or remove any part of the file content shown
    above as the current state. Only append the new function.
verification:
  type: static_analysis
  target: plan_manager.domain.relation.validate_relation
  expected: >
    The function validate_relation accepts one Relation argument and
    returns None. It raises RelationValidationError when type is not one
    of the seven allowed values, or when from_concept or to_concept does
    not match CONCEPT_ID_PATTERN. It returns normally when type is allowed
    and both endpoints match the pattern, including when from_concept
    equals to_concept.
status: done

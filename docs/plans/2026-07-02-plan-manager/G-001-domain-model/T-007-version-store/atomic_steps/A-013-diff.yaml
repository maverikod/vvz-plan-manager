step_id: A-013
parent_tactical_step: T-007
name: diff
target_file: plan_manager/storage/version_ops.py
operation: modify_file
priority: 4
depends_on: [A-010, A-011, A-012]
concepts: [C-018]
prompt: |
  Modify the existing file `plan_manager/storage/version_ops.py` to add one new
  import and one new function, `diff`. Operation: modify_file. Target file:
  `plan_manager/storage/version_ops.py`.

  Full current content of the target file (post-prior-AS state — this is the exact
  content to modify, not a summary):

  ```python
  """Version-store read operations: state reconstruction, log, diff, checkout, revert."""
  import uuid

  import psycopg

  from plan_manager.storage.version_store import VersionStoreError


  def state_at(
      conn: psycopg.Connection, plan_uuid: uuid.UUID, revision_uuid: uuid.UUID
  ) -> dict[uuid.UUID, uuid.UUID]:
      """Reconstruct the entity state at a revision by walking the parent chain.

      Walks from ``revision_uuid`` back to the root revision (parent_uuid is
      None) via each revision's ``parent_uuid``. For each entity_uuid touched
      by any revision on the walk, the node_version from the FIRST revision
      encountered during the walk is kept (i.e. the most recent revision on
      the walk that touched that entity); node versions from revisions
      encountered later in the walk for the same entity_uuid are ignored.

      :param conn: open database connection.
      :param plan_uuid: uuid of the plan to reconstruct state for.
      :param revision_uuid: uuid of the revision to reconstruct state at.
      :return: dict mapping entity_uuid to the node_version uuid holding that
          entity's content at ``revision_uuid``.
      :raises VersionStoreError: if ``revision_uuid`` does not exist for
          ``plan_uuid``.
      """
      state: dict[uuid.UUID, uuid.UUID] = {}
      current_uuid: uuid.UUID | None = revision_uuid
      while current_uuid is not None:
          cur = conn.execute(
              "SELECT parent_uuid, node_version_uuids FROM revision WHERE plan_uuid = %s AND uuid = %s",
              (plan_uuid, current_uuid),
          )
          row = cur.fetchone()
          if row is None:
              raise VersionStoreError(
                  f"revision not found: plan_uuid={plan_uuid} uuid={current_uuid}"
              )
          parent_uuid, node_version_uuids = row
          if node_version_uuids:
              nv_cur = conn.execute(
                  "SELECT uuid, entity_uuid FROM node_version WHERE uuid = ANY(%s)",
                  (list(node_version_uuids),),
              )
              for node_version_uuid, entity_uuid in nv_cur.fetchall():
                  if entity_uuid not in state:
                      state[entity_uuid] = node_version_uuid
          current_uuid = parent_uuid
      return state


  def get_node_version_content(conn: psycopg.Connection, node_version_uuid: uuid.UUID) -> dict:
      """Fetch the content dict of a node_version row by its uuid.

      :param conn: open database connection.
      :param node_version_uuid: uuid of the node_version row to fetch.
      :return: the JSON content dict stored on that node_version row.
      :raises VersionStoreError: if no node_version row with that uuid exists.
      """
      cur = conn.execute(
          "SELECT content FROM node_version WHERE uuid = %s",
          (node_version_uuid,),
      )
      row = cur.fetchone()
      if row is None:
          raise VersionStoreError(f"node_version not found: uuid={node_version_uuid}")
      return row[0]


  def log(conn: psycopg.Connection, plan_uuid: uuid.UUID, revision_uuid: uuid.UUID) -> list[dict]:
      """List revision records from ``revision_uuid`` back to the root revision.

      Walks from ``revision_uuid`` to the root (parent_uuid is None) via each
      revision's ``parent_uuid``, in that order (most recent first).

      :param conn: open database connection.
      :param plan_uuid: uuid of the plan to list revisions for.
      :param revision_uuid: uuid of the revision to start the walk from.
      :return: list of revision record dicts, each with keys ``uuid``,
          ``parent_uuid``, ``author``, ``message``, ``created_at``, and
          ``node_version_uuids``, ordered from ``revision_uuid`` to the root.
      :raises VersionStoreError: if ``revision_uuid`` does not exist for
          ``plan_uuid``.
      """
      records: list[dict] = []
      current_uuid: uuid.UUID | None = revision_uuid
      while current_uuid is not None:
          cur = conn.execute(
              "SELECT uuid, parent_uuid, author, message, created_at, node_version_uuids "
              "FROM revision WHERE plan_uuid = %s AND uuid = %s",
              (plan_uuid, current_uuid),
          )
          row = cur.fetchone()
          if row is None:
              raise VersionStoreError(
                  f"revision not found: plan_uuid={plan_uuid} uuid={current_uuid}"
              )
          rev_uuid, parent_uuid, author, message, created_at, node_version_uuids = row
          records.append(
              {
                  "uuid": rev_uuid,
                  "parent_uuid": parent_uuid,
                  "author": author,
                  "message": message,
                  "created_at": created_at,
                  "node_version_uuids": list(node_version_uuids) if node_version_uuids else [],
              }
          )
          current_uuid = parent_uuid
      return records
  ```

  Required changes:
  1. Add one new import after `import psycopg`, before the blank line that precedes
     `from plan_manager.storage.version_store import VersionStoreError`, so the
     import block becomes:

  ```python
  import uuid

  import psycopg

  from plan_manager.storage.canonical import canonical_json
  from plan_manager.storage.version_store import VersionStoreError
  ```

     `canonical_json` has the exact signature `def canonical_json(value: object) ->
     bytes` (defined in `plan_manager/storage/canonical.py`, sibling-owned): it
     returns the canonical JSON serialization of its argument as UTF-8 bytes, with
     sorted keys and no insignificant whitespace, so two values compare equal under
     `canonical_json` iff they are semantically identical JSON.
  2. Append a new function below `log`, separated by two blank lines, with this
     exact signature:

  ```python
  def diff(
      conn: psycopg.Connection,
      plan_uuid: uuid.UUID,
      revision_a: uuid.UUID,
      revision_b: uuid.UUID,
  ) -> dict:
  ```

  Docstring (reST-style, `:param` entries in parameter order then `:return`), exact
  text and format:

  ```
  Diff two revisions into added, removed, and changed entities.

  Compares ``state_at(revision_a)`` and ``state_at(revision_b)`` on
  entity_uuid keys. An entity present only in ``revision_b`` is added; an
  entity present only in ``revision_a`` is removed; an entity present in
  both but with a different node_version uuid is changed, with
  field-level detail: the sorted list of top-level content keys whose
  canonical JSON differs between the two node versions' content.

  :param conn: open database connection.
  :param plan_uuid: uuid of the plan to diff.
  :param revision_a: uuid of the first (base) revision.
  :param revision_b: uuid of the second (compared) revision.
  :return: dict with keys ``added`` (list of entity_uuid), ``removed``
      (list of entity_uuid), and ``changed`` (list of dicts each with
      keys ``entity_uuid`` and ``fields``, ``fields`` being the sorted
      list of differing top-level content keys).
  ```

  Function body, exactly this logic (`state_at` is the function already defined
  earlier in this same file; `get_node_version_content` is the function already
  defined earlier in this same file):

  ```python
      state_a = state_at(conn, plan_uuid, revision_a)
      state_b = state_at(conn, plan_uuid, revision_b)
      added = sorted(state_b.keys() - state_a.keys(), key=str)
      removed = sorted(state_a.keys() - state_b.keys(), key=str)
      changed = []
      for entity_uuid in sorted(state_a.keys() & state_b.keys(), key=str):
          node_version_a = state_a[entity_uuid]
          node_version_b = state_b[entity_uuid]
          if node_version_a == node_version_b:
              continue
          content_a = get_node_version_content(conn, node_version_a)
          content_b = get_node_version_content(conn, node_version_b)
          all_keys = set(content_a.keys()) | set(content_b.keys())
          fields = sorted(
              key
              for key in all_keys
              if canonical_json(content_a.get(key)) != canonical_json(content_b.get(key))
          )
          changed.append({"entity_uuid": entity_uuid, "fields": fields})
      return {"added": added, "removed": removed, "changed": changed}
  ```

  Constraints (all explicit, no deviation):
  - Do not modify `state_at`, `get_node_version_content`, or `log` in any way.
  - Insert the `from plan_manager.storage.canonical import canonical_json` line
    exactly where shown, before the `version_store` import line; do not reorder any
    other existing import line.
  - Set operations (`-`, `&`) on dict keys and `sorted(..., key=str)` are used
    exactly as shown for deterministic, comparison-safe ordering of `uuid.UUID`
    values (which are not natively orderable by `<` across all Python versions in a
    way this AS relies on; sorting by their string form is explicit and
    deterministic).
  - A node_version comparison for "changed" uses node_version uuid equality
    (`node_version_a == node_version_b`), not content comparison; only entities
    whose node_version differs are examined further.
  - The `fields` list for a changed entity contains a key `key` iff
    `canonical_json(content_a.get(key))` differs from
    `canonical_json(content_b.get(key))`, where `.get(key)` returns `None` if the
    key is absent from that content dict (a key present in only one of the two
    contents therefore appears in `fields` unless both sides evaluate to `None`).
  - Do not call `conn.commit()` inside this function (it performs no writes).
  - Resulting file must be valid, importable Python 3.12+ source, well under 400
    lines total.

  MRS excerpt (concept referenced: C-018):

  ```yaml
  - concept_id: C-018
    name: VersionStore
    definition: Git-like append-only version model of node versions, revisions, and refs inside the database.
    properties:
    - node versions content-addressed by SHA-256 of canonical JSON (sorted keys, UTF-8)
    - revisions carry parent, author, change set; one mutation produces one revision
    - any intermediate plan state is addressable by revision id
    - refs are plan head plus one per open cascade
    - operations log, diff, checkout-read, revert; history never rewritten
  ```

  This function implements the `diff` operation from the C-018 property
  "operations log, diff, checkout-read, revert; history never rewritten", including
  the field-level detail required for "changed" entities.
verification:
  type: import
  target: plan_manager.storage.version_ops
  expected: >
    The module imports without error and exposes a callable `diff(conn, plan_uuid,
    revision_a, revision_b)` with the exact parameter and return types shown in the
    signature above.
status: done

step_id: A-004
parent_tactical_step: T-001
name: get-open-cascade
target_file: plan_manager/cascade/record.py
operation: modify_file
priority: 4
depends_on: [A-003]
concepts: [C-016, C-035]
status: draft
prompt: |
  Modify the file plan_manager/cascade/record.py. Its full current content,
  as it exists after all lower-priority atomic steps on this file have been
  applied, is exactly:

  """Cascade record entity: lifecycle states and database operations for the cascade table."""

  import uuid
  from dataclasses import dataclass
  from datetime import datetime

  import psycopg


  CASCADE_STATUSES: frozenset[str] = frozenset({"open", "committed", "aborted"})


  class CascadeError(ValueError):
      """Raised on any cascade lifecycle violation."""


  @dataclass(frozen=True)
  class CascadeRecord:
      """A cascade record: one row of the cascade table.

      Fields, in order: uuid (the cascade's own identity), plan_uuid (the
      owning plan), name (the cascade reference name held in the version
      store, "cascade/<cascade uuid>"), base_revision_uuid (the plan head
      revision the cascade was anchored at when opened, or None),
      status (one of the three values in CASCADE_STATUSES), created_at
      (creation timestamp).
      """

      uuid: uuid.UUID
      plan_uuid: uuid.UUID
      name: str
      base_revision_uuid: uuid.UUID | None
      status: str
      created_at: datetime


  def insert_cascade(conn: psycopg.Connection, record: CascadeRecord) -> None:
      """Insert a cascade record into the cascade table.

      Executes an INSERT into the normative `cascade` table with columns
      (uuid, plan_uuid, name, base_revision_uuid, status, created_at), using
      the corresponding fields of `record` as the six parameter values, in
      that column order.

      Raises CascadeError("plan already has an open cascade") if the insert
      violates the database's partial unique index on (plan_uuid) WHERE
      status = 'open' (a psycopg.errors.UniqueViolation), because that index
      allows at most one open cascade per plan.
      """
      try:
          with conn.cursor() as cur:
              cur.execute(
                  "INSERT INTO cascade (uuid, plan_uuid, name, base_revision_uuid, status, created_at) "
                  "VALUES (%s, %s, %s, %s, %s, %s)",
                  (
                      record.uuid,
                      record.plan_uuid,
                      record.name,
                      record.base_revision_uuid,
                      record.status,
                      record.created_at,
                  ),
              )
      except psycopg.errors.UniqueViolation:
          raise CascadeError("plan already has an open cascade")

  Append the following function to the end of the file, separated from
  `insert_cascade` by exactly two blank lines. Do not change anything above
  it; it must remain byte-identical to what is shown above.

  def get_open_cascade(conn: psycopg.Connection, plan_uuid: uuid.UUID) -> CascadeRecord | None:
      """Return the open cascade record for a plan, or None if there is none.

      Executes a SELECT of columns (uuid, plan_uuid, name, base_revision_uuid,
      status, created_at) from the `cascade` table filtered to
      `plan_uuid = %s AND status = 'open'`, fetches at most one row, and
      returns it as a CascadeRecord, or None when no row is found.
      """
      with conn.cursor() as cur:
          cur.execute(
              "SELECT uuid, plan_uuid, name, base_revision_uuid, status, created_at "
              "FROM cascade WHERE plan_uuid = %s AND status = 'open'",
              (plan_uuid,),
          )
          row = cur.fetchone()
      if row is None:
          return None
      return CascadeRecord(
          uuid=row[0],
          plan_uuid=row[1],
          name=row[2],
          base_revision_uuid=row[3],
          status=row[4],
          created_at=row[5],
      )

  Constraints (all binding, no deviation):
  - Function signature is exactly
    `def get_open_cascade(conn: psycopg.Connection, plan_uuid: uuid.UUID) -> CascadeRecord | None:`.
    `psycopg.Connection` refers to the `psycopg` module already imported in
    this file as `import psycopg`. `uuid.UUID` refers to the standard
    library `uuid` module already imported as `import uuid`.
    `CascadeRecord` refers to the dataclass already defined above in this
    same file.
  - The SQL text, column order, and WHERE clause must be exactly as shown,
    with a single `%s` placeholder bound to `(plan_uuid,)`.
  - Use `with conn.cursor() as cur:` as the cursor context manager, and call
    `cur.fetchone()` exactly once, assigning it to `row`, exactly as shown.
  - After the `with` block: if `row is None`, return `None`; otherwise
    construct and return a `CascadeRecord` from the six positional row
    values in exactly the order shown (`row[0]` through `row[5]` mapped to
    `uuid`, `plan_uuid`, `name`, `base_revision_uuid`, `status`,
    `created_at` respectively, using keyword arguments as shown).
  - No exceptions are caught in this function.
  - The docstring is exactly the text shown above.

  MRS excerpt (only the concepts relevant to this step):

  - concept_id: C-016
    name: CascadeChange
    definition: Transactional top-down change set anchored at the plan head revision, published atomically on green gate.
    properties:
    - begin acquires the per-plan lock; commit advances head only when gate is green
    - automatic needs_review propagation per invalidation rule
    - one open cascade per plan; MRS mutations are cascade-only at any status

  - concept_id: C-035
    name: Database
    definition: In-container PostgreSQL with pgvector serving as the single source of truth for all plan data.
    properties:
    - entities as rows; referential integrity at write time
    - every stored entity carries an immutable UUID identity; human-readable ids are scoped names mapped to it
    - hosts the version store, locks, and embedding cache
    - accessed via psycopg 3 with plain SQL; no ORM; runtime code never emits DDL

  Context: this function is the lookup side of the one-open-cascade-per-plan
  rule (CascadeChange, C-016): it lets a caller check, before attempting to
  open a new cascade, whether the plan already has one open row in the
  Database (C-035)'s `cascade` table.
verification:
  type: manual
  target: plan_manager.cascade.record.get_open_cascade
  expected: >
    Reading the function body against the prompt confirms: the SELECT
    statement selects the six columns in the specified order filtered by
    plan_uuid and status='open'; cur.fetchone() is called exactly once;
    when no row is found the function returns None; when a row is found the
    function returns a CascadeRecord built from the row's six positional
    values in the specified order; no exception is caught inside the
    function.

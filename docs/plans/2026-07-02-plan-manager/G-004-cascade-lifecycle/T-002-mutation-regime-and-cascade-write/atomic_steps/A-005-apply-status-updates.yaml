step_id: A-005
parent_tactical_step: T-002
name: apply_status_updates cascade-driven status writes
target_file: plan_manager/cascade/write.py
operation: modify_file
priority: 2
depends_on: [A-004]
concepts: [C-005, C-007, C-018, C-035]
prompt: |
  Modify the file `plan_manager/cascade/write.py`. Its current full
  content (after all lower-priority atomic steps on this file have been
  applied) is exactly:

  ```python
  """Cascade-scoped write path for CascadeChange (C-016).

  Records, for an admitted in-cascade mutation already applied to the
  stored entity rows (C-035), one revision in the version store (C-018)
  attributed to the cascade under the cascade reference, applying
  accompanying step status updates validated by the status model (C-007).
  """

  import uuid

  import psycopg

  from plan_manager.cascade.record import CascadeError, CascadeRecord
  from plan_manager.domain.status_model import validate_transition
  from plan_manager.domain.step_store import Step, get_step
  from plan_manager.storage.version_store import get_ref, record_revision


  def step_snapshot(step: Step, status: str) -> dict:
      """Build the full post-change content snapshot of a step node for
      the version store (C-018).

      The snapshot reflects `status`, not `step.status` — the caller
      passes the new status the step is being written under.

      Args:
          step: the step whose fields are snapshotted.
          status: the status value to record in the snapshot.

      Returns:
          A dict with exactly these keys, in this order: "kind", "uuid",
          "plan_uuid", "parent_step_uuid", "level", "step_id", "slug",
          "fields", "depends_on", "concepts", "status". "kind" is always
          the literal string "step". "uuid" and "plan_uuid" are `str(...)`
          of the corresponding Step uuid fields. "parent_step_uuid" is
          `str(step.parent_step_uuid)` when it is not None, else None.
          "level", "step_id", "slug", "fields", "depends_on", and
          "concepts" are copied verbatim from `step`. "status" is the
          `status` argument, not `step.status`.
      """
      return {
          "kind": "step",
          "uuid": str(step.uuid),
          "plan_uuid": str(step.plan_uuid),
          "parent_step_uuid": str(step.parent_step_uuid) if step.parent_step_uuid is not None else None,
          "level": step.level,
          "step_id": step.step_id,
          "slug": step.slug,
          "fields": step.fields,
          "depends_on": step.depends_on,
          "concepts": step.concepts,
          "status": status,
      }
  ```

  Apply this change: append, after the end of `step_snapshot`, two blank
  lines and then the following new function, so that the file ends with
  this function and a single trailing newline:

  ```python
  def apply_status_updates(
      conn: psycopg.Connection, updates: list[tuple[uuid.UUID, str]]
  ) -> list[tuple[uuid.UUID, dict]]:
      """Apply cascade-driven status updates to the stored step rows (C-035).

      For each `(step_uuid, new_status)` pair in `updates`, in list order:
      load the step via `get_step`; if its current status already equals
      `new_status`, skip it (no SQL statement is executed and no snapshot
      is appended for it); otherwise validate the transition by calling
      `validate_transition(step.status, new_status, is_atomic_step=(step.level == 5), via_cascade=True)`,
      execute the SQL statement `UPDATE step SET status = %s WHERE uuid = %s`
      with parameters `(new_status, step_uuid)`, and append
      `(step_uuid, step_snapshot(step, new_status))` to the result list.

      Args:
          conn: open database connection.
          updates: list of (step_uuid, new_status) pairs to apply, in the
              order they must be applied.

      Returns:
          The list of (step_uuid, snapshot) pairs for every step actually
          updated (i.e. excluding skipped no-op updates), in the order the
          updates were processed.

      Raises:
          StatusTransitionError: propagated from `validate_transition`
              when a transition is illegal.
      """
      snapshots: list[tuple[uuid.UUID, dict]] = []
      for step_uuid, new_status in updates:
          step = get_step(conn, step_uuid)
          if step.status == new_status:
              continue
          validate_transition(step.status, new_status, is_atomic_step=(step.level == 5), via_cascade=True)
          conn.execute("UPDATE step SET status = %s WHERE uuid = %s", (new_status, step_uuid))
          snapshots.append((step_uuid, step_snapshot(step, new_status)))
      return snapshots
  ```

  Constraints:
  - The function signature (parameter names, types, order) and return
    type are reproduced exactly as shown above.
  - The body's control flow, the skip condition (`step.status ==
    new_status`), the exact call to `validate_transition` with
    `is_atomic_step=(step.level == 5)` and `via_cascade=True`, the exact
    SQL text `"UPDATE step SET status = %s WHERE uuid = %s"` executed via
    `conn.execute(...)` with parameters `(new_status, step_uuid)`, and the
    call to `step_snapshot(step, new_status)` are reproduced exactly as
    shown.
  - Nothing else in the file changes: the module docstring, imports, and
    `step_snapshot` are left byte-identical to the current content shown
    above.

  Sibling contracts used (import verbatim from the modules already
  imported into this file; do not reimplement):
  - module `plan_manager/domain/status_model.py`:
    `def validate_transition(current: str, new: str, is_atomic_step: bool, via_cascade: bool) -> None`
    (raises StatusTransitionError(ValueError) on an illegal transition;
    needs_review is legal ONLY with via_cascade=True).
  - module `plan_manager/domain/step_store.py`: `def get_step(conn, step_uuid) -> Step`;
    `Step` is a dataclass with fields `uuid, plan_uuid, parent_step_uuid,
    level, step_id, slug, fields: dict, depends_on: list[str],
    concepts: list[str], status: str`.

  MRS excerpt (concept definitions relevant to this AS):
  - C-005 Step: "Single stored entity for plan levels 3-5 with level,
    step id, slug, level-specific fields, and status." Relevant property:
    "mutation regime depends on status (draft direct, frozen via
    cascade)."
  - C-007 StatusModel: "Lifecycle state machine for plan artifacts with
    enforced legal transitions." Relevant property: "needs_review is set
    only by cascade propagation, never by direct request."
  - C-018 VersionStore: "Git-like append-only version model of node
    versions, revisions, and refs inside the database." Relevant
    property: "revisions carry parent, author, change set; one mutation
    produces one revision."
  - C-035 Database: "In-container PostgreSQL with pgvector serving as the
    single source of truth for all plan data." Relevant property:
    "accessed via psycopg 3 with plain SQL; no ORM; runtime code never
    emits DDL."
verification:
  type: static_analysis
  target: plan_manager.cascade.write.apply_status_updates
  expected: >
    The module still parses as valid Python and step_snapshot is
    unchanged. apply_status_updates iterates updates in order, skips a
    pair whose step already has the target status (no UPDATE, no
    snapshot appended), calls validate_transition with is_atomic_step
    true only when the step's level equals 5 and via_cascade always
    True, executes an UPDATE of the step table's status column keyed by
    uuid for every non-skipped pair, and returns the list of
    (step_uuid, snapshot) pairs for exactly the non-skipped pairs in
    processing order.
status: draft

step_id: A-003
parent_tactical_step: T-001
name: add-assign-labels
target_file: plan_manager/hrs/paragraphs.py
operation: modify_file
priority: 3
depends_on: [A-002]
concepts: [C-002, C-018, C-016, C-021]
prompt: |
  Target file: plan_manager/hrs/paragraphs.py (existing module; modify).
  Operation: modify_file.

  Current full content of the target file, exactly as it exists after all
  lower-priority atomic steps on this file have been applied (this is the
  complete current file; treat it as ground truth, not as an excerpt):

  ```python
  """HRS paragraph access and mutation operations.

  Read and mutate stored HRS paragraphs (C-002) through the stored-paragraph
  primitives in plan_manager.domain.paragraph_store, and record each
  mutation's revision attribution through plan_manager.storage.version_store
  (direct mode) or plan_manager.cascade.write (cascade mode).

  Only binding paragraphs are ever stored: paragraph_store never holds a
  non-binding row. This module defines a function named list_paragraphs that
  intentionally shadows the module-level function of the same name in
  plan_manager.domain.paragraph_store. The store module is imported by module
  name (``from plan_manager.domain import paragraph_store``) and its function
  is always called qualified as ``paragraph_store.list_paragraphs(...)`` so
  this module's own list_paragraphs never recurses into itself.
  """

  import uuid

  from plan_manager.domain import paragraph_store


  def list_paragraphs(conn, plan_uuid: uuid.UUID) -> list[dict]:
      """Return every stored paragraph of a plan in position order.

      Reads all stored binding paragraphs of the plan identified by
      ``plan_uuid`` through
      ``plan_manager.domain.paragraph_store.list_paragraphs(conn, plan_uuid)``
      and projects each row to a dict with keys "label" (str | None),
      "binding" (bool, always True because only binding paragraphs are ever
      stored), "position" (int), and "text" (str). The store function already
      returns rows in position order; this function preserves that order.

      :param conn: an open psycopg 3 database connection.
      :param plan_uuid: uuid.UUID identifying the plan.
      :return: list[dict] -- one dict per stored paragraph, in position order.
      """
      rows = paragraph_store.list_paragraphs(conn, plan_uuid)
      return [
          {
              "label": row.label,
              "binding": True,
              "position": row.position,
              "text": row.text,
          }
          for row in rows
      ]


  def get_paragraph(conn, plan_uuid: uuid.UUID, label: str) -> dict | None:
      """Resolve one stored paragraph by its bare label.

      Calls this module's own list_paragraphs(conn, plan_uuid) and returns
      the single dict whose "label" key equals the given bare ``label``
      string. Returns None when no stored paragraph carries that label.

      :param conn: an open psycopg 3 database connection.
      :param plan_uuid: uuid.UUID identifying the plan.
      :param label: str, the bare four-character label to resolve.
      :return: dict | None -- the matching paragraph dict, or None.
      """
      for paragraph in list_paragraphs(conn, plan_uuid):
          if paragraph["label"] == label:
              return paragraph
      return None
  ```

  Required change, part 1 (imports): insert exactly these five new import
  lines immediately after the existing line
  `from plan_manager.domain import paragraph_store` (which stays exactly
  where it is), before the two blank lines that currently separate the
  imports from the list_paragraphs function. The resulting import block
  (replacing the current 3-line import block shown above with this 8-line
  block, still followed by exactly two blank lines before
  `def list_paragraphs`) must read exactly:

  import uuid

  from plan_manager.domain import paragraph_store
  from plan_manager.domain.labeling import assign_missing_labels
  from plan_manager.storage.version_store import record_revision
  from plan_manager.domain.plan import get_plan
  from plan_manager.cascade.record import CascadeRecord
  from plan_manager.cascade.write import cascade_write

  Do not change the module docstring, the `import uuid` line, the existing
  list_paragraphs function, or the existing get_paragraph function; they
  must remain byte-identical to the current content shown above other than
  the import block insertion described here.

  Required change, part 2 (new function): append the following new function
  to the end of the file, after the existing get_paragraph function,
  separated by exactly two blank lines:

    def assign_labels(
        conn, plan_uuid: uuid.UUID, author: str, cascade: CascadeRecord | None
    ) -> list[str]:

  Signature and typing: conn is an open psycopg 3 database connection (no
  type annotation); plan_uuid is annotated uuid.UUID; author is annotated
  str; cascade is annotated CascadeRecord | None where CascadeRecord is the
  class imported above from plan_manager.cascade.record, whose verified
  shape is:

    @dataclass(frozen=True)
    class CascadeRecord:
        uuid: uuid.UUID
        plan_uuid: uuid.UUID
        name: str
        base_revision_uuid: uuid.UUID
        status: ...
        created_at: ...

  The return type is list[str].

  Imported contracts used by this function body (quoted verbatim, import
  already added above, never reimplement):
  - `def list_paragraphs(conn, plan_uuid) -> list[StoredParagraph]` on
    plan_manager.domain.paragraph_store (called qualified as
    paragraph_store.list_paragraphs(conn, plan_uuid)); StoredParagraph has
    fields uuid: uuid.UUID, plan_uuid: uuid.UUID, label: str | None,
    text: str, position: int; rows are already in position order.
  - `def assign_missing_labels(paragraphs) -> tuple[list, list[str]]` on
    plan_manager.domain.labeling: pure normative algorithm generating fresh
    unique four-character base36 labels for entries whose label is None;
    returns (fully labeled list, list of new labels), the labeled list in
    the same order as the input list, with a label written into every entry
    whose input label was None and every other entry unchanged.
  - `def get_plan(conn, plan_uuid) -> Plan` on plan_manager.domain.plan;
    Plan has field head_revision_uuid among others.
  - `def record_revision(conn, plan_uuid, author, message, changes, parent_revision_uuid, ref_name) -> uuid.UUID`
    on plan_manager.storage.version_store; changes is
    list[tuple[uuid.UUID, dict]]; ref_name=None advances the plan head.
  - `def cascade_write(conn, plan_uuid, cascade, node_uuid, node_snapshot, status_updates, author, message) -> uuid.UUID`
    on plan_manager.cascade.write; node_uuid: uuid.UUID, node_snapshot: dict,
    status_updates: list[tuple[uuid.UUID, str]].

  Behavior (exact, no alternatives; write this exact function body):

      rows = paragraph_store.list_paragraphs(conn, plan_uuid)
      labeled, new_labels = assign_missing_labels(rows)
      if not new_labels:
          return new_labels

      message = "assign paragraph labels"
      changed = [
          (rows[i].uuid, labeled[i])
          for i in range(len(rows))
          if rows[i].label is None
      ]

      with conn.cursor() as cur:
          for row_uuid, new_row in changed:
              cur.execute(
                  "UPDATE paragraph SET label = %s WHERE uuid = %s",
                  (new_row.label, row_uuid),
              )

      if cascade is None:
          changes = [
              (
                  row_uuid,
                  {
                      "kind": "paragraph",
                      "uuid": str(row_uuid),
                      "plan_uuid": str(plan_uuid),
                      "label": new_row.label,
                      "text": new_row.text,
                      "position": new_row.position,
                  },
              )
              for row_uuid, new_row in changed
          ]
          plan = get_plan(conn, plan_uuid)
          record_revision(
              conn,
              plan_uuid,
              author,
              message,
              changes,
              plan.head_revision_uuid,
              ref_name=None,
          )
      else:
          for row_uuid, new_row in changed:
              snapshot = {
                  "kind": "paragraph",
                  "uuid": str(row_uuid),
                  "plan_uuid": str(plan_uuid),
                  "label": new_row.label,
                  "text": new_row.text,
                  "position": new_row.position,
              }
              cascade_write(
                  conn, plan_uuid, cascade, row_uuid, snapshot, [], author, message
              )

      return new_labels

  Explanation of the exact semantics (binding, not optional): `rows` are the
  currently stored paragraphs before this call, in position order. Calling
  `assign_missing_labels(rows)` yields `labeled` (same length and order as
  `rows`, every previously-None label now filled) and `new_labels` (just the
  new label strings). If `new_labels` is empty, nothing was unlabeled, so the
  function returns immediately without writing anything and without
  recording any revision — this is required by the target contract's
  "then no revision is recorded" rule. Otherwise, `changed` pairs each
  original row's uuid with its corresponding post-label entry in `labeled`,
  selecting exactly the indices where the original row's label was None
  (zip by index is valid because assign_missing_labels preserves input order
  and length). Each changed row is persisted with the plain SQL statement
  `UPDATE paragraph SET label = %s WHERE uuid = %s`, executed through a
  cursor obtained via `conn.cursor()` used as a context manager (psycopg 3
  plain SQL; no ORM).

  Revision attribution rule (binding, shared with the sibling mutating
  function in this module): when `cascade` is None, call
  `record_revision(conn, plan_uuid, author, message, changes,
  get_plan(conn, plan_uuid).head_revision_uuid, ref_name=None)` exactly once
  with `changes` built as the list of `(row_uuid, snapshot)` tuples for every
  changed row, where each snapshot is the paragraph node snapshot dict
  `{"kind": "paragraph", "uuid": str(row_uuid), "plan_uuid": str(plan_uuid),
  "label": new_row.label, "text": new_row.text, "position":
  new_row.position}` (no "deleted" key — this is not a deletion). When
  `cascade` is a CascadeRecord, call `cascade_write(conn, plan_uuid, cascade,
  row_uuid, snapshot, [], author, message)` once per changed row, with
  `status_updates=[]` and the same snapshot shape. `message` is the literal
  string `"assign paragraph labels"` in both branches.

  Docstring (place immediately after the def line, exactly):

      """Assign fresh labels to every unlabeled stored binding paragraph.

      Reads every stored paragraph of the plan through
      ``plan_manager.domain.paragraph_store.list_paragraphs(conn, plan_uuid)``,
      then calls ``plan_manager.domain.labeling.assign_missing_labels(rows)``
      which returns ``(labeled, new_labels)``: ``labeled`` is the input list
      with a fresh unique four-character base36 label written into every
      entry whose label was None, in the same order as ``rows``; ``new_labels``
      is the list of newly generated label strings. When ``new_labels`` is
      empty, nothing was unlabeled: this function returns the empty list
      immediately and records no revision.

      Otherwise, for every index i where ``rows[i].label is None``, this
      function issues the plain SQL statement
      ``UPDATE paragraph SET label = %s WHERE uuid = %s`` with parameters
      ``(labeled[i].label, rows[i].uuid)`` through a cursor obtained from
      ``conn.cursor()`` used as a context manager, and builds the paragraph
      node snapshot dict ``{"kind": "paragraph", "uuid": str(rows[i].uuid),
      "plan_uuid": str(plan_uuid), "label": labeled[i].label, "text":
      labeled[i].text, "position": labeled[i].position}`` for that changed
      row.

      Revision attribution rule (binding): when ``cascade`` is None, call
      ``plan_manager.storage.version_store.record_revision(conn, plan_uuid,
      author, message, changes, get_plan(conn, plan_uuid).head_revision_uuid,
      ref_name=None)`` exactly once, where ``changes`` is the list of
      ``(row_uuid, snapshot)`` tuples for every changed row and ``message``
      is the literal string "assign paragraph labels". When ``cascade`` is a
      ``plan_manager.cascade.record.CascadeRecord``, call
      ``plan_manager.cascade.write.cascade_write(conn, plan_uuid, cascade,
      row_uuid, snapshot, [], author, message)`` once per changed row, with
      ``status_updates=[]`` and the same literal ``message``.

      :param conn: an open psycopg 3 database connection.
      :param plan_uuid: uuid.UUID identifying the plan.
      :param author: str, the revision author.
      :param cascade: plan_manager.cascade.record.CascadeRecord | None -- the
          open cascade record, or None for direct head-advancing attribution.
      :return: list[str] -- the freshly generated labels; empty when nothing
          was unlabeled.
      """

  Constraints:
  - Python 3.12+ syntax; PEP 604 union type CascadeRecord | None.
  - Plain SQL via psycopg 3 cursor; no ORM; no other SQL statements besides
    the single UPDATE shown.
  - Only binding paragraphs are ever stored, so every row read here is
    already binding; no filtering by a binding flag is performed (there is
    none to filter).
  - Do not alter the module docstring, imports (beyond the part-1 insertion
    specified above), list_paragraphs, or get_paragraph.

  MRS excerpt (concepts referenced by this AS):

    - concept_id: C-002
      name: Paragraph
      definition: Addressable binding unit of the HRS text carrying a stable four-character base36 label.
      properties:
      - label is position-independent, unique within one HRS, survives edits
      - non-binding blocks are excluded from labeling and coverage
      - HRS prose is human-owned; the server only assigns labels and toggles non-binding markup
      - parsing and label-assignment algorithms are normative

    - concept_id: C-018
      name: VersionStore
      definition: Git-like append-only version model of node versions, revisions, and refs inside the database.
      properties:
      - node versions content-addressed by SHA-256 of canonical JSON (sorted keys, UTF-8)
      - revisions carry parent, author, change set; one mutation produces one revision
      - any intermediate plan state is addressable by revision id
      - refs are plan head plus one per open cascade
      - operations log, diff, checkout-read, revert; history never rewritten

    - concept_id: C-016
      name: CascadeChange
      definition: Transactional top-down change set anchored at the plan head revision, published atomically on green gate.
      properties:
      - begin acquires the per-plan lock; commit advances head only when gate is green
      - automatic needs_review propagation per invalidation rule
      - one open cascade per plan; MRS mutations are cascade-only at any status

    - concept_id: C-021
      name: ExchangeFormat
      definition: File import/export representation of a plan in the standard layout; HRS as Markdown, all other artifacts as YAML.
      properties:
      - export optionally at a named revision; import is the only file-to-truth path
      - safe YAML emission with mandatory round-trip; hrs_export byte-identical

  C-002 grounds the label-assignment algorithm as normative and the label as
  the stable four-character base36 identifier. C-018 grounds the direct-mode
  revision recording via record_revision (append-only version store, one
  mutation produces one revision). C-016 grounds the cascade-mode branch via
  cascade_write (transactional change set anchored at the plan head,
  recorded per node). C-021 is referenced because label assignment is "the
  only textual edit permitted in the HRS" and is invoked by the HRS import
  path described by ExchangeFormat (a separate module, out of scope for this
  AS); this AS implements no import or export logic itself.
verification:
  type: import
  target: plan_manager.hrs.paragraphs.assign_labels
  expected: >
    Importing plan_manager.hrs.paragraphs succeeds and the module exposes a callable named assign_labels accepting exactly four positional parameters (conn, plan_uuid, author, cascade). When no stored paragraph is unlabeled it returns an empty list and issues no SQL and records no revision. When at least one stored paragraph is unlabeled, it issues one UPDATE per changed row, returns the list of newly generated labels, and records exactly one revision via record_revision when cascade is None or one cascade_write call per changed row when cascade is a CascadeRecord.
status: done

step_id: A-004
parent_tactical_step: T-003
name: store-vector
target_file: plan_manager/scoring/embedding.py
operation: modify_file
priority: 4
depends_on: [A-003]
concepts: [C-022, C-035]
prompt: |
  Modify the file `plan_manager/scoring/embedding.py` (operation:
  modify_file).

  The file currently contains exactly the following content (this is the
  full current state of the file):

  ```python
  """Embedding client and cache for EmbeddingService (C-022) integration.

  Provides text embedding retrieval with a database-backed cache keyed by
  content hash, and an explicit typed unavailability condition instead of
  raising on service failure.
  """

  import json
  import urllib.error
  import urllib.request
  import uuid
  from datetime import datetime, timezone

  import psycopg

  from plan_manager.storage.canonical import content_hash


  class EmbeddingUnavailable(Exception):
      """Raised when the embedding service cannot be reached or returns a
      malformed response.

      Carries the reason as its single string argument. Callers catch this
      exception to degrade semantic scoring only; every other function keeps
      working when the embedding service is unavailable.
      """


  def text_content_hash(text: str) -> str:
      """Return the canonical content hash of ``text``.

      Parameters
      ----------
      text:
          The text whose content hash is computed.

      Returns
      -------
      str
          The SHA-256 hex digest of the canonical JSON of ``text``, as
          produced by ``plan_manager.storage.canonical.content_hash``.
      """
      return content_hash(text)


  def get_cached_vector(conn: psycopg.Connection, text: str) -> list[float] | None:
      """Look up a cached embedding vector by the content hash of ``text``.

      Parameters
      ----------
      conn:
          An open psycopg 3 database connection.
      text:
          The text whose cached vector is looked up; the lookup key is its
          content hash via ``text_content_hash``.

      Returns
      -------
      list[float] | None
          The cached vector as a list of floats if a row exists for the
          content hash, or ``None`` if there is no cached vector.
      """
      key = text_content_hash(text)
      with conn.cursor() as cur:
          cur.execute(
              "SELECT vector::text FROM embedding_cache WHERE content_hash = %s",
              (key,),
          )
          row = cur.fetchone()
      if row is None:
          return None
      return json.loads(row[0])
  ```

  Append exactly one new top-level function after `get_cached_vector`,
  separated from it by exactly two blank lines, with exactly this
  signature, docstring, and body:

  ```python
  def store_vector(conn: psycopg.Connection, text: str, vector: list[float]) -> None:
      """Store an embedding vector in the cache keyed by the content hash of ``text``.

      Parameters
      ----------
      conn:
          An open psycopg 3 database connection.
      text:
          The text whose vector is being cached; the cache key is its content
          hash via ``text_content_hash``.
      vector:
          The embedding vector to store, as a list of floats.

      Returns
      -------
      None

      Behavior
      --------
      Inserts one row into ``embedding_cache`` with a freshly generated
      ``uuid.uuid4()`` as ``uuid``, the content hash of ``text`` as
      ``content_hash``, ``vector`` (JSON-encoded via ``json.dumps`` and cast
      to ``::vector``) as ``vector``, and ``datetime.now(timezone.utc)`` as
      ``created_at``. On a content-hash conflict the insert is a no-op
      (``ON CONFLICT (content_hash) DO NOTHING``).
      """
      key = text_content_hash(text)
      with conn.cursor() as cur:
          cur.execute(
              "INSERT INTO embedding_cache (uuid, content_hash, vector, created_at) "
              "VALUES (%s, %s, %s::vector, %s) "
              "ON CONFLICT (content_hash) DO NOTHING",
              (uuid.uuid4(), key, json.dumps(vector), datetime.now(timezone.utc)),
          )
  ```

  Exact requirements:
  - Parameters: `conn` of type `psycopg.Connection`; `text` of type `str`;
    `vector` of type `list[float]`. Return type: `None`.
  - Compute the cache key by calling `text_content_hash(text)`, the
    function already defined earlier in this same file.
  - Open a cursor from `conn` using `with conn.cursor() as cur:` and
    execute exactly the SQL text (as one Python string built from three
    concatenated literals exactly as shown)
    `"INSERT INTO embedding_cache (uuid, content_hash, vector, created_at) "`
    `"VALUES (%s, %s, %s::vector, %s) "`
    `"ON CONFLICT (content_hash) DO NOTHING"`
    with the bound parameter tuple
    `(uuid.uuid4(), key, json.dumps(vector), datetime.now(timezone.utc))`.
  - `uuid.uuid4()` uses the `uuid` module already imported at the top of
    the file via `import uuid`. `json.dumps` uses the `json` module
    already imported via `import json`. `datetime.now(timezone.utc)` uses
    `datetime` and `timezone` already imported via
    `from datetime import datetime, timezone`.
  - Do not return the cursor result, do not call `conn.commit()`, and do
    not raise or catch any exception in this function.
  - Do not modify the module docstring, the imports, the
    `EmbeddingUnavailable` class, `text_content_hash`, or
    `get_cached_vector` in any way. Do not add any other function.
  - The file must end immediately after the closing `)` of the
    `cur.execute(...)` call, with a single trailing newline, and nothing
    else.

  Normative schema (embedding_cache table, already exists in the
  database; this AS never emits DDL): columns `uuid`, `content_hash`,
  `vector`, `created_at` — `content_hash` is `text UNIQUE`; `vector` is a
  pgvector value read and written through SQL text casts; `created_at` is
  `timestamptz`.

  Concept context (MRS excerpt):
  - concept_id: C-022
    name: EmbeddingService
    definition: Sole optional external integration providing text
      embeddings through a configured URL.
    properties:
    - unavailability degrades only semantic scoring, with explicit low
      trust
    - vectors cached in the database keyed by content hash; no vector
      files
  - concept_id: C-035
    name: Database
    definition: In-container PostgreSQL with pgvector serving as the
      single source of truth for all plan data.
    properties (relevant to this AS):
    - hosts the version store, locks, and embedding cache
    - accessed via psycopg 3 with plain SQL; no ORM; runtime code never
      emits DDL

  `store_vector` implements the cache-write side of C-022's property
  "vectors cached in the database keyed by content hash; no vector
  files": the vector is written only into the C-035 `embedding_cache`
  table, never to the filesystem.
verification:
  type: manual
  target: plan_manager.scoring.embedding.store_vector
  expected: Calling store_vector inserts exactly one row into
    embedding_cache for a text whose content hash has no existing row;
    calling it again with the same text is a no-op that does not raise
    and does not create a duplicate row, per ON CONFLICT (content_hash) DO
    NOTHING.
status: draft

step_id: A-005
parent_tactical_step: T-005
name: Create concept_add command scaffold (schema and validate_params)
target_file: plan_manager/commands/concept_add_command.py
operation: create_file
priority: 1
depends_on: []
concepts: [C-023, C-026, C-003, C-016]
prompt: |
  Target file: `plan_manager/commands/concept_add_command.py`. Operation: create_file
  (the file does not exist yet; create it with exactly the content specified below).
  A later step will append the execute method to this same file; do not add an
  execute method here.

  MRS excerpt (verbatim from spec.yaml, only the concepts relevant to this file):

  - concept_id: C-003
    name: Concept
    definition: MRS basis axis with canonical name, one-sentence definition, properties, and source labels.
    properties:
    - identifier pattern C-NNN, unique within plan
    - selected to be as orthogonal as possible; basis quality is measured

  - concept_id: C-016
    name: CascadeChange
    definition: Transactional top-down change set anchored at the plan head revision, published atomically on green gate.
    properties:
    - begin acquires the per-plan lock; commit advances head only when gate is green
    - automatic needs_review propagation per invalidation rule
    - one open cascade per plan; MRS mutations are cascade-only at any status

  - concept_id: C-023
    name: CommandContract
    definition: Adapter-conformant command class shape with schema, metadata, and semantic validation.
    properties:
    - class attributes name, version, descr, category, author, email, result_class, use_queue
    - get_schema strict subset with explicit additionalProperties false
    - shallow adapter validation compensated by validate_params semantics
    - read commands never mutate; mutating commands verify by re-read

  - concept_id: C-026
    name: ErrorModel
    definition: Two-layer error contract of adapter JSON-RPC codes and stable domain string codes.
    properties:
    - every declared error case returnable and every returnable case declared

  Imported foundation contracts used in this AS: `mcp_proxy_adapter.commands.base`:
  `class Command`. `mcp_proxy_adapter.commands.result`: `class SuccessResult`
  (constructed as `SuccessResult(data=<dict>)`) and `class ErrorResult`. Python
  standard library `uuid`: `uuid.UUID(str)` raises ValueError on a malformed UUID
  string.

  Write the file with exactly this content:

  ```python
  """Command: add a new concept to the plan MRS (concept_add)."""

  import uuid

  from mcp_proxy_adapter.commands.base import Command
  from mcp_proxy_adapter.commands.result import SuccessResult, ErrorResult


  class ConceptAddCommand(Command):
      """Add a new MRS concept (C-003) to a resolved plan under an open cascade (C-016).

      MRS entities are cascade-only at any plan status: every call must carry the
      UUID of an already-open cascade in cascade_uuid.
      """

      name = "concept_add"
      version = "1.0.0"
      descr = "Add a new concept to the plan MRS under an open cascade."
      category = "mrs"
      author = "Vasiliy Zdanovskiy"
      email = "vasilyvz@gmail.com"
      result_class = SuccessResult
      use_queue = False

      @classmethod
      def get_schema(cls) -> dict:
          """Return the strict input schema for concept_add."""
          return {
              "type": "object",
              "properties": {
                  "plan": {
                      "type": "string",
                      "description": "Plan identifier (UUID or unique plan name) to resolve.",
                  },
                  "cascade_uuid": {
                      "type": "string",
                      "description": "UUID of the open cascade admitting this mutation. Required: MRS entities are cascade-only at any status.",
                  },
                  "concept_id": {
                      "type": "string",
                      "description": "Concept identifier in pattern C-NNN. Must be unique within the plan.",
                  },
                  "name": {
                      "type": "string",
                      "description": "Concept canonical name.",
                  },
                  "definition": {
                      "type": "string",
                      "description": "Concept one-sentence definition.",
                  },
                  "properties": {
                      "type": "array",
                      "items": {"type": "string"},
                      "default": [],
                      "description": "Free-form property statements of the concept.",
                  },
                  "source_labels": {
                      "type": "array",
                      "items": {"type": "string"},
                      "default": [],
                      "description": "HRS paragraph labels (four-character base36) that justify this concept; each must resolve to a stored binding paragraph.",
                  },
              },
              "required": ["plan", "cascade_uuid", "concept_id", "name", "definition"],
              "additionalProperties": False,
          }

      def validate_params(self, params: dict) -> dict:
          """Validate params: shallow schema checks, then parse cascade_uuid format.

          Raises:
              ValueError: if cascade_uuid is not a well-formed UUID string.
          """
          params = super().validate_params(params)
          uuid.UUID(params["cascade_uuid"])
          return params
  ```

  Constraints:
  - Exactly this file content; no execute method, no additional imports.
  - Do not read, create, or modify any other file.
  - The class must be named ConceptAddCommand and inherit from Command.
  - get_schema must return an object schema with additionalProperties: false,
    required exactly ["plan", "cascade_uuid", "concept_id", "name", "definition"],
    "properties" and "source_labels" as arrays of string with default [].
  - validate_params calls super().validate_params(params) first, then
    `uuid.UUID(params["cascade_uuid"])` to validate format (return value discarded;
    the call is for validation side effect only).
verification:
  type: import
  target: plan_manager/commands/concept_add_command.py
  expected: >
    Importing the module succeeds. ConceptAddCommand.name == "concept_add",
    category == "mrs", result_class is SuccessResult, use_queue is False.
    get_schema() returns an object schema with additionalProperties False and
    required == ["plan", "cascade_uuid", "concept_id", "name", "definition"].
    validate_params raises ValueError when cascade_uuid is not a well-formed
    UUID string and returns the params unchanged otherwise.
status: done

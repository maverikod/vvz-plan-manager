step_id: A-013
parent_tactical_step: T-005
name: Create concept_remove metadata module
target_file: plan_manager/commands/concept_remove_metadata.py
operation: create_file
priority: 1
depends_on: []
concepts: [C-023, C-026, C-003, C-016]
prompt: |
  Target file: `plan_manager/commands/concept_remove_metadata.py`. Operation:
  create_file (the file does not exist yet; create it with exactly the content
  specified below).

  MRS excerpt (verbatim from spec.yaml, only the concepts relevant to this file):

  - concept_id: C-016
    name: CascadeChange
    definition: Transactional top-down change set anchored at the plan head revision, published atomically on green gate.
    properties:
    - one open cascade per plan; MRS mutations are cascade-only at any status

  - concept_id: C-003
    name: Concept
    definition: MRS basis axis with canonical name, one-sentence definition, properties, and source labels.
    properties:
    - identifier pattern C-NNN, unique within plan

  - concept_id: C-023
    name: CommandContract
    definition: Adapter-conformant command class shape with schema, metadata, and semantic validation.
    properties:
    - metadata dictionary must never contradict the schema
    - read commands never mutate; mutating commands verify by re-read

  - concept_id: C-026
    name: ErrorModel
    definition: Two-layer error contract of adapter JSON-RPC codes and stable domain string codes.
    properties:
    - every declared error case returnable and every returnable case declared

  Context: this module documents the ConceptRemoveCommand class defined in
  `plan_manager/commands/concept_remove_command.py` (a sibling file, already
  created; do not read or modify it — the facts you need about it are restated
  here). That class has: name = "concept_remove", version = "1.0.0", descr =
  "Remove an existing concept from the plan MRS under an open cascade.",
  category = "mrs". Its get_schema() requires plan, cascade_uuid, concept_id
  (all strings). Its execute is cascade-only (rejects without a valid open
  cascade with code CASCADE_CONFLICT), removes the concept via the domain store
  (raising CONCEPT_NOT_FOUND when concept_id is unknown), records the removal
  as a cascade revision with a tombstone snapshot (the pre-removal fields plus
  "deleted": true), and verifies the write by re-reading (confirming the
  concept no longer resolves), returning the removed concept_id, deleted: true,
  and a revision_uuid.

  Write the file with exactly this content:

  ```python
  """Extended AI/documentation metadata for the concept_remove command."""

  from typing import Any, Dict


  def get_concept_remove_metadata(cls) -> Dict[str, Any]:
      """Return extended metadata for ConceptRemoveCommand.

      Args:
          cls: The ConceptRemoveCommand class (passed by its classmethod metadata()).

      Returns:
          A metadata dictionary with keys name, version, description, category,
          author, email, detailed_description, parameters, return_value,
          usage_examples, error_cases, best_practices.
      """
      return {
          "name": cls.name,
          "version": cls.version,
          "description": cls.descr,
          "category": cls.category,
          "author": cls.author,
          "email": cls.email,
          "detailed_description": (
              "Removes an existing MRS concept (C-003) from a resolved plan. "
              "MRS entities are cascade-only (C-016) at any plan status: the "
              "call must carry the UUID of an already-open cascade in "
              "cascade_uuid, or it is rejected with CASCADE_CONFLICT. The "
              "removal is destructive: it is recorded as a cascade revision "
              "with a tombstone node snapshot (the pre-removal fields plus "
              "\"deleted\": true) and verified by re-reading the concept after "
              "write to confirm it no longer resolves. This command does not "
              "support dry_run; the reviewable, revertible unit is the open "
              "cascade itself (C-016), which can be aborted before commit."
          ),
          "parameters": {
              "plan": {
                  "description": "Plan identifier (UUID or unique plan name) to resolve.",
                  "type": "string",
                  "required": True,
              },
              "cascade_uuid": {
                  "description": "UUID of the open cascade admitting this mutation.",
                  "type": "string",
                  "required": True,
              },
              "concept_id": {
                  "description": "Concept identifier of the concept to remove.",
                  "type": "string",
                  "required": True,
              },
          },
          "return_value": {
              "success": {
                  "description": "The removed concept identifier, confirmed by re-read, with its cascade revision.",
                  "data": {
                      "concept_id": "Concept identifier that was removed.",
                      "deleted": "Always true on success.",
                      "revision_uuid": "UUID of the cascade revision that recorded this removal.",
                  },
                  "example": {
                      "concept_id": "C-037",
                      "deleted": True,
                      "revision_uuid": "9f8e7d6c-5b4a-3c2d-1e0f-a1b2c3d4e5f6",
                  },
              },
              "error": {
                  "description": "Error result with a stable domain code.",
                  "code": "stable error code string",
                  "message": "human-readable message",
                  "details": "additional diagnostic fields when available",
              },
          },
          "usage_examples": [
              {
                  "description": "Remove an existing concept under an open cascade.",
                  "command": {
                      "plan": "plan-manager",
                      "cascade_uuid": "9f8e7d6c-5b4a-3c2d-1e0f-a1b2c3d4e5f6",
                      "concept_id": "C-037",
                  },
                  "explanation": "Removes concept C-037 under the given open cascade and confirms it no longer resolves.",
              },
          ],
          "error_cases": {
              "PLAN_NOT_FOUND": {
                  "description": "The plan identifier does not resolve to a stored plan.",
                  "message": "plan not found: {plan}",
                  "solution": "List plans and retry with a valid plan identifier or UUID.",
              },
              "CASCADE_CONFLICT": {
                  "description": "cascade_uuid does not identify the plan's currently open cascade.",
                  "message": "cascade admission rejected: {details}",
                  "solution": "Begin a cascade and pass its cascade_uuid, or use the plan's currently open cascade.",
              },
              "CONCEPT_NOT_FOUND": {
                  "description": "concept_id does not exist in the plan.",
                  "message": "concept not found: {concept_id}",
                  "solution": "Call concept_list and retry with a valid concept_id.",
              },
          },
          "best_practices": [
              "Confirm no relation still references concept_id before removal, or expect relation_list to still show stale references until cleaned up.",
              "Begin a cascade before calling concept_remove; MRS entities are cascade-only at any plan status.",
              "concept_remove is destructive; verify concept_id with concept_get before calling it.",
          ],
      }
  ```

  Constraints:
  - Exactly this file content; no additional functions or imports.
  - Do not read, create, or modify any other file.
  - The function must be named get_concept_remove_metadata and accept exactly
    one positional parameter named cls.
  - error_cases must contain exactly the keys PLAN_NOT_FOUND, CASCADE_CONFLICT,
    CONCEPT_NOT_FOUND, each with description, message, solution.
  - The returned dict must contain all of: name, version, description, category,
    author, email, detailed_description, parameters, return_value, usage_examples,
    error_cases, best_practices.
verification:
  type: import
  target: plan_manager/commands/concept_remove_metadata.py
  expected: >
    Importing the module succeeds. get_concept_remove_metadata(cls) called
    with a stub object exposing name/version/descr/category/author/email
    attributes returns a dict containing all required top-level keys;
    error_cases has exactly the keys PLAN_NOT_FOUND, CASCADE_CONFLICT,
    CONCEPT_NOT_FOUND, each with description, message, and solution;
    parameters has entries for plan, cascade_uuid, concept_id, all required
    True.
status: draft

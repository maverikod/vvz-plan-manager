step_id: A-006
parent_tactical_step: T-005
name: Add cascade-wired execute method to concept_add command
target_file: plan_manager/commands/concept_add_command.py
operation: modify_file
priority: 2
depends_on: [A-005]
concepts: [C-023, C-026, C-003, C-016]
prompt: |
  Target file: `plan_manager/commands/concept_add_command.py`. Operation: modify_file.

  Current full content of the target file (post all lower-priority AS on this file):

  ```python
  """Command: add a new concept to the plan MRS (concept_add)."""

  import uuid

  from mcp_proxy_adapter.commands.base import Command
  from mcp_proxy_adapter.commands.result import SuccessResult, ErrorResult


  class ConceptAddCommand(Command):
      """Add a new MRS concept (C-003) to a resolved plan under an open cascade (C-016).

      MRS entities are cascade-only at any plan status: every call must carry the
      UUID of an already-open cascade in cascade_uuid.
      """

      name = "concept_add"
      version = "1.0.0"
      descr = "Add a new concept to the plan MRS under an open cascade."
      category = "mrs"
      author = "Vasiliy Zdanovskiy"
      email = "vasilyvz@gmail.com"
      result_class = SuccessResult
      use_queue = False

      @classmethod
      def get_schema(cls) -> dict:
          """Return the strict input schema for concept_add."""
          return {
              "type": "object",
              "properties": {
                  "plan": {
                      "type": "string",
                      "description": "Plan identifier (UUID or unique plan name) to resolve.",
                  },
                  "cascade_uuid": {
                      "type": "string",
                      "description": "UUID of the open cascade admitting this mutation. Required: MRS entities are cascade-only at any status.",
                  },
                  "concept_id": {
                      "type": "string",
                      "description": "Concept identifier in pattern C-NNN. Must be unique within the plan.",
                  },
                  "name": {
                      "type": "string",
                      "description": "Concept canonical name.",
                  },
                  "definition": {
                      "type": "string",
                      "description": "Concept one-sentence definition.",
                  },
                  "properties": {
                      "type": "array",
                      "items": {"type": "string"},
                      "default": [],
                      "description": "Free-form property statements of the concept.",
                  },
                  "source_labels": {
                      "type": "array",
                      "items": {"type": "string"},
                      "default": [],
                      "description": "HRS paragraph labels (four-character base36) that justify this concept; each must resolve to a stored binding paragraph.",
                  },
              },
              "required": ["plan", "cascade_uuid", "concept_id", "name", "definition"],
              "additionalProperties": False,
          }

      def validate_params(self, params: dict) -> dict:
          """Validate params: shallow schema checks, then parse cascade_uuid format.

          Raises:
              ValueError: if cascade_uuid is not a well-formed UUID string.
          """
          params = super().validate_params(params)
          uuid.UUID(params["cascade_uuid"])
          return params
  ```

  MRS excerpt (verbatim from spec.yaml, only the concepts relevant to this file):

  - concept_id: C-016
    name: CascadeChange
    definition: Transactional top-down change set anchored at the plan head revision, published atomically on green gate.
    properties:
    - begin acquires the per-plan lock; commit advances head only when gate is green
    - automatic needs_review propagation per invalidation rule
    - one open cascade per plan; MRS mutations are cascade-only at any status

  - concept_id: C-003
    name: Concept
    definition: MRS basis axis with canonical name, one-sentence definition, properties, and source labels.
    properties:
    - identifier pattern C-NNN, unique within plan

  - concept_id: C-026
    name: ErrorModel
    definition: Two-layer error contract of adapter JSON-RPC codes and stable domain string codes.
    properties:
    - every declared error case returnable and every returnable case declared

  - concept_id: C-023
    name: CommandContract
    definition: Adapter-conformant command class shape with schema, metadata, and semantic validation.
    properties:
    - read commands never mutate; mutating commands verify by re-read

  Imported foundation/sibling contracts used by the new code (quote verbatim; import
  them, never reimplement their bodies):

  - Module `plan_manager.commands.errors`:
    `def domain_error(code: str, message: str, details: dict | None = None) -> ErrorResult`
    and `def map_exception(exc: Exception) -> ErrorResult` (maps
    psycopg.errors.UniqueViolation to code "DUPLICATE_ID"; other exceptions to a
    stable generic error result).
  - Module `plan_manager.commands.resolve`:
    `def resolve_plan(conn, plan: str) -> Plan` — Plan has fields uuid, name,
    status, context_budget, head_revision_uuid; raises an exception mapped by
    map_exception to code "PLAN_NOT_FOUND" when the plan cannot be resolved.
  - Module `plan_manager.runtime.context`:
    `def db_connection()` — context manager yielding a psycopg.Connection.
  - Module `plan_manager.cascade.record`:
    `class CascadeError(ValueError)`.
  - Module `plan_manager.cascade.regime`:
    `def check_admission(conn, plan_uuid, target_kind: str, target_uuid, cascade_uuid) -> CascadeRecord | None`
    — for target_kind "concept" a request without a valid open cascade_uuid raises
    CascadeError (MRS entities are cascade-only at any status); CascadeRecord has
    fields uuid, plan_uuid, name, base_revision_uuid, status, created_at.
  - Module `plan_manager.cascade.write`:
    `def cascade_write(conn, plan_uuid, cascade: CascadeRecord, node_uuid, node_snapshot: dict, status_updates: list[tuple], author: str, message: str) -> uuid.UUID`
    — writes the cascade revision and returns the new revision uuid.
  - Module `plan_manager.cascade.propagation`:
    `def mrs_invalidation(nodes: dict) -> list[tuple]` — returns
    (step_uuid, "needs_review") for every global step; validated cascade-driven.
  - Module `plan_manager.views.dependency_graph`:
    `def load_steps(conn, plan_uuid) -> dict` — maps step uuid to Step.
  - Module `plan_manager.domain.concept_store`:
    `@dataclass class Concept:` fields, in order, concept_id: str, name: str,
    definition: str, properties: List[str], source_labels: List[str] (the
    dataclass has no uuid and no plan_uuid field);
    `class ConceptValidationError(ValueError)` — raised for all write-time
    validation failures, including duplicate concept_id;
    `def get_concept(conn, plan_uuid, concept_id: str) -> Concept | None`;
    `def insert_concept(conn, plan_uuid, concept: Concept) -> uuid.UUID` —
    validates at write time that concept_id matches ^C-\d{3}$, name and
    definition are non-empty after stripping, properties is a list of
    non-empty strings, source_labels is a list of strings each matching
    ^\{[0-9a-z]{4}\}$, and concept_id is unique within the plan; any
    violation, including the duplicate-concept_id case, raises
    ConceptValidationError (never psycopg.errors.UniqueViolation); on
    success returns the new row's uuid.UUID directly (the store does not
    return the uuid on the Concept object, since Concept carries no uuid
    field).
  - Module `plan_manager.domain.paragraph_store`:
    `def list_paragraphs(conn, plan_uuid) -> list` — returns the stored
    paragraphs of the plan; each returned row exposes a `.label` attribute
    holding its bare label (no surrounding braces), plus text and position.

  Source-label resolution (write-time, performed by this command, not the
  store): before calling insert_concept, build
  `stored = {row.label for row in list_paragraphs(conn, p.uuid)}`; for every
  entry in source_labels (defaulting to [] when omitted), strip a leading
  "{" and trailing "}" via `label.strip("{}")` and require the stripped value
  to be a member of `stored`; on the first entry that is not a member, return
  `domain_error("PARAGRAPH_NOT_FOUND", f"source label does not resolve: {label}")`
  immediately (do not call insert_concept).

  Duplicate detection (binding rule): call insert_concept inside
  `try: ... except ConceptValidationError as exc:`. Let `msg = str(exc)`. If
  `concept_id in msg and "already" in msg` (the store's uniqueness-violation
  message names the concept_id and states it already exists), return
  `domain_error("DUPLICATE_ID", msg)`. Otherwise re-raise the exception with
  a bare `raise` statement (malformed input is caught by validate_params/the
  schema first, so a ConceptValidationError that does not name concept_id as
  already existing is a residual defect, not a declared error case).

  Node snapshot convention (binding): concept snapshot is exactly
  `{"kind": "concept", "uuid": str(row_uuid), "plan_uuid": str, "concept_id",
  "name", "definition", "properties", "source_labels"}`, where row_uuid is
  the uuid.UUID returned directly by insert_concept (used as cascade_write's
  node_uuid too).

  Required edit 1 — insert these import lines immediately after the existing line
  `from mcp_proxy_adapter.commands.result import SuccessResult, ErrorResult` and
  before the blank line that precedes `class ConceptAddCommand(Command):`:

  ```python
  from plan_manager.commands.errors import domain_error, map_exception
  from plan_manager.commands.resolve import resolve_plan
  from plan_manager.runtime.context import db_connection
  from plan_manager.cascade.record import CascadeError
  from plan_manager.cascade.regime import check_admission
  from plan_manager.cascade.write import cascade_write
  from plan_manager.cascade.propagation import mrs_invalidation
  from plan_manager.views.dependency_graph import load_steps
  from plan_manager.domain.concept_store import (
      Concept,
      ConceptValidationError,
      get_concept,
      insert_concept,
  )
  from plan_manager.domain.paragraph_store import list_paragraphs
  ```

  Required edit 2 — append this method at the end of the class body, after
  validate_params, keeping the same 4-space class-body indentation:

  ```python
      async def execute(
          self,
          plan: str,
          cascade_uuid: str,
          concept_id: str,
          name: str,
          definition: str,
          properties: list[str] | None = None,
          source_labels: list[str] | None = None,
      ) -> SuccessResult | ErrorResult:
          """Add a new concept under an open cascade and verify by re-read.

          Args:
              plan: Plan identifier (UUID or unique plan name) to resolve.
              cascade_uuid: UUID string of the open cascade admitting this mutation.
              concept_id: New concept identifier in pattern C-NNN.
              name: Concept canonical name.
              definition: Concept one-sentence definition.
              properties: Free-form property statements; defaults to [] when omitted.
              source_labels: HRS paragraph labels justifying the concept; defaults
                  to [] when omitted.

          Returns:
              SuccessResult with the written concept fields plus revision_uuid, or
              ErrorResult with code PLAN_NOT_FOUND, CASCADE_CONFLICT,
              DUPLICATE_ID, or PARAGRAPH_NOT_FOUND.
          """
          try:
              with db_connection() as conn:
                  p = resolve_plan(conn, plan)
                  parsed_cascade = uuid.UUID(cascade_uuid)
                  try:
                      rec = check_admission(conn, p.uuid, "concept", None, parsed_cascade)
                  except CascadeError as exc:
                      return domain_error("CASCADE_CONFLICT", str(exc))
                  resolved_labels = source_labels or []
                  stored = {row.label for row in list_paragraphs(conn, p.uuid)}
                  for label in resolved_labels:
                      bare = label.strip("{}")
                      if bare not in stored:
                          return domain_error(
                              "PARAGRAPH_NOT_FOUND",
                              f"source label does not resolve: {label}",
                          )
                  concept = Concept(
                      concept_id=concept_id,
                      name=name,
                      definition=definition,
                      properties=properties or [],
                      source_labels=resolved_labels,
                  )
                  try:
                      row_uuid = insert_concept(conn, p.uuid, concept)
                  except ConceptValidationError as exc:
                      msg = str(exc)
                      if concept_id in msg and "already" in msg:
                          return domain_error("DUPLICATE_ID", msg)
                      raise
                  nodes = load_steps(conn, p.uuid)
                  status_updates = mrs_invalidation(nodes)
                  snapshot = {
                      "kind": "concept",
                      "uuid": str(row_uuid),
                      "plan_uuid": str(p.uuid),
                      "concept_id": concept.concept_id,
                      "name": concept.name,
                      "definition": concept.definition,
                      "properties": concept.properties,
                      "source_labels": concept.source_labels,
                  }
                  revision_uuid = cascade_write(
                      conn, p.uuid, rec, row_uuid, snapshot, status_updates,
                      "api", f"concept_add: {concept.concept_id}",
                  )
                  verified = get_concept(conn, p.uuid, concept.concept_id)
                  if verified is None:
                      raise RuntimeError("concept_add: write verification failed")
                  return SuccessResult(data={
                      "uuid": str(row_uuid),
                      "plan_uuid": str(p.uuid),
                      "concept_id": verified.concept_id,
                      "name": verified.name,
                      "definition": verified.definition,
                      "properties": verified.properties,
                      "source_labels": verified.source_labels,
                      "revision_uuid": str(revision_uuid),
                  })
          except Exception as exc:
              return map_exception(exc)
  ```

  Constraints:
  - Apply exactly the two edits above; do not change any other line of the
    current content.
  - Do not read, create, or modify any other file.
  - The resulting file must define exactly one class, ConceptAddCommand, with
    methods get_schema, validate_params, execute (in that order) and no other
    methods.
  - execute's final exception handler must be exactly
    `except Exception as exc: return map_exception(exc)`.
  - CASCADE_REQUIRED is not reachable from this command (cascade_uuid is a
    required schema parameter) and must not appear anywhere in this file.
  - The source-label resolution loop runs before insert_concept is called and
    returns PARAGRAPH_NOT_FOUND directly (no exception) for the first
    unresolved label.
  - The duplicate-detection condition after insert_concept is exactly
    `concept_id in msg and "already" in msg`, where msg = str(exc) inside
    `except ConceptValidationError as exc:`; any other ConceptValidationError
    is re-raised with a bare `raise`.
verification:
  type: import
  target: plan_manager/commands/concept_add_command.py
  expected: >
    Importing the module succeeds and the resulting file is at most 400 lines.
    ConceptAddCommand.execute is an async method accepting plan, cascade_uuid,
    concept_id, name, definition, properties, source_labels. When
    check_admission raises CascadeError, execute returns an ErrorResult with
    code CASCADE_CONFLICT. When a supplied source_labels entry (braces
    stripped) is not among the plan's stored paragraph labels, execute
    returns an ErrorResult with code PARAGRAPH_NOT_FOUND without calling
    insert_concept. When insert_concept raises ConceptValidationError whose
    message contains both concept_id and "already", execute returns an
    ErrorResult with code DUPLICATE_ID; any other ConceptValidationError
    propagates to the outer handler via map_exception. On success, execute
    calls insert_concept then cascade_write, then re-reads the concept via
    get_concept, and returns a SuccessResult whose data includes uuid (the
    uuid returned by insert_concept), plan_uuid, concept_id, name,
    definition, properties, source_labels, and revision_uuid equal to
    str(cascade_write's return value).
status: draft

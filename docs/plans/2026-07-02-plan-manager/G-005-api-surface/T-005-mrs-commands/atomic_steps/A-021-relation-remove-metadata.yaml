step_id: A-021
parent_tactical_step: T-005
name: Create relation_remove metadata module
target_file: plan_manager/commands/relation_remove_metadata.py
operation: create_file
priority: 1
depends_on: []
concepts: [C-023, C-026, C-004, C-016]
prompt: |
  Target file: `plan_manager/commands/relation_remove_metadata.py`. Operation:
  create_file (the file does not exist yet; create it with exactly the content
  specified below).

  MRS excerpt (verbatim from spec.yaml, only the concepts relevant to this file):

  - concept_id: C-016
    name: CascadeChange
    definition: Transactional top-down change set anchored at the plan head revision, published atomically on green gate.
    properties:
    - one open cascade per plan; MRS mutations are cascade-only at any status

  - concept_id: C-004
    name: Relation
    definition: Typed directed edge between two concepts restricted to seven allowed types.
    properties:
    - types are uses, owns, implements, extends, depends_on, produces, consumes
    - free-form types rejected at write time

  - concept_id: C-023
    name: CommandContract
    definition: Adapter-conformant command class shape with schema, metadata, and semantic validation.
    properties:
    - metadata dictionary must never contradict the schema
    - read commands never mutate; mutating commands verify by re-read

  - concept_id: C-026
    name: ErrorModel
    definition: Two-layer error contract of adapter JSON-RPC codes and stable domain string codes.
    properties:
    - every declared error case returnable and every returnable case declared

  Context: this module documents the RelationRemoveCommand class defined in
  `plan_manager/commands/relation_remove_command.py` (a sibling file, already
  created; do not read or modify it — the facts you need about it are restated
  here). That class has: name = "relation_remove", version = "1.0.0", descr =
  "Remove an existing relation between two concepts under an open cascade.",
  category = "mrs". Its get_schema() requires plan, cascade_uuid, from_concept,
  to_concept (strings) and type (string enum of the seven allowed relation
  types: uses, owns, implements, extends, depends_on, produces, consumes). Its
  execute is cascade-only (rejects without a valid open cascade with code
  CASCADE_CONFLICT), removes the relation via the domain store (raising
  RELATION_NOT_FOUND when no such (from_concept, to_concept, type) edge
  exists), records the removal as a cascade revision with a tombstone
  snapshot (the pre-removal fields plus "deleted": true), and verifies the
  write by re-reading the relation list (confirming the edge no longer
  appears), returning the removed edge, deleted: true, and a revision_uuid.

  Write the file with exactly this content:

  ```python
  """Extended AI/documentation metadata for the relation_remove command."""

  from typing import Any, Dict


  def get_relation_remove_metadata(cls) -> Dict[str, Any]:
      """Return extended metadata for RelationRemoveCommand.

      Args:
          cls: The RelationRemoveCommand class (passed by its classmethod metadata()).

      Returns:
          A metadata dictionary with keys name, version, description, category,
          author, email, detailed_description, parameters, return_value,
          usage_examples, error_cases, best_practices.
      """
      return {
          "name": cls.name,
          "version": cls.version,
          "description": cls.descr,
          "category": cls.category,
          "author": cls.author,
          "email": cls.email,
          "detailed_description": (
              "Removes an existing MRS relation (C-004) between two concepts. "
              "MRS entities are cascade-only (C-016) at any plan status: the "
              "call must carry the UUID of an already-open cascade in "
              "cascade_uuid, or it is rejected with CASCADE_CONFLICT. The "
              "removal is destructive: it is recorded as a cascade revision "
              "with a tombstone node snapshot (the pre-removal fields plus "
              "\"deleted\": true) and verified by re-reading the relation "
              "list after write to confirm the edge no longer appears. This "
              "command does not support dry_run; the reviewable, revertible "
              "unit is the open cascade itself (C-016), which can be aborted "
              "before commit."
          ),
          "parameters": {
              "plan": {
                  "description": "Plan identifier (UUID or unique plan name) to resolve.",
                  "type": "string",
                  "required": True,
              },
              "cascade_uuid": {
                  "description": "UUID of the open cascade admitting this mutation.",
                  "type": "string",
                  "required": True,
              },
              "from_concept": {
                  "description": "Concept identifier of the relation source endpoint.",
                  "type": "string",
                  "required": True,
              },
              "to_concept": {
                  "description": "Concept identifier of the relation target endpoint.",
                  "type": "string",
                  "required": True,
              },
              "type": {
                  "description": "Relation type; one of the seven allowed types.",
                  "type": "string",
                  "required": True,
                  "enum": ["uses", "owns", "implements", "extends", "depends_on", "produces", "consumes"],
              },
          },
          "return_value": {
              "success": {
                  "description": "The removed relation confirmed by re-read, with its cascade revision.",
                  "data": {
                      "from_concept": "Relation source endpoint concept_id that was removed.",
                      "to_concept": "Relation target endpoint concept_id that was removed.",
                      "type": "Relation type that was removed.",
                      "deleted": "Always true on success.",
                      "revision_uuid": "UUID of the cascade revision that recorded this removal.",
                  },
                  "example": {
                      "from_concept": "C-004",
                      "to_concept": "C-003",
                      "type": "depends_on",
                      "deleted": True,
                      "revision_uuid": "9f8e7d6c-5b4a-3c2d-1e0f-a1b2c3d4e5f6",
                  },
              },
              "error": {
                  "description": "Error result with a stable domain code.",
                  "code": "stable error code string",
                  "message": "human-readable message",
                  "details": "additional diagnostic fields when available",
              },
          },
          "usage_examples": [
              {
                  "description": "Remove an existing relation under an open cascade.",
                  "command": {
                      "plan": "plan-manager",
                      "cascade_uuid": "9f8e7d6c-5b4a-3c2d-1e0f-a1b2c3d4e5f6",
                      "from_concept": "C-004",
                      "to_concept": "C-003",
                      "type": "depends_on",
                  },
                  "explanation": "Removes the depends_on edge from C-004 to C-003 under the given open cascade and confirms it no longer resolves.",
              },
          ],
          "error_cases": {
              "PLAN_NOT_FOUND": {
                  "description": "The plan identifier does not resolve to a stored plan.",
                  "message": "plan not found: {plan}",
                  "solution": "List plans and retry with a valid plan identifier or UUID.",
              },
              "CASCADE_CONFLICT": {
                  "description": "cascade_uuid does not identify the plan's currently open cascade.",
                  "message": "cascade admission rejected: {details}",
                  "solution": "Begin a cascade and pass its cascade_uuid, or use the plan's currently open cascade.",
              },
              "RELATION_NOT_FOUND": {
                  "description": "No relation with the given from_concept, to_concept, and type exists.",
                  "message": "relation not found: {from_concept}-{type}->{to_concept}",
                  "solution": "Call relation_list and retry with a from_concept, to_concept, and type that match an existing edge.",
              },
          },
          "best_practices": [
              "Call relation_list before relation_remove to confirm the exact edge (from_concept, to_concept, type) exists.",
              "Begin a cascade before calling relation_remove; MRS entities are cascade-only at any plan status.",
              "relation_remove is destructive; re-check with relation_list after the cascade commits.",
          ],
      }
  ```

  Constraints:
  - Exactly this file content; no additional functions or imports.
  - Do not read, create, or modify any other file.
  - The function must be named get_relation_remove_metadata and accept exactly
    one positional parameter named cls.
  - error_cases must contain exactly the keys PLAN_NOT_FOUND, CASCADE_CONFLICT,
    RELATION_NOT_FOUND, each with description, message, solution.
  - The returned dict must contain all of: name, version, description, category,
    author, email, detailed_description, parameters, return_value, usage_examples,
    error_cases, best_practices.
verification:
  type: import
  target: plan_manager/commands/relation_remove_metadata.py
  expected: >
    Importing the module succeeds. get_relation_remove_metadata(cls) called
    with a stub object exposing name/version/descr/category/author/email
    attributes returns a dict containing all required top-level keys;
    error_cases has exactly the keys PLAN_NOT_FOUND, CASCADE_CONFLICT,
    RELATION_NOT_FOUND, each with description, message, and solution;
    parameters has entries for plan, cascade_uuid, from_concept, to_concept,
    type, all required True, with type carrying the seven-entry enum.
status: draft

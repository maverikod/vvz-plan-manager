step_id: A-006
parent_tactical_step: T-008
name: BranchPromptCommand execute method
target_file: plan_manager/commands/branch_prompt_command.py
operation: modify_file
priority: 5
depends_on: [A-005]
concepts: [C-023, C-026, C-008, C-011]
prompt: |
  Modify the existing file plan_manager/commands/branch_prompt_command.py.

  MRS excerpt (concept definitions relevant to this AS):

  - concept_id: C-008
    name: Branch
    definition: Addressable derived view of one path HRS-slice -> GS -> TS -> AS; the unit of verification and prompt assembly.
    properties:
    - never materialized as a stored artifact
    - a mechanically clean branch is measurable while siblings are broken
    source_labels: ["{c9a6}", "{f1w9}", "{r9f1}"]

  - concept_id: C-011
    name: Prompt
    definition: Ephemeral assembled executor input for one atomic step built deterministically from the tree.
    properties:
    - fixed concatenation order MRS excerpt, HRS slice, GS, TS, AS delta
    - byte-identical for the same tree state; token estimate against the plan's user-set context budget
    - dump snapshots are derived artifacts never read back
    - the server never executes prompts
    source_labels: ["{t3u5}", "{y6q1}", "{s5m8}", "{r9f1}", "{j1w5}"]

  - concept_id: C-023
    name: CommandContract
    definition: Adapter-conformant command class shape with schema, metadata, and semantic validation.
    properties:
    - class attributes name, version, descr, category, author, email, result_class, use_queue
    - get_schema strict subset with explicit additionalProperties false
    - shallow adapter validation compensated by validate_params semantics
    - metadata dictionary must never contradict the schema
    - read commands never mutate; mutating commands verify by re-read
    - destructive or bulk operations expose a dry-run mode defaulting to on
    source_labels: ["{t4k1}", "{y2m6}", "{h2f8}", "{f4d6}", "{z5t4}", "{r1c7}"]

  - concept_id: C-026
    name: ErrorModel
    definition: Two-layer error contract of adapter JSON-RPC codes and stable domain string codes.
    properties:
    - every declared error case returnable and every returnable case declared
    source_labels: ["{g9b1}"]

  Verbatim foundation/sibling contracts to import and use, never
  reimplement:

  - plan_manager/commands/errors.py:
    `class DomainCommandError(Exception)` with fields code, message,
    details=None.
    `def domain_error(code, message, details=None) -> ErrorResult`.
    `def map_exception(exc) -> ErrorResult` — maps ScoreRefusedError to
    code "GATE_RED", EmbeddingUnavailable to code "EMBEDDINGS_UNAVAILABLE",
    and other exceptions to their declared domain codes.
  - plan_manager/commands/resolve.py:
    `def resolve_plan(conn, plan: str) -> Plan` — Plan has fields uuid,
    name, status, context_budget, head_revision_uuid. context_budget is
    the plan's user-set token budget (default 4000), plan data, not
    configuration. Raises a DomainCommandError with code PLAN_NOT_FOUND
    when plan does not resolve.
  - plan_manager/runtime/context.py:
    `def db_connection()` — returns a context manager; used as
    `with db_connection() as conn:` to obtain a psycopg 3 connection.
  - plan_manager/views/branch.py:
    `@dataclass Branch` with fields plan_uuid, gs: Step, ts: Step,
    atomic: Step, hrs_slice: list[Paragraph].
    `def resolve_branch(conn, plan_uuid, gs_step_id: str, ts_step_id: str, as_step_id: str) -> Branch`
    — raises ValueError naming the missing step id when a step does not
    resolve.
  - plan_manager/views/prompt_assembly.py:
    `def assemble_prompt(conn, branch: Branch) -> str` — the
    deterministic executor prompt in the fixed order (MRS excerpt, HRS
    slice, GS, TS, AS delta under fixed headers), byte-identical for the
    same tree state.
    `def token_estimate(text: str) -> int` — the published default
    `(len(text) + 3) // 4`.

  CURRENT FILE CONTENT (exact, after all lower-priority AS on this file
  have been applied):

  """BranchPromptCommand: assemble the deterministic executor prompt for one branch."""
  from typing import Any, ClassVar, Dict

  from mcp_proxy_adapter.commands.base import Command
  from mcp_proxy_adapter.commands.result import SuccessResult

  from plan_manager.commands.branch_prompt_metadata import get_branch_prompt_metadata


  class BranchPromptCommand(Command):
      """Assemble and return the executor prompt for one branch, read-only."""

      name: ClassVar[str] = "branch_prompt"
      version: ClassVar[str] = "1.0.0"
      descr: ClassVar[str] = (
          "Assemble the deterministic executor prompt for one branch and "
          "report its token estimate against the plan's context budget."
      )
      category: ClassVar[str] = "branch"
      author: ClassVar[str] = "Vasiliy Zdanovskiy"
      email: ClassVar[str] = "vasilyvz@gmail.com"
      result_class = SuccessResult
      use_queue: ClassVar[bool] = False

      @classmethod
      def get_schema(cls) -> Dict[str, Any]:
          """Return the machine-readable input schema for this command.

          :return: A dict with object type, properties for plan,
              gs_step_id, ts_step_id, as_step_id (all type string with
              descriptions), a required list naming all four, and
              additionalProperties False.
          """
          return {
              "type": "object",
              "properties": {
                  "plan": {
                      "type": "string",
                      "description": "Plan identifier (name or UUID).",
                  },
                  "gs_step_id": {
                      "type": "string",
                      "description": "Global step id of the branch, e.g. 'G-005'.",
                  },
                  "ts_step_id": {
                      "type": "string",
                      "description": "Tactical step id of the branch, e.g. 'T-008'.",
                  },
                  "as_step_id": {
                      "type": "string",
                      "description": "Atomic step id of the branch, e.g. 'A-001'.",
                  },
              },
              "required": ["plan", "gs_step_id", "ts_step_id", "as_step_id"],
              "additionalProperties": False,
          }

      @classmethod
      def metadata(cls) -> Dict[str, Any]:
          """Return extended AI/documentation metadata for this command.

          :return: The dictionary produced by get_branch_prompt_metadata(cls).
          """
          return get_branch_prompt_metadata(cls)

      def validate_params(self, params: Dict[str, Any]) -> Dict[str, Any]:
          """Validate and normalize the raw parameters for this command.

          :param params: Raw parameter dict as received from the
              adapter, already checked against get_schema(): object
              type, the four required keys plan, gs_step_id, ts_step_id,
              as_step_id present, all four values of type string, no
              additional properties.
          :return: The params dict, unchanged beyond the base validation
              already performed by the superclass. No further semantic
              validation applies to this command: existence of the plan
              and of each step id is resolved and reported as a domain
              error during execute(), not during schema validation.
          """
          params = super().validate_params(params)
          return params

  REQUIRED CHANGE:

  1. Replace the import block with exactly this new import block
     (adds ErrorResult to the result import, and adds four new
     import lines for the foundation/sibling contracts; all other
     lines are unchanged):

     from typing import Any, ClassVar, Dict

     from mcp_proxy_adapter.commands.base import Command
     from mcp_proxy_adapter.commands.result import ErrorResult, SuccessResult

     from plan_manager.commands.branch_prompt_metadata import get_branch_prompt_metadata
     from plan_manager.commands.errors import domain_error, map_exception
     from plan_manager.commands.resolve import resolve_plan
     from plan_manager.runtime.context import db_connection
     from plan_manager.views.branch import resolve_branch
     from plan_manager.views.prompt_assembly import assemble_prompt, token_estimate

  2. Append this method to the end of the BranchPromptCommand class
     body, immediately after validate_params, separated by exactly one
     blank line:

      def execute(
          self,
          plan: str,
          gs_step_id: str,
          ts_step_id: str,
          as_step_id: str,
      ) -> SuccessResult | ErrorResult:
          """Resolve one branch, assemble its prompt, and report the budget verdict.

          :param plan: Plan identifier (name or UUID) resolved via
              resolve_plan(conn, plan) -> Plan (fields uuid, name,
              status, context_budget, head_revision_uuid).
          :param gs_step_id: Global step id of the branch (e.g. 'G-005').
          :param ts_step_id: Tactical step id of the branch (e.g. 'T-008').
          :param as_step_id: Atomic step id of the branch (e.g. 'A-001').
          :return: SuccessResult(data={"prompt": str, "token_estimate": int,
              "context_budget": int, "within_budget": bool}) on success;
              ErrorResult with code STEP_NOT_FOUND when resolve_branch
              raises ValueError naming the missing step id; ErrorResult
              from map_exception(exc) for any other exception, including
              a DomainCommandError with code PLAN_NOT_FOUND raised by
              resolve_plan when the plan does not resolve.
          """
          try:
              with db_connection() as conn:
                  p = resolve_plan(conn, plan)
                  try:
                      branch = resolve_branch(
                          conn, p.uuid, gs_step_id, ts_step_id, as_step_id
                      )
                  except ValueError as exc:
                      return domain_error("STEP_NOT_FOUND", str(exc))
                  text = assemble_prompt(conn, branch)
                  est = token_estimate(text)
                  return SuccessResult(
                      data={
                          "prompt": text,
                          "token_estimate": est,
                          "context_budget": p.context_budget,
                          "within_budget": est <= p.context_budget,
                      }
                  )
          except Exception as exc:
              return map_exception(exc)

  Constraints:
  - Do not modify any other part of the existing file content shown
    above beyond the single import-block replacement and the single
    method append described.
  - The method signature is exactly the multi-line signature shown,
    returning `SuccessResult | ErrorResult`.
  - The inner try/except around resolve_branch catches only ValueError
    and returns `domain_error("STEP_NOT_FOUND", str(exc))`.
  - The outer try/except wraps the entire `with db_connection() as conn:`
    block, catches `Exception as exc`, and returns
    `map_exception(exc)` as the final handler.
  - The success data dict has exactly the four keys prompt,
    token_estimate, context_budget, within_budget, computed exactly as
    shown (within_budget is `est <= p.context_budget`).
  - No other exception types are caught; no other error codes are
    returned directly from this method.
verification:
  type: import
  target: plan_manager.commands.branch_prompt_command
  expected: >
    The module imports without error; BranchPromptCommand.execute(self, plan,
    gs_step_id, ts_step_id, as_step_id) resolves the plan and branch, returns
    SuccessResult(data={"prompt", "token_estimate", "context_budget",
    "within_budget"}) on success, returns an ErrorResult with code
    STEP_NOT_FOUND when resolve_branch raises ValueError, and returns
    map_exception(exc) for any other exception including plan resolution
    failure (PLAN_NOT_FOUND).
status: done

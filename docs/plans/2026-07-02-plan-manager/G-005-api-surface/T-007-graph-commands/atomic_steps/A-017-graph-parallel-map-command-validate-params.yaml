step_id: A-017
parent_tactical_step: T-007
name: Add validate_params to GraphParallelMapCommand
target_file: plan_manager/commands/graph_parallel_map_command.py
operation: modify_file
priority: 4
depends_on: [A-016]
concepts: [C-023]
prompt: |
  Modify the existing file at the exact project-relative path
  `plan_manager/commands/graph_parallel_map_command.py`.

  Current full content of the file (state after all lower-priority atomic
  steps on this file have been applied):

  ```python
  """Graph command: parallel wave partition of a plan's steps (C-009)."""
  from __future__ import annotations

  from mcp_proxy_adapter.commands.base import Command
  from mcp_proxy_adapter.commands.result import SuccessResult

  from plan_manager.commands.graph_parallel_map_metadata import (
      get_graph_parallel_map_metadata,
  )


  class GraphParallelMapCommand(Command):
      """Return the parallel wave partition of a plan's steps by prerequisite depth."""

      name = "graph_parallel_map"
      version = "1.0.0"
      descr = "Return the parallel wave partition of a plan's steps by prerequisite depth."
      category = "graph"
      author = "Vasiliy Zdanovskiy"
      email = "vasilyvz@gmail.com"
      result_class = SuccessResult
      use_queue = False

      @classmethod
      def get_schema(cls) -> dict:
          return {
              "type": "object",
              "properties": {
                  "plan": {
                      "type": "string",
                      "description": "Plan identifier (name or uuid) resolved against the catalog.",
                  },
              },
              "required": ["plan"],
              "additionalProperties": False,
          }

      @classmethod
      def metadata(cls) -> dict:
          return get_graph_parallel_map_metadata(cls)
  ```

  Replace this content with exactly the following full file content,
  byte-identical:

  ```python
  """Graph command: parallel wave partition of a plan's steps (C-009)."""
  from __future__ import annotations

  from mcp_proxy_adapter.commands.base import Command
  from mcp_proxy_adapter.commands.result import SuccessResult

  from plan_manager.commands.graph_parallel_map_metadata import (
      get_graph_parallel_map_metadata,
  )


  class GraphParallelMapCommand(Command):
      """Return the parallel wave partition of a plan's steps by prerequisite depth."""

      name = "graph_parallel_map"
      version = "1.0.0"
      descr = "Return the parallel wave partition of a plan's steps by prerequisite depth."
      category = "graph"
      author = "Vasiliy Zdanovskiy"
      email = "vasilyvz@gmail.com"
      result_class = SuccessResult
      use_queue = False

      @classmethod
      def get_schema(cls) -> dict:
          return {
              "type": "object",
              "properties": {
                  "plan": {
                      "type": "string",
                      "description": "Plan identifier (name or uuid) resolved against the catalog.",
                  },
              },
              "required": ["plan"],
              "additionalProperties": False,
          }

      @classmethod
      def metadata(cls) -> dict:
          return get_graph_parallel_map_metadata(cls)

      def validate_params(self, params: dict) -> dict:
          params = super().validate_params(params)
          return params
  ```

  Constraints:
  - Append the `validate_params` method shown above, with signature
    `def validate_params(self, params: dict) -> dict:`, after the existing
    `metadata` method, separated by exactly one blank line.
  - The body is exactly two statements: `params = super().validate_params(
    params)` followed by `return params`. `graph_parallel_map` has no
    parameter semantics beyond what the JSON Schema from `get_schema()`
    already enforces (one required string, no mutually exclusive fields, no
    numeric ranges), so no additional semantic checks are added; this
    method exists solely to satisfy the CommandContract method shape and to
    invoke the base validator.
  - No other line changes.
  - Do not add `execute` in this edit.

  MRS excerpt (concept definitions relevant to this AS):

  C-023 CommandContract: "Adapter-conformant command class shape with
  schema, metadata, and semantic validation." Relevant properties: "shallow
  adapter validation compensated by validate_params semantics" and "read
  commands never mutate; mutating commands verify by re-read" —
  graph_parallel_map is a read command with no additional semantic
  constraints beyond the schema, so validate_params delegates entirely to
  the base implementation and performs no mutation.
verification:
  type: static_analysis
  target: plan_manager/commands/graph_parallel_map_command.py
  expected: >
    The file parses as valid Python 3.12 source with no syntax errors and
    GraphParallelMapCommand defines validate_params(self, params) that
    calls super().validate_params(params) and returns its result
    unchanged.
status: draft

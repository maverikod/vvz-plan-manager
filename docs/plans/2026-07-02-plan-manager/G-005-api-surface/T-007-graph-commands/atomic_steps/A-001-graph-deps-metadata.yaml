step_id: A-001
parent_tactical_step: T-007
name: Create graph_deps_metadata.py
target_file: plan_manager/commands/graph_deps_metadata.py
operation: create_file
priority: 1
depends_on: []
concepts: [C-023, C-026]
prompt: |
  Create a new Python file at the exact project-relative path
  `plan_manager/commands/graph_deps_metadata.py` with exactly the following
  content, byte-identical:

  ```python
  """Metadata for the graph_deps command (C-009, C-023)."""
  from __future__ import annotations


  def get_graph_deps_metadata(cls) -> dict:
      return {
          "name": cls.name,
          "version": cls.version,
          "description": cls.descr,
          "category": cls.category,
          "author": cls.author,
          "email": cls.email,
          "detailed_description": (
              "Resolves the plan against the catalog, loads the dependency "
              "projection (C-009) for the plan once, locates the step "
              "identified by step_id, and returns its dependency neighborhood: "
              "the steps it depends on and the steps that depend on it. Each "
              "step in the result is classified by its artifact path. The "
              "command is read-only and mutates nothing."
          ),
          "parameters": {
              "plan": {
                  "description": "Plan identifier (name or uuid) resolved against the catalog.",
                  "type": "string",
                  "required": True,
              },
              "step_id": {
                  "description": "step_id of the target step within the plan.",
                  "type": "string",
                  "required": True,
              },
          },
          "return_value": {
              "success": {
                  "description": "The dependency neighborhood of the target step.",
                  "data": {
                      "step": "Artifact path of the target step.",
                      "depends_on": "Artifact paths of steps the target step depends on.",
                      "dependents": "Artifact paths of steps that depend on the target step.",
                  },
                  "example": {
                      "step": "G-001-domain-model/T-002-step-lifecycle/atomic_steps/A-003-status.yaml",
                      "depends_on": ["G-001-domain-model/T-001-plan-aggregate/atomic_steps/A-001-plan.yaml"],
                      "dependents": [],
                  },
              },
              "error": {
                  "description": "Domain error returned when the plan or the step cannot be resolved.",
                  "code": "PLAN_NOT_FOUND | STEP_NOT_FOUND",
                  "message": "Human-readable message identifying the missing plan or step.",
                  "details": "None for these error codes.",
              },
          },
          "usage_examples": [
              {
                  "description": "Get the dependency neighborhood of one step.",
                  "command": {"plan": "plan_manager", "step_id": "T-007"},
                  "explanation": "Returns the steps T-007 depends on and the steps depending on T-007.",
              }
          ],
          "error_cases": {
              "PLAN_NOT_FOUND": {
                  "description": "The plan identifier does not match any plan in the catalog.",
                  "message": "plan not found: {plan}",
                  "solution": "List plans through the catalog command and retry with a valid plan identifier.",
              },
              "STEP_NOT_FOUND": {
                  "description": "No step with the given step_id exists in the resolved plan.",
                  "message": "step not found: {step_id}",
                  "solution": "Inspect the plan tree to find a valid step_id and retry.",
              },
          },
          "best_practices": [
              "Resolve the plan tree first if the exact step_id is not already known.",
              "Treat depends_on and dependents as read-only classification, not as an edit surface.",
          ],
      }
  ```

  Constraints:
  - The module has exactly one top-level function, `get_graph_deps_metadata`,
    with signature `def get_graph_deps_metadata(cls) -> dict:` (`cls` is the
    command class; its `name`, `version`, `descr`, `category`, `author`,
    `email` class attributes are read via `cls.name`, `cls.version`,
    `cls.descr`, `cls.category`, `cls.author`, `cls.email`).
  - The returned dict has exactly these top-level keys, matching the
    metadatastd required_fields for `metadata()`: `name`, `version`,
    `description`, `category`, `author`, `email`, `detailed_description`,
    `parameters`, `return_value`, `usage_examples`, `error_cases`,
    `best_practices`.
  - `parameters["plan"]` and `parameters["step_id"]` each carry exactly the
    keys `description` (str), `type` (str, value `"string"`), `required`
    (bool, value `True`) — the metadatastd parameters_entry_required set.
  - `return_value` has exactly the keys `success` and `error`. `success` has
    `description`, `data` (mapping field name to field description), and
    `example` (mapping field name to an example value). `error` has
    `description`, `code`, `message`, `details`.
  - `error_cases` has exactly the keys `PLAN_NOT_FOUND` and `STEP_NOT_FOUND`,
    each with exactly `description`, `message`, `solution` — these are the
    only two error codes the graph_deps command can return: `PLAN_NOT_FOUND`
    when `plan_manager.commands.resolve.resolve_plan(conn, plan)` cannot
    resolve the `plan` parameter, `STEP_NOT_FOUND` when no step with the
    given `step_id` exists in the resolved plan.
  - No imports beyond `from __future__ import annotations` are needed or
    permitted; the function only reads attributes off `cls` and returns a
    literal dict.
  - Do not add any other function, class, or module-level statement.

  MRS excerpt (concept definitions relevant to this AS):

  C-023 CommandContract: "Adapter-conformant command class shape with
  schema, metadata, and semantic validation." Relevant property: "metadata
  dictionary must never contradict the schema" — the `parameters` entries
  produced here (`plan`, `step_id`, both required strings) must mirror
  exactly the parameter names and required-ness that the command's
  `get_schema()` declares.

  C-026 ErrorModel: "Two-layer error contract of adapter JSON-RPC codes and
  stable domain string codes." Relevant property: "every declared error case
  returnable and every returnable case declared" — the `error_cases` dict
  produced here must list exactly the domain error codes the graph_deps
  command execution path can return, no more and no fewer.
verification:
  type: import
  target: plan_manager.commands.graph_deps_metadata
  expected: >
    Importing plan_manager.commands.graph_deps_metadata succeeds and the
    module defines a callable get_graph_deps_metadata(cls) that, given an
    object with name, version, descr, category, author, email attributes,
    returns a dict containing exactly the metadatastd required top-level
    keys, with parameters plan and step_id both marked required and with
    error_cases containing exactly PLAN_NOT_FOUND and STEP_NOT_FOUND.
status: done

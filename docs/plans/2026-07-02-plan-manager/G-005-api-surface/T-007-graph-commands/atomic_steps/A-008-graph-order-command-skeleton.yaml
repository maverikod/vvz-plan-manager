step_id: A-008
parent_tactical_step: T-007
name: Create graph_order_command.py class skeleton
target_file: plan_manager/commands/graph_order_command.py
operation: create_file
priority: 1
depends_on: []
concepts: [C-023]
prompt: |
  Create a new Python file at the exact project-relative path
  `plan_manager/commands/graph_order_command.py` with exactly the following
  content, byte-identical:

  ```python
  """Graph command: topological execution order of a plan's steps (C-009)."""
  from __future__ import annotations

  from mcp_proxy_adapter.commands.base import Command
  from mcp_proxy_adapter.commands.result import SuccessResult


  class GraphOrderCommand(Command):
      """Return the topological execution order of a plan's steps."""

      name = "graph_order"
      version = "1.0.0"
      descr = "Return the topological execution order of a plan's steps."
      category = "graph"
      author = "Vasiliy Zdanovskiy"
      email = "vasilyvz@gmail.com"
      result_class = SuccessResult
      use_queue = False
  ```

  Constraints:
  - Import exactly two names: `Command` from `mcp_proxy_adapter.commands.base`
    and `SuccessResult` from `mcp_proxy_adapter.commands.result`. Do not
    import anything else in this file yet.
  - Define exactly one class, `GraphOrderCommand`, inheriting from
    `Command`, with the one-line docstring shown above, verbatim.
  - The class body contains only the eight ClassVar-style attribute
    assignments shown above, in that exact order, with those exact literal
    values. Do not add any method, any other attribute, or any blank line
    inside the class body beyond what is shown.
  - `name` is the literal string `"graph_order"`.
  - `descr` is exactly `"Return the topological execution order of a
    plan's steps."`.
  - `category` is the literal string `"graph"`.
  - `author` is the literal string `"Vasiliy Zdanovskiy"` and `email` is the
    literal string `"vasilyvz@gmail.com"`.
  - `result_class` is the class object `SuccessResult` (not a string, not an
    instance).
  - `use_queue` is the literal boolean `False`.
  - Do not add `get_schema`, `metadata`, `validate_params`, or `execute` in
    this file yet; they are added by later edits to this same file.

  MRS excerpt (concept definitions relevant to this AS):

  C-023 CommandContract: "Adapter-conformant command class shape with
  schema, metadata, and semantic validation." Relevant property: "class
  attributes name, version, descr, category, author, email, result_class,
  use_queue" — every command class realizing CommandContract must define
  exactly this fixed set of class attributes with these exact names.
verification:
  type: static_analysis
  target: plan_manager/commands/graph_order_command.py
  expected: >
    The file parses as valid Python 3.12 source with no syntax errors and
    defines a single class GraphOrderCommand(Command) whose body consists
    solely of the eight listed class attributes with the exact literal
    values specified, and no methods.
status: draft

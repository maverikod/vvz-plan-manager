step_id: A-023
parent_tactical_step: T-007
name: Add validate_params to GraphImpactCommand
target_file: plan_manager/commands/graph_impact_command.py
operation: modify_file
priority: 4
depends_on: [A-022]
concepts: [C-023]
prompt: |
  Modify the existing file at the exact project-relative path
  `plan_manager/commands/graph_impact_command.py`.

  Current full content of the file (state after all lower-priority atomic
  steps on this file have been applied):

  ```python
  """Graph command: read-only invalidation projection of one step (C-009)."""
  from __future__ import annotations

  from mcp_proxy_adapter.commands.base import Command
  from mcp_proxy_adapter.commands.result import SuccessResult

  from plan_manager.commands.graph_impact_metadata import get_graph_impact_metadata


  class GraphImpactCommand(Command):
      """Return the read-only transitive impact set of one step."""

      name = "graph_impact"
      version = "1.0.0"
      descr = "Return the read-only transitive impact set of one step."
      category = "graph"
      author = "Vasiliy Zdanovskiy"
      email = "vasilyvz@gmail.com"
      result_class = SuccessResult
      use_queue = False

      @classmethod
      def get_schema(cls) -> dict:
          return {
              "type": "object",
              "properties": {
                  "plan": {
                      "type": "string",
                      "description": "Plan identifier (name or uuid) resolved against the catalog.",
                  },
                  "step_id": {
                      "type": "string",
                      "description": "step_id of the target step within the plan.",
                  },
              },
              "required": ["plan", "step_id"],
              "additionalProperties": False,
          }

      @classmethod
      def metadata(cls) -> dict:
          return get_graph_impact_metadata(cls)
  ```

  Replace this content with exactly the following full file content,
  byte-identical:

  ```python
  """Graph command: read-only invalidation projection of one step (C-009)."""
  from __future__ import annotations

  from mcp_proxy_adapter.commands.base import Command
  from mcp_proxy_adapter.commands.result import SuccessResult

  from plan_manager.commands.graph_impact_metadata import get_graph_impact_metadata


  class GraphImpactCommand(Command):
      """Return the read-only transitive impact set of one step."""

      name = "graph_impact"
      version = "1.0.0"
      descr = "Return the read-only transitive impact set of one step."
      category = "graph"
      author = "Vasiliy Zdanovskiy"
      email = "vasilyvz@gmail.com"
      result_class = SuccessResult
      use_queue = False

      @classmethod
      def get_schema(cls) -> dict:
          return {
              "type": "object",
              "properties": {
                  "plan": {
                      "type": "string",
                      "description": "Plan identifier (name or uuid) resolved against the catalog.",
                  },
                  "step_id": {
                      "type": "string",
                      "description": "step_id of the target step within the plan.",
                  },
              },
              "required": ["plan", "step_id"],
              "additionalProperties": False,
          }

      @classmethod
      def metadata(cls) -> dict:
          return get_graph_impact_metadata(cls)

      def validate_params(self, params: dict) -> dict:
          params = super().validate_params(params)
          return params
  ```

  Constraints:
  - Append the `validate_params` method shown above, with signature
    `def validate_params(self, params: dict) -> dict:`, after the existing
    `metadata` method, separated by exactly one blank line.
  - The body is exactly two statements: `params = super().validate_params(
    params)` followed by `return params`. `graph_impact` has no parameter
    semantics beyond what the JSON Schema from `get_schema()` already
    enforces (two required strings, no mutually exclusive fields, no
    numeric ranges), so no additional semantic checks are added; this
    method exists solely to satisfy the CommandContract method shape and to
    invoke the base validator.
  - No other line changes.
  - Do not add `execute` in this edit.

  MRS excerpt (concept definitions relevant to this AS):

  C-023 CommandContract: "Adapter-conformant command class shape with
  schema, metadata, and semantic validation." Relevant properties: "shallow
  adapter validation compensated by validate_params semantics" and "read
  commands never mutate; mutating commands verify by re-read" —
  graph_impact is a read command with no additional semantic constraints
  beyond the schema, so validate_params delegates entirely to the base
  implementation and performs no mutation. graph_impact never changes any
  step's status; it is a read-only invalidation projection.
verification:
  type: static_analysis
  target: plan_manager/commands/graph_impact_command.py
  expected: >
    The file parses as valid Python 3.12 source with no syntax errors and
    GraphImpactCommand defines validate_params(self, params) that calls
    super().validate_params(params) and returns its result unchanged.
status: draft

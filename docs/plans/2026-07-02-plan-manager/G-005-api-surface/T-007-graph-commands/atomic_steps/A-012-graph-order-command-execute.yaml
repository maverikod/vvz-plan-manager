step_id: A-012
parent_tactical_step: T-007
name: Add execute to GraphOrderCommand
target_file: plan_manager/commands/graph_order_command.py
operation: modify_file
priority: 5
depends_on: [A-011]
concepts: [C-009, C-023, C-026]
prompt: |
  Modify the existing file at the exact project-relative path
  `plan_manager/commands/graph_order_command.py`.

  Current full content of the file (state after all lower-priority atomic
  steps on this file have been applied):

  ```python
  """Graph command: topological execution order of a plan's steps (C-009)."""
  from __future__ import annotations

  from mcp_proxy_adapter.commands.base import Command
  from mcp_proxy_adapter.commands.result import SuccessResult

  from plan_manager.commands.graph_order_metadata import get_graph_order_metadata


  class GraphOrderCommand(Command):
      """Return the topological execution order of a plan's steps."""

      name = "graph_order"
      version = "1.0.0"
      descr = "Return the topological execution order of a plan's steps."
      category = "graph"
      author = "Vasiliy Zdanovskiy"
      email = "vasilyvz@gmail.com"
      result_class = SuccessResult
      use_queue = False

      @classmethod
      def get_schema(cls) -> dict:
          return {
              "type": "object",
              "properties": {
                  "plan": {
                      "type": "string",
                      "description": "Plan identifier (name or uuid) resolved against the catalog.",
                  },
              },
              "required": ["plan"],
              "additionalProperties": False,
          }

      @classmethod
      def metadata(cls) -> dict:
          return get_graph_order_metadata(cls)

      def validate_params(self, params: dict) -> dict:
          params = super().validate_params(params)
          return params
  ```

  Replace this content with exactly the following full file content,
  byte-identical:

  ```python
  """Graph command: topological execution order of a plan's steps (C-009)."""
  from __future__ import annotations

  from mcp_proxy_adapter.commands.base import Command
  from mcp_proxy_adapter.commands.result import SuccessResult, ErrorResult

  from plan_manager.commands.errors import domain_error, map_exception
  from plan_manager.commands.graph_order_metadata import get_graph_order_metadata
  from plan_manager.commands.resolve import resolve_plan
  from plan_manager.runtime.context import db_connection
  from plan_manager.verify.gate_data import artifact_path_of
  from plan_manager.views.dependency_graph import (
      build_edges,
      load_steps,
      topological_order,
  )


  class GraphOrderCommand(Command):
      """Return the topological execution order of a plan's steps."""

      name = "graph_order"
      version = "1.0.0"
      descr = "Return the topological execution order of a plan's steps."
      category = "graph"
      author = "Vasiliy Zdanovskiy"
      email = "vasilyvz@gmail.com"
      result_class = SuccessResult
      use_queue = False

      @classmethod
      def get_schema(cls) -> dict:
          return {
              "type": "object",
              "properties": {
                  "plan": {
                      "type": "string",
                      "description": "Plan identifier (name or uuid) resolved against the catalog.",
                  },
              },
              "required": ["plan"],
              "additionalProperties": False,
          }

      @classmethod
      def metadata(cls) -> dict:
          return get_graph_order_metadata(cls)

      def validate_params(self, params: dict) -> dict:
          params = super().validate_params(params)
          return params

      async def execute(self, **kwargs) -> SuccessResult | ErrorResult:
          try:
              plan = kwargs["plan"]
              with db_connection() as conn:
                  plan_obj = resolve_plan(conn, plan)
                  nodes = load_steps(conn, plan_obj.uuid)
                  edges = build_edges(nodes)
                  order, residual = topological_order(nodes, edges)
                  if residual:
                      return domain_error(
                          "CYCLE_DETECTED",
                          "dependency graph has a cycle",
                          {
                              "cycle": [
                                  artifact_path_of(nodes, nodes[u]) for u in residual
                              ]
                          },
                      )
                  data = {
                      "order": [artifact_path_of(nodes, nodes[u]) for u in order]
                  }
              return SuccessResult(data=data)
          except Exception as exc:
              return map_exception(exc)
  ```

  Constraints:
  - Change the existing result import line to
    `from mcp_proxy_adapter.commands.result import SuccessResult,
    ErrorResult` (add `ErrorResult`).
  - Add four new import lines, each `from <module> import <names>`, in this
    exact order, alphabetically by module path, each a separate `from`
    statement:
    `from plan_manager.commands.errors import domain_error, map_exception`;
    `from plan_manager.commands.graph_order_metadata import
    get_graph_order_metadata` (already present, unchanged, kept in its
    alphabetical position);
    `from plan_manager.commands.resolve import resolve_plan`;
    `from plan_manager.runtime.context import db_connection`;
    `from plan_manager.verify.gate_data import artifact_path_of`;
    `from plan_manager.views.dependency_graph import (build_edges,
    load_steps, topological_order)` (parenthesized, one name per line,
    exactly as shown in the replacement content above).
  - `plan_manager.commands.errors` defines `def domain_error(code: str,
    message: str, details: dict | None = None) -> ErrorResult:` and
    `def map_exception(exc) -> ErrorResult:` which converts any exception
    into an ErrorResult.
  - `plan_manager.commands.resolve` defines `def resolve_plan(conn, plan:
    str) -> Plan:` which raises `DomainCommandError("PLAN_NOT_FOUND", ...)`
    when the plan cannot be resolved from the catalog; `Plan` has fields
    `uuid`, `name`, `status`, `context_budget`, `head_revision_uuid`.
  - `plan_manager.runtime.context` defines `def db_connection():`, a
    context manager yielding a `psycopg.Connection`, used as
    `with db_connection() as conn:`.
  - `plan_manager.views.dependency_graph` defines `def load_steps(conn,
    plan_uuid) -> dict[uuid.UUID, Step]:` where `Step` has fields `uuid`,
    `plan_uuid`, `parent_step_uuid`, `level`, `step_id`, `slug`, `fields:
    dict`, `depends_on: list[str]`, `concepts: list[str]`, `status: str`;
    `def build_edges(nodes: dict[uuid.UUID, Step]) -> set[tuple[uuid.UUID,
    uuid.UUID]]:` returning every `(prerequisite_uuid, dependent_uuid)`
    edge of the dependency graph; and `def topological_order(nodes, edges)
    -> tuple[list[uuid.UUID], list[uuid.UUID]]:` returning `(order,
    cycle_residual)` — a Kahn topological order with deterministic
    tie-break, plus the residual subgraph's node uuids; `cycle_residual` is
    the empty list when the graph is acyclic and is non-empty (listing the
    residual cycle members) when a cycle prevents a total order.
  - `plan_manager.verify.gate_data` defines `def artifact_path_of(nodes:
    dict[uuid.UUID, Step], step: Step) -> str:`.
  - Append the `execute` method shown above, with signature `async def
    execute(self, **kwargs) -> SuccessResult | ErrorResult:`, after the
    existing `validate_params` method, separated by exactly one blank line.
  - `topological_order(nodes, edges)` is called directly inside the outer
    `try` block (no nested `try` around it: it never raises for a cycle, it
    reports the cycle via its `cycle_residual` return value). Immediately
    after the call, `if residual:` — when `residual` (the second tuple
    element) is non-empty, return `domain_error("CYCLE_DETECTED",
    "dependency graph has a cycle", {"cycle": [artifact_path_of(nodes,
    nodes[u]) for u in residual]})` directly — this return happens before
    the outer `except Exception as exc: return map_exception(exc)` handler
    is reached, so a cycle is intercepted before generic exception mapping.
  - `resolve_plan` is called directly (not wrapped in an inner try); any
    `DomainCommandError("PLAN_NOT_FOUND", ...)` it raises propagates to the
    outer `except Exception as exc: return map_exception(exc)` handler.
  - The success payload is a dict with exactly one key, `order`, a list of
    `artifact_path_of` strings for each uuid in the first element (`order`)
    of the tuple returned by `topological_order(nodes, edges)`, in that
    exact order, returned as `SuccessResult(data=data)`. This branch is
    reached only when `residual` is empty.
  - No other line changes.

  MRS excerpt (concept definitions relevant to this AS):

  C-009 DependencyGraph: "Computed projection set over steps covering
  execution order, parallelism, cycles, and impact." Relevant property:
  "Kahn topological order with deterministic tie-break; residual subgraph
  reported as cycle set" — topological_order() implements exactly this
  algorithm and its second return value (`cycle_residual`) carries exactly
  the residual subgraph members, empty when the graph is acyclic.

  C-023 CommandContract: "Adapter-conformant command class shape with
  schema, metadata, and semantic validation." Relevant property: "read
  commands never mutate; mutating commands verify by re-read" — execute
  only opens a connection to read the plan and step projection, and never
  writes.

  C-026 ErrorModel: "Two-layer error contract of adapter JSON-RPC codes and
  stable domain string codes." Relevant property: "every declared error
  case returnable and every returnable case declared" — execute can return
  exactly the two domain codes PLAN_NOT_FOUND (mapped from resolve_plan's
  exception via map_exception) and CYCLE_DETECTED (returned directly via
  domain_error when topological_order reports a non-empty cycle_residual).
verification:
  type: import
  target: plan_manager.commands.graph_order_command
  expected: >
    Importing plan_manager.commands.graph_order_command succeeds and
    GraphOrderCommand defines name, version, descr, category, author,
    email, result_class, use_queue, get_schema, metadata, validate_params,
    and an async execute method that resolves the plan, loads the step
    projection, computes the topological order, returns a SuccessResult
    with an order list of artifact paths on success, returns a
    CYCLE_DETECTED ErrorResult with the cycle's artifact paths when
    topological_order reports a non-empty cycle residual, and returns a
    PLAN_NOT_FOUND ErrorResult when the plan cannot be resolved.
status: done

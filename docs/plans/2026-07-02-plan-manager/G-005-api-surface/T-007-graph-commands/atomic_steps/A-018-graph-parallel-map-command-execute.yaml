step_id: A-018
parent_tactical_step: T-007
name: Add execute to GraphParallelMapCommand
target_file: plan_manager/commands/graph_parallel_map_command.py
operation: modify_file
priority: 5
depends_on: [A-017]
concepts: [C-009, C-023, C-026]
prompt: |
  Modify the existing file at the exact project-relative path
  `plan_manager/commands/graph_parallel_map_command.py`.

  Current full content of the file (state after all lower-priority atomic
  steps on this file have been applied):

  ```python
  """Graph command: parallel wave partition of a plan's steps (C-009)."""
  from __future__ import annotations

  from mcp_proxy_adapter.commands.base import Command
  from mcp_proxy_adapter.commands.result import SuccessResult

  from plan_manager.commands.graph_parallel_map_metadata import (
      get_graph_parallel_map_metadata,
  )


  class GraphParallelMapCommand(Command):
      """Return the parallel wave partition of a plan's steps by prerequisite depth."""

      name = "graph_parallel_map"
      version = "1.0.0"
      descr = "Return the parallel wave partition of a plan's steps by prerequisite depth."
      category = "graph"
      author = "Vasiliy Zdanovskiy"
      email = "vasilyvz@gmail.com"
      result_class = SuccessResult
      use_queue = False

      @classmethod
      def get_schema(cls) -> dict:
          return {
              "type": "object",
              "properties": {
                  "plan": {
                      "type": "string",
                      "description": "Plan identifier (name or uuid) resolved against the catalog.",
                  },
              },
              "required": ["plan"],
              "additionalProperties": False,
          }

      @classmethod
      def metadata(cls) -> dict:
          return get_graph_parallel_map_metadata(cls)

      def validate_params(self, params: dict) -> dict:
          params = super().validate_params(params)
          return params
  ```

  Replace this content with exactly the following full file content,
  byte-identical:

  ```python
  """Graph command: parallel wave partition of a plan's steps (C-009)."""
  from __future__ import annotations

  from mcp_proxy_adapter.commands.base import Command
  from mcp_proxy_adapter.commands.result import SuccessResult, ErrorResult

  from plan_manager.commands.errors import map_exception
  from plan_manager.commands.graph_parallel_map_metadata import (
      get_graph_parallel_map_metadata,
  )
  from plan_manager.commands.resolve import resolve_plan
  from plan_manager.runtime.context import db_connection
  from plan_manager.verify.gate_data import artifact_path_of
  from plan_manager.views.dependency_graph import load_steps, parallel_waves


  class GraphParallelMapCommand(Command):
      """Return the parallel wave partition of a plan's steps by prerequisite depth."""

      name = "graph_parallel_map"
      version = "1.0.0"
      descr = "Return the parallel wave partition of a plan's steps by prerequisite depth."
      category = "graph"
      author = "Vasiliy Zdanovskiy"
      email = "vasilyvz@gmail.com"
      result_class = SuccessResult
      use_queue = False

      @classmethod
      def get_schema(cls) -> dict:
          return {
              "type": "object",
              "properties": {
                  "plan": {
                      "type": "string",
                      "description": "Plan identifier (name or uuid) resolved against the catalog.",
                  },
              },
              "required": ["plan"],
              "additionalProperties": False,
          }

      @classmethod
      def metadata(cls) -> dict:
          return get_graph_parallel_map_metadata(cls)

      def validate_params(self, params: dict) -> dict:
          params = super().validate_params(params)
          return params

      async def execute(self, **kwargs) -> SuccessResult | ErrorResult:
          try:
              plan = kwargs["plan"]
              with db_connection() as conn:
                  plan_obj = resolve_plan(conn, plan)
                  nodes = load_steps(conn, plan_obj.uuid)
                  waves = parallel_waves(nodes)
                  data = {
                      "waves": [
                          [artifact_path_of(nodes, nodes[u]) for u in wave]
                          for wave in waves
                      ]
                  }
              return SuccessResult(data=data)
          except Exception as exc:
              return map_exception(exc)
  ```

  Constraints:
  - Change the existing result import line to
    `from mcp_proxy_adapter.commands.result import SuccessResult,
    ErrorResult` (add `ErrorResult`).
  - Add four new import lines/blocks, in this exact order, alphabetically
    by module path:
    `from plan_manager.commands.errors import map_exception`;
    the existing parenthesized `from
    plan_manager.commands.graph_parallel_map_metadata import (
    get_graph_parallel_map_metadata, )` block, unchanged, kept in its
    alphabetical position;
    `from plan_manager.commands.resolve import resolve_plan`;
    `from plan_manager.runtime.context import db_connection`;
    `from plan_manager.verify.gate_data import artifact_path_of`;
    `from plan_manager.views.dependency_graph import load_steps,
    parallel_waves`.
  - `plan_manager.commands.errors` defines `def map_exception(exc) ->
    ErrorResult:` which converts any exception into an ErrorResult.
  - `plan_manager.commands.resolve` defines `def resolve_plan(conn, plan:
    str) -> Plan:` which raises `DomainCommandError("PLAN_NOT_FOUND", ...)`
    when the plan cannot be resolved from the catalog; `Plan` has fields
    `uuid`, `name`, `status`, `context_budget`, `head_revision_uuid`.
  - `plan_manager.runtime.context` defines `def db_connection():`, a
    context manager yielding a `psycopg.Connection`, used as
    `with db_connection() as conn:`.
  - `plan_manager.views.dependency_graph` defines `def load_steps(conn,
    plan_uuid) -> dict[uuid.UUID, Step]:` where `Step` has fields `uuid`,
    `plan_uuid`, `parent_step_uuid`, `level`, `step_id`, `slug`, `fields:
    dict`, `depends_on: list[str]`, `concepts: list[str]`, `status: str`;
    and `def parallel_waves(nodes) -> list[list[uuid.UUID]]:` returning the
    wave partition, outer list ordered by ascending prerequisite depth,
    each inner list the uuids of the steps in that wave.
  - `plan_manager.verify.gate_data` defines `def artifact_path_of(nodes:
    dict[uuid.UUID, Step], step: Step) -> str:`.
  - Append the `execute` method shown above, with signature `async def
    execute(self, **kwargs) -> SuccessResult | ErrorResult:`, after the
    existing `validate_params` method, separated by exactly one blank
    line.
  - `resolve_plan` is called directly (not wrapped in an inner try); any
    `DomainCommandError("PLAN_NOT_FOUND", ...)` it raises propagates to the
    final `except Exception as exc: return map_exception(exc)` handler.
  - The success payload is a dict with exactly one key, `waves`, a list
    with one entry per wave in `parallel_waves(nodes)` (in that exact
    order), each entry a list of `artifact_path_of` strings for the uuids
    in that wave (in that exact order), returned as
    `SuccessResult(data=data)`.
  - No other line changes.

  MRS excerpt (concept definitions relevant to this AS):

  C-009 DependencyGraph: "Computed projection set over steps covering
  execution order, parallelism, cycles, and impact." Relevant property:
  "waves partition nodes by prerequisite depth; impact follows the
  invalidation rule" — parallel_waves() implements exactly this
  depth-based partition.

  C-023 CommandContract: "Adapter-conformant command class shape with
  schema, metadata, and semantic validation." Relevant property: "read
  commands never mutate; mutating commands verify by re-read" — execute
  only opens a connection to read the plan and step projection, and never
  writes.

  C-026 ErrorModel: "Two-layer error contract of adapter JSON-RPC codes and
  stable domain string codes." Relevant property: "every declared error
  case returnable and every returnable case declared" — execute can return
  exactly the single domain code PLAN_NOT_FOUND, converted to an
  ErrorResult by map_exception via the final except handler.
verification:
  type: import
  target: plan_manager.commands.graph_parallel_map_command
  expected: >
    Importing plan_manager.commands.graph_parallel_map_command succeeds and
    GraphParallelMapCommand defines name, version, descr, category,
    author, email, result_class, use_queue, get_schema, metadata,
    validate_params, and an async execute method that resolves the plan,
    loads the step projection, computes the wave partition, and returns a
    SuccessResult with a waves list of lists of artifact paths on success,
    and returns a PLAN_NOT_FOUND ErrorResult when the plan cannot be
    resolved.
status: draft

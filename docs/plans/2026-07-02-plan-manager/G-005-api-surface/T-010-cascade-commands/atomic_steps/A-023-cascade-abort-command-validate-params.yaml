step_id: A-023
parent_tactical_step: T-010
name: CascadeAbortCommand.validate_params
target_file: plan_manager/commands/cascade_abort_command.py
operation: modify_file
priority: 4
depends_on:
- A-022
concepts:
- C-023
prompt: |-
  Target file: plan_manager/commands/cascade_abort_command.py
  Operation: modify_file
  Method signature: def validate_params(self, params: dict) -> dict:

  The file currently contains exactly the following content (this is the state after all lower-priority steps on this file have been applied; do not alter any existing line):

  ```python
  """Cascade abort command: discards the open cascade of a plan."""

  from mcp_proxy_adapter.commands.base import Command
  from mcp_proxy_adapter.commands.result import SuccessResult, ErrorResult

  from plan_manager.commands.errors import domain_error, map_exception
  from plan_manager.commands.resolve import resolve_plan
  from plan_manager.runtime.context import db_connection
  from plan_manager.cascade.record import get_open_cascade
  from plan_manager.cascade.close import abort_cascade
  from plan_manager.domain.plan import get_plan
  from plan_manager.commands.cascade_abort_metadata import get_cascade_abort_metadata


  class CascadeAbortCommand(Command):
      """Discard the plan's open cascade and restore the base revision."""

      name = "cascade_abort"
      version = "1.0.0"
      descr = (
          "Discard the plan's open cascade, restoring the working state "
          "to the base revision."
      )
      category = "cascade"
      author = "Vasiliy Zdanovskiy"
      email = "vasilyvz@gmail.com"
      result_class = SuccessResult
      use_queue = False

      @classmethod
      def get_schema(cls) -> dict:
          """Return the JSON Schema for cascade_abort input parameters.

          Returns:
              A JSON Schema object accepting exactly one required string
              parameter, "plan".
          """
          return {
              "type": "object",
              "properties": {
                  "plan": {
                      "type": "string",
                      "description": "Plan UUID or unique plan name.",
                  }
              },
              "required": ["plan"],
              "additionalProperties": False,
          }

      @classmethod
      def metadata(cls) -> dict:
          """Return the extended documentation metadata for cascade_abort.

          Returns:
              The dictionary produced by get_cascade_abort_metadata(cls).
          """
          return get_cascade_abort_metadata(cls)

  ```

  Append exactly the following method to the end of the class body, directly after the metadata method shown above, separated by one blank line:

  ```python

      def validate_params(self, params: dict) -> dict:
          """Validate cascade_abort input parameters.

          Args:
              params: Raw input parameters as received by the command.

          Returns:
              The validated parameters dictionary.
          """
          params = super().validate_params(params)
          return params

  ```

  Constraints:
  - Python 3.12+ syntax; use built-in generic types (dict, list) for annotations, not typing.Dict/typing.List.
  - Do not add any code beyond exactly what this prompt specifies.
  - Do not add a __main__ guard, logging, or any helper not listed here.
  - Preserve exactly one blank line between top-level imports and the class definition, and exactly one blank line between methods.
  - Do not reformat or otherwise change any line of the current file content shown above; only append the specified new code at the specified location.
  - The method must call super().validate_params(params) first and return its result unmodified; no additional semantic validation is performed by this command because get_schema's required=["plan"] and additionalProperties=false are the only constraints on input.

  MRS excerpt (concepts referenced by this step):
  concepts:
  - concept_id: C-023
    name: CommandContract
    definition: Adapter-conformant command class shape with schema, metadata, and semantic validation.
    properties:
    - class attributes name, version, descr, category, author, email, result_class, use_queue
    - get_schema strict subset with explicit additionalProperties false
    - shallow adapter validation compensated by validate_params semantics
    - metadata dictionary must never contradict the schema
    - read commands never mutate; mutating commands verify by re-read
    - destructive or bulk operations expose a dry-run mode defaulting to on
    source_labels: ["{t4k1}", "{y2m6}", "{h2f8}", "{f4d6}", "{z5t4}", "{r1c7}"]
verification:
  type: import
  target: plan_manager.commands.cascade_abort_command.CascadeAbortCommand.validate_params
  expected: >
    Importing plan_manager.commands.cascade_abort_command succeeds and CascadeAbortCommand.validate_params
    is defined, calls super().validate_params(params) first, and returns its result unmodified.
status: draft

step_id: A-004
parent_tactical_step: T-006
name: step_tree command class
target_file: plan_manager/commands/step_tree_command.py
operation: create_file
priority: 1
depends_on: [A-003]
concepts: [C-023, C-026, C-005]
prompt: |
  Create the file `plan_manager/commands/step_tree_command.py` from scratch
  (the file does not exist yet; do not assume any pre-existing content).
  This is a short, single-pass command (read-only, no cascade admission),
  so the whole class — including a fully implemented `execute` — is written
  in this one AS.

  Required imports at the top of the file, exactly these and in this order:

  ```python
  from typing import Any, ClassVar

  from mcp_proxy_adapter.commands.base import Command
  from mcp_proxy_adapter.commands.result import ErrorResult, SuccessResult

  from plan_manager.commands.errors import map_exception
  from plan_manager.commands.resolve import resolve_plan
  from plan_manager.commands.step_tree_metadata import get_step_tree_metadata
  from plan_manager.runtime.context import db_connection
  from plan_manager.verify.gate_data import artifact_path_of
  from plan_manager.views.dependency_graph import load_steps
  ```

  Module docstring (first line of the file, before imports):
  `"""Command: return the full step tree of a plan."""`

  Define exactly one class:

  ```python
  class StepTreeCommand(Command):
  ```

  Class docstring: `"""Return the plan's full step tree with statuses."""`

  Class body — exactly these class attributes:

  ```python
  name: ClassVar[str] = "step_tree"
  version: ClassVar[str] = "1.0.0"
  descr: ClassVar[str] = "Return the plan's full step tree as a flat, sorted list with statuses."
  category: ClassVar[str] = "step"
  author: ClassVar[str] = "Vasiliy Zdanovskiy"
  email: ClassVar[str] = "vasilyvz@gmail.com"
  result_class: ClassVar[type] = SuccessResult
  use_queue: ClassVar[bool] = False
  ```

  Then exactly these four methods, in this order:

  1.
  ```python
  @classmethod
  def get_schema(cls) -> dict[str, Any]:
      """Return the machine-readable input schema for step_tree.

      Returns:
          A JSON-Schema-shaped dict with `type`, `properties`, `required`,
          and `additionalProperties` keys.
      """
      return {
          "type": "object",
          "properties": {
              "plan": {
                  "type": "string",
                  "description": "Plan identifier (UUID or name) to resolve the plan against the catalog.",
              },
          },
          "required": ["plan"],
          "additionalProperties": False,
      }
  ```

  2.
  ```python
  def validate_params(self, params: dict[str, Any]) -> dict[str, Any]:
      """Validate step_tree parameters.

      Args:
          params: Raw parameter dict as received by the adapter.

      Returns:
          The validated parameter dict, unchanged beyond the base
          validator's own normalization.
      """
      params = super().validate_params(params)
      return params
  ```

  3.
  ```python
  async def execute(self, plan: str) -> SuccessResult | ErrorResult:
      """Return the plan's full step tree as a flat, sorted list.

      Args:
          plan: Plan identifier (UUID or name).

      Returns:
          SuccessResult with data {"tree": [...]} on success, where each
          tree entry is {"path", "step_id", "slug", "level", "status"}
          sorted by (level, path); ErrorResult with a stable domain error
          code on failure.
      """
      try:
          with db_connection() as conn:
              p = resolve_plan(conn, plan)
              nodes = load_steps(conn, p.uuid)
              tree = [
                  {
                      "path": artifact_path_of(nodes, s),
                      "step_id": s.step_id,
                      "slug": s.slug,
                      "level": s.level,
                      "status": s.status,
                  }
                  for s in nodes.values()
              ]
              tree.sort(key=lambda entry: (entry["level"], entry["path"]))
              return SuccessResult(data={"tree": tree})
      except Exception as exc:
          return map_exception(exc)
  ```

  4.
  ```python
  @classmethod
  def metadata(cls) -> dict[str, Any]:
      """Return the extended documentation metadata for step_tree.

      Returns:
          The dict produced by `get_step_tree_metadata(cls)`.
      """
      return get_step_tree_metadata(cls)
  ```

  Constraints:
  - `nodes` is a `dict[uuid.UUID, Step]` as returned by `load_steps`; do not
    import `uuid` — the module never constructs a `uuid.UUID` value.
  - Do not add any other methods, attributes, or imports beyond what is
    listed above.
  - Do not read or reference any file other than the imports listed above.

  Verbatim contracts used by the imports above (for reference; do not
  redefine these — they already exist in their respective modules):
  - `plan_manager/commands/errors.py` defines
    `def map_exception(exc) -> ErrorResult`.
  - `plan_manager/commands/resolve.py` defines
    `def resolve_plan(conn, plan: str) -> Plan` where `Plan` has fields
    `uuid, name, status, context_budget, head_revision_uuid`.
  - `plan_manager/runtime/context.py` defines `def db_connection()`, a
    context manager yielding a `psycopg.Connection`.
  - `plan_manager/views/dependency_graph.py` defines
    `def load_steps(conn, plan_uuid) -> dict[uuid.UUID, Step]` where `Step`
    has fields `uuid, plan_uuid, parent_step_uuid, level, step_id, slug,
    fields: dict, depends_on: list[str], concepts: list[str], status: str`.
  - `plan_manager/verify/gate_data.py` defines
    `def artifact_path_of(nodes, step) -> str`.
  - `plan_manager/commands/step_tree_metadata.py` defines
    `def get_step_tree_metadata(cls: type) -> dict[str, Any]`.

  MRS excerpt (concept definitions relevant to this AS):

  ```yaml
  - concept_id: C-023
    name: CommandContract
    definition: Adapter-conformant command class shape with schema, metadata, and semantic validation.
    properties:
    - class attributes name, version, descr, category, author, email, result_class, use_queue
    - get_schema strict subset with explicit additionalProperties false
    - shallow adapter validation compensated by validate_params semantics
    - metadata dictionary must never contradict the schema
    - read commands never mutate; mutating commands verify by re-read
    - destructive or bulk operations expose a dry-run mode defaulting to on

  - concept_id: C-026
    name: ErrorModel
    definition: Two-layer error contract of adapter JSON-RPC codes and stable domain string codes.
    properties:
    - every declared error case returnable and every returnable case declared

  - concept_id: C-005
    name: Step
    definition: Single stored entity for plan levels 3-5 with level, step id, slug, level-specific fields, and status.
    properties:
    - uniform operations across levels; per-level required fields are declarative schema
    - scaffolded creation auto-assigns the next free zero-padded id
    - mutation regime depends on status (draft direct, frozen via cascade)
    - move rewrites every reference to the moved id in one operation
  ```
verification:
  type: import
  target: plan_manager/commands/step_tree_command.py
  expected: >
    The module imports without error; `StepTreeCommand` is a subclass of
    `Command` with class attributes name="step_tree", version="1.0.0",
    category="step", author="Vasiliy Zdanovskiy",
    email="vasilyvz@gmail.com", result_class is SuccessResult, and
    use_queue is False; `StepTreeCommand.get_schema()` returns a dict with
    type "object", required exactly ["plan"], and additionalProperties
    False; `StepTreeCommand.metadata()` returns the dict produced by
    get_step_tree_metadata; `execute` resolves the plan, loads steps, and
    returns a SuccessResult whose data is {"tree": [...]} with entries
    sorted by (level, path).
status: done

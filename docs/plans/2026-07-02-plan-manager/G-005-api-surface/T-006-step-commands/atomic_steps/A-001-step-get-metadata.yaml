step_id: A-001
parent_tactical_step: T-006
name: step_get metadata module
target_file: plan_manager/commands/step_get_metadata.py
operation: create_file
priority: 1
depends_on: []
concepts: [C-023, C-026]
prompt: |
  Create the file `plan_manager/commands/step_get_metadata.py` from scratch
  (the file does not exist yet; do not assume any pre-existing content).

  Write exactly one module-level function:

  ```python
  def get_step_get_metadata(cls: type) -> dict[str, Any]:
  ```

  Required imports at the top of the file:

  ```python
  from typing import Any
  ```

  Module docstring (first line of the file, before imports):
  `"""Extended AI/documentation metadata for the step_get command."""`

  Function docstring (Google style):
  ```
  """Return the extended metadata dictionary for StepGetCommand.

  Args:
      cls: The StepGetCommand class object, used to source identity
          attributes (name, version, category, author, email) so the
          metadata dictionary never drifts from the class definition.

  Returns:
      A dictionary with the required metadata fields: name, version,
      description, category, author, email, detailed_description,
      parameters, return_value, usage_examples, error_cases,
      best_practices.
  """
  ```

  The function body must `return` a dict literal built exactly as follows
  (use `cls.name`, `cls.version`, `cls.descr`, `cls.category`, `cls.author`,
  `cls.email` to source the first six values so the metadata can never drift
  from the command class):

  ```python
  return {
      "name": cls.name,
      "version": cls.version,
      "description": cls.descr,
      "category": cls.category,
      "author": cls.author,
      "email": cls.email,
      "detailed_description": (
          "Returns one step of a plan (level 3 global step, level 4 "
          "tactical step, or level 5 atomic step) identified by its "
          "human-readable step_id, together with its resolved parent "
          "artifact path. This is a read-only command: it never mutates "
          "the plan and performs no admission or cascade checks. The "
          "plan is resolved against the database catalog by the plan "
          "parameter; step_id is matched among the plan's loaded steps."
      ),
      "parameters": {
          "plan": {
              "description": "Plan identifier (UUID or name) resolved against the catalog.",
              "type": "string",
              "required": True,
          },
          "step_id": {
              "description": "Human-readable step identifier (e.g. G-001, T-006, A-003).",
              "type": "string",
              "required": True,
          },
      },
      "return_value": {
          "success": {
              "description": "The step's resolved fields and its parent path.",
              "data": {
                  "uuid": "Immutable UUID identity of the step, as a string.",
                  "step_id": "Human-readable step identifier.",
                  "slug": "Kebab-case slug of the step.",
                  "level": "Hierarchy level of the step (3, 4, or 5).",
                  "status": "Current lifecycle status of the step.",
                  "parent_path": "Artifact path of the parent step, or null for a level-3 step.",
                  "fields": "Level-specific field dictionary.",
                  "depends_on": "List of step_id values this step depends on.",
                  "concepts": "List of MRS concept_id values this step realizes.",
                  "path": "Artifact path of this step.",
              },
              "example": {
                  "uuid": "3fa85f64-5717-4562-b3fc-2c963f66afa6",
                  "step_id": "T-006",
                  "slug": "step-commands",
                  "level": 4,
                  "status": "draft",
                  "parent_path": "docs/plans/example/G-005-api-surface/README.yaml",
                  "fields": {},
                  "depends_on": [],
                  "concepts": ["C-023", "C-026", "C-005", "C-006", "C-007", "C-016"],
                  "path": "docs/plans/example/G-005-api-surface/T-006-step-commands/README.yaml",
              },
          },
          "error": {
              "description": "A domain error result carrying a stable string code.",
              "code": "Stable domain error code (see error_cases).",
              "message": "Human-readable error message.",
              "details": "Additional diagnostic fields when available.",
          },
      },
      "usage_examples": [
          {
              "description": "Read one tactical step by its step_id.",
              "command": {"plan": "plan_manager", "step_id": "T-006"},
              "explanation": "Returns T-006 with its resolved parent path and level-specific fields.",
          },
      ],
      "error_cases": {
          "PLAN_NOT_FOUND": {
              "description": "The plan identifier does not resolve to a plan in the catalog.",
              "message": "plan not found: {plan}",
              "solution": "Call the plan catalog command and retry with a valid plan identifier.",
          },
          "STEP_NOT_FOUND": {
              "description": "No step with the given step_id exists in the resolved plan.",
              "message": "step not found: {step_id}",
              "solution": "Call step_tree to list valid step_id values for the plan.",
          },
      },
      "best_practices": [
          "Use step_tree first to discover valid step_id values before calling step_get.",
          "This command never mutates state; it is safe to call at any time and any status.",
      ],
  }
  ```

  Constraints:
  - The file contains only the module docstring, the `from typing import Any`
    import, and the single function `get_step_get_metadata`.
  - Every key and value in the returned dict must match exactly what is shown
    above; do not add, remove, or rename keys.
  - Do not import or reference `plan_manager.commands.step_get_command` or any
    other file.

  MRS excerpt (concept definitions relevant to this AS):

  ```yaml
  - concept_id: C-023
    name: CommandContract
    definition: Adapter-conformant command class shape with schema, metadata, and semantic validation.
    properties:
    - class attributes name, version, descr, category, author, email, result_class, use_queue
    - get_schema strict subset with explicit additionalProperties false
    - shallow adapter validation compensated by validate_params semantics
    - metadata dictionary must never contradict the schema
    - read commands never mutate; mutating commands verify by re-read
    - destructive or bulk operations expose a dry-run mode defaulting to on

  - concept_id: C-026
    name: ErrorModel
    definition: Two-layer error contract of adapter JSON-RPC codes and stable domain string codes.
    properties:
    - every declared error case returnable and every returnable case declared
  ```
verification:
  type: import
  target: plan_manager/commands/step_get_metadata.py
  expected: >
    The module imports without error; `get_step_get_metadata` is callable
    with a single positional argument and, when called with an object
    exposing name/version/descr/category/author/email attributes, returns a
    dict containing exactly the keys name, version, description, category,
    author, email, detailed_description, parameters, return_value,
    usage_examples, error_cases, best_practices, with error_cases containing
    exactly the keys PLAN_NOT_FOUND and STEP_NOT_FOUND.
status: done

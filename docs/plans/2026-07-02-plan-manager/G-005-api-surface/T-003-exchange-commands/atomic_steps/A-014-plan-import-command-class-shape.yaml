step_id: A-014
parent_tactical_step: T-003
name: plan-import-command-class-shape
target_file: plan_manager/commands/plan_import_command.py
operation: create_file
priority: 1
depends_on: [A-013]
concepts: [C-023, C-021]
prompt: |
  Create the file plan_manager/commands/plan_import_command.py with the
  exact full content below (operation: create_file — the file does not
  exist yet, assume no pre-existing content). This AS creates only the
  module docstring, imports, and the class declaration with its class
  attributes; it does not define any method.

  Exact full file content to write:

  """PlanImportCommand: create a new plan from a standard file layout."""

  from pathlib import Path
  from typing import Any, ClassVar, Dict, Type

  from mcp_proxy_adapter.commands.base import Command
  from mcp_proxy_adapter.commands.result import SuccessResult, ErrorResult

  from plan_manager.commands.errors import domain_error, map_exception
  from plan_manager.commands.resolve import resolve_plan
  from plan_manager.commands.plan_import_metadata import get_plan_import_metadata
  from plan_manager.exchange.importer import import_plan, validate_layout
  from plan_manager.runtime.context import app_config, db_connection


  class PlanImportCommand(Command):
      """Create a new plan from a standard file layout under the export root."""

      name: ClassVar[str] = "plan_import"
      version: ClassVar[str] = "1.0.0"
      descr: ClassVar[str] = "Import a plan from a standard file layout under the configured export root."
      category: ClassVar[str] = "exchange"
      author: ClassVar[str] = "Vasiliy Zdanovskiy"
      email: ClassVar[str] = "vasilyvz@gmail.com"
      result_class: ClassVar[Type[SuccessResult]] = SuccessResult
      use_queue: ClassVar[bool] = True

  Constraints (all explicit, no open decisions):
  - Reproduce the content above byte-for-byte, including blank lines and
    indentation (4 spaces).
  - The class PlanImportCommand extends
    mcp_proxy_adapter.commands.base.Command (imported exactly as
    `from mcp_proxy_adapter.commands.base import Command`).
  - The class attributes are declared with ClassVar type annotations in
    exactly the order shown: name, version, descr, category, author,
    email, result_class, use_queue.
  - result_class is typed ClassVar[Type[SuccessResult]] and set to
    SuccessResult, imported exactly as `from
    mcp_proxy_adapter.commands.result import SuccessResult, ErrorResult`.
  - use_queue is True because plan_import is mutating and queue-bound.
  - category is the literal string "exchange".
  - author is the literal string "Vasiliy Zdanovskiy"; email is the
    literal string "vasilyvz@gmail.com".
  - descr is a non-empty one-line summary string as shown.
  - Do not add get_schema, validate_params, metadata, or execute methods
    in this AS; the class body contains only the eight ClassVar
    attributes shown, nothing else.
  - Do not add any other import, function, or class to this file. Do not
    read or modify any other file.

  Imported foundation/sibling contracts referenced by these imports
  (quoted verbatim; import them exactly as shown, do not reimplement
  them):
  - plan_manager/commands/errors.py exposes: `class DomainCommandError(Exception)`
    (code, message, details=None); `def domain_error(code, message,
    details=None) -> ErrorResult`; `def map_exception(exc) -> ErrorResult`.
  - plan_manager/commands/resolve.py exposes: `def resolve_plan(conn, plan: str) -> Plan`
    (raises DomainCommandError with code PLAN_NOT_FOUND when not found);
    Plan has fields uuid, name, status, context_budget, head_revision_uuid.
  - plan_manager/runtime/context.py exposes: `def db_connection()` — a
    context manager yielding a psycopg.Connection; `def app_config() -> AppConfig`
    — AppConfig has fields embedding_url, export_root: str,
    scoring_threshold, scoring_aggregation, trust_floor, concept_weight.
  - plan_manager/exchange/importer.py exposes: `def validate_layout(source_root: str) -> list[str]`
    (issue list; empty means valid); `def import_plan(conn, source_root: str, author: str) -> uuid.UUID`
    (the created plan uuid; the only file-to-truth path).
  - plan_manager/commands/plan_import_metadata.py exposes:
    `def get_plan_import_metadata(cls) -> Dict[str, Any]` returning the
    metadatastd.yaml-shaped documentation dict for this command.
  - Filesystem discipline: import sources are selected by NAME under the
    configured export root — request parameters never carry filesystem
    paths. `pathlib.Path` is imported exactly as `from pathlib import
    Path` for building `Path(app_config().export_root) / source`.

  MRS excerpt (concept definitions relevant to this AS, verbatim from
  spec.yaml):

  - concept_id: C-023
    name: CommandContract
    definition: Adapter-conformant command class shape with schema,
      metadata, and semantic validation.
    properties:
    - class attributes name, version, descr, category, author, email,
      result_class, use_queue
    - get_schema strict subset with explicit additionalProperties false
    - shallow adapter validation compensated by validate_params semantics
    - metadata dictionary must never contradict the schema
    - read commands never mutate; mutating commands verify by re-read
    - destructive or bulk operations expose a dry-run mode defaulting to
      on

  - concept_id: C-021
    name: ExchangeFormat
    definition: File import/export representation of a plan in the
      standard layout; HRS as Markdown, all other artifacts as YAML.
    properties:
    - export optionally at a named revision; import is the only
      file-to-truth path
    - safe YAML emission with mandatory round-trip; hrs_export
      byte-identical
verification:
  type: import
  target: plan_manager.commands.plan_import_command.PlanImportCommand
  expected: >
    Importing plan_manager.commands.plan_import_command succeeds and
    defines a class PlanImportCommand subclassing
    mcp_proxy_adapter.commands.base.Command with class attributes
    name="plan_import", version="1.0.0", a non-empty descr,
    category="exchange", author="Vasiliy Zdanovskiy",
    email="vasilyvz@gmail.com", result_class is SuccessResult, and
    use_queue is True. No method is yet defined on the class.
status: draft

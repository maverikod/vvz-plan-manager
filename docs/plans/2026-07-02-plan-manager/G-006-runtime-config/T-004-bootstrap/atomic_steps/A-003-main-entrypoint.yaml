step_id: A-003
parent_tactical_step: T-004
name: main-entrypoint
target_file: plan_manager/main.py
operation: modify_file
priority: 3
depends_on: [A-002]
concepts: [C-027, C-028]
status: draft
prompt: |
  Modify the file `plan_manager/main.py` (operation: modify_file). The
  file currently contains exactly the content shown below (this is the
  full, current, byte-exact state of the file — reproduce it unchanged
  and append the new function described after it; do not alter a single
  character of the existing content):

  """Server entry point and platform bootstrap for plan_manager.

  Realises ServerRuntime (C-027): the entry point receives a configuration
  file path, initializes the runtime (validating the custom configuration
  section, C-028, and aborting startup with an explicit report on invalid
  configuration or unreadable secrets), then creates the application through
  the platform application factory and runs it with the platform's server
  engine. The platform (mcp-proxy-adapter) owns the entire external surface:
  the JSON-RPC endpoint with single and batch calls, the asynchronous
  endpoint, health, command listing, heartbeat, WebSocket job push, and
  OpenAPI and help output. Supported protocols (http, https, mtls) follow
  the platform configuration unchanged. The server registers command classes
  only through the platform hook mechanism; it adds no custom route and
  patches no platform internal. The queue-manager section and the platform
  registration section (proxy registration URL, heartbeat, server id,
  instance UUID) are platform-owned and pass through untouched — the server
  functions identically with registration disabled.

  AUTO_IMPORT_MODULES declares, for the platform auto-import facility, the
  modules that spawned worker processes must import so that command classes
  and the registration hook are available in worker process memory.
  """

  import argparse
  import asyncio
  import sys

  import hypercorn.asyncio
  import hypercorn.config

  from plan_manager.runtime.context import init_runtime
  from plan_manager.runtime.config import ConfigError

  AUTO_IMPORT_MODULES: tuple[str, ...] = (
      "plan_manager.hooks",
      "plan_manager.commands.registration",
  )


  def build_app(config_path: str):
      """Create the platform ASGI application for the given configuration file.

      This is the only function that touches the platform API surface
      (mcp_proxy_adapter.api.app.create_app); the imports are placed inside
      the function body so that a mismatch against the platform contract
      stays localized to this one function. The platform owns the entire
      external surface: JSON-RPC with single and batch calls, the
      asynchronous endpoint, health, command listing, heartbeat, WebSocket
      job push, and OpenAPI and help output. Supported protocols (http,
      https, mtls) follow the platform configuration unchanged. The server
      registers command classes only; it adds no route and patches no
      platform internal. The queue-manager section and the platform
      registration section (registration URL, heartbeat URL and interval,
      server id, instance UUID) are read by the platform from the same
      configuration file and passed through untouched — the server functions
      identically with registration disabled.

      Args:
          config_path: Path to the JSON configuration file containing the
              platform-owned sections (server, registration, auth, queue
              manager, and optional ssl/transport/security) plus the
              plan_manager custom section.

      Returns:
          The ASGI application created by the platform application factory.
      """
      from plan_manager import hooks as _plan_manager_hooks
      from mcp_proxy_adapter.api.app import create_app
      from plan_manager.runtime.config import load_raw

      del _plan_manager_hooks
      return create_app(
          app_config=load_raw(config_path),
          config_path=config_path,
      )

  Append exactly two blank lines after the content above, then the new
  function `main` described below. `main` is the command-line entry
  point: it validates the own configuration section before any platform
  component starts, aborting explicitly on failure, then builds and
  serves the platform application.

  MRS excerpt (concept definitions relevant to this step, quoted from the
  machine specification):

  - C-027 ServerRuntime: "Adapter-owned runtime hosting the command
    registry, transport, queue, and bootstrap sequence." Property used
    here: entry point `main --config`; create_app; hypercorn
    engine.
  - C-028 Configuration: "Single JSON configuration with adapter sections
    plus one plan_manager section validated by a Pydantic model."
    Property used here: password only from mounted secrets; invalid
    config aborts startup.

  Imported sibling contract used by this function (quote verbatim; this
  module already exists in the repository, is not created or modified by
  this step, and is not read — only its public signature, given here, is
  needed; the import of it already exists at module level in the current
  file content above):

  - `plan_manager/runtime/context.py` exposes
    `def init_runtime(config_path: str) -> None` (raises `ConfigError`
    with the explicit report on invalid configuration or unreadable
    secrets); `ConfigError` is `plan_manager/runtime/config.py`'s
    `class ConfigError(ValueError)`, already imported at module level in
    the current file content above.

  Names already defined earlier in this same file (shown in the current
  file content above) and used by this function without re-import:
  `argparse`, `asyncio`, `sys`, `hypercorn.asyncio`, `hypercorn.config`,
  `init_runtime`, `ConfigError`, and `build_app(config_path: str)`.

  Add exactly this function (four-space indentation as shown):

  def main(argv: list[str] | None = None) -> int:
      """Run the plan_manager server bootstrap sequence.

      Parses the single required ``--config`` command-line option, then
      initializes the runtime by validating the custom configuration section
      (C-028) before any platform component starts. On invalid configuration
      or unreadable secrets, ``init_runtime`` raises ``ConfigError``; the
      explicit report from that error is printed to stderr and the function
      returns 1 without creating the platform application. On successful
      initialization, the platform application is created through
      ``build_app`` and served with the platform's hypercorn engine under
      ``asyncio.run``.

      Args:
          argv: Command-line arguments excluding the program name; when
              ``None``, arguments are read from ``sys.argv[1:]``.

      Returns:
          0 on clean shutdown after successful startup; 1 when configuration
          validation aborts startup before any platform component starts.
      """
      parser = argparse.ArgumentParser(prog="plan_manager")
      parser.add_argument(
          "--config",
          required=True,
          help="Path to the JSON configuration file.",
      )
      args = parser.parse_args(argv)

      try:
          init_runtime(args.config)
      except ConfigError as exc:
          print(f"configuration error: {exc}", file=sys.stderr)
          return 1

      app = build_app(args.config)
      config = hypercorn.config.Config()
      asyncio.run(hypercorn.asyncio.serve(app, config))
      return 0

  Constraints (all binding, no deviation):
  - Do not modify any character of the pre-existing content shown above;
    append only.
  - Exactly two blank lines separate the `build_app` function's closing
    return statement from the `def main(argv: list[str] | None = None) -> int:`
    line.
  - The docstring of main is exact and complete as shown above; do not
    paraphrase, shorten, or reorder any sentence of it.
  - The argparse option is named exactly `--config`, is `required=True`,
    and its `help` text is exactly `"Path to the JSON configuration file."`;
    no other option or positional argument is defined.
  - `--config` is required so that `argparse` itself rejects invocation
    without it before any other code in `main` runs.
  - Configuration validation happens by calling `init_runtime(args.config)`
    before `build_app` is called, and before `hypercorn.config.Config()`
    or `hypercorn.asyncio.serve` are referenced at all — no platform
    component is constructed until `init_runtime` returns without raising.
  - On `ConfigError`, the message printed to stderr is exactly
    `f"configuration error: {exc}"` via `print(..., file=sys.stderr)`, and
    `main` returns `1` immediately, without calling `build_app` or any
    hypercorn function.
  - On success, `build_app(args.config)` is called exactly once, its
    result assigned to `app`; then `hypercorn.config.Config()` is
    instantiated with no arguments and assigned to `config`; then
    `asyncio.run(hypercorn.asyncio.serve(app, config))` is called; then
    `main` returns `0`.
  - Do not define the `if __name__ == "__main__":` guard or any other
    statement in this step; the file after this step ends immediately
    after `main`'s final `return 0` statement, with a single trailing
    newline.
verification:
  type: static_analysis
  target: plan_manager.main.main
  expected: >
    The function main accepts an optional argv parameter typed as
    list[str] | None defaulting to None and returns int; it defines a
    single required --config argparse option; it calls init_runtime with
    the parsed config path before any hypercorn or build_app call; on
    ConfigError it prints "configuration error: " followed by the
    exception to stderr and returns 1 without calling build_app; on
    success it calls build_app once, constructs hypercorn.config.Config,
    runs hypercorn.asyncio.serve through asyncio.run, and returns 0.

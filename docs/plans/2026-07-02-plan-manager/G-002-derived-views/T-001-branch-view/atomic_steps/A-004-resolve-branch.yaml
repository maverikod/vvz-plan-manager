step_id: A-004
parent_tactical_step: T-001
name: Define resolve_branch function
target_file: plan_manager/views/branch.py
operation: modify_file
priority: 4
depends_on: [A-003]
concepts: [C-008, C-005, C-002]
prompt: >
  Modify the existing Python source file at the exact project-relative path
  `plan_manager/views/branch.py`.

  The file currently contains exactly this content (this is the full
  post-prior-step state; open it and modify it in place, do not recreate
  it from scratch):

  ```python
  """Branch derived view (C-008): resolves one HRS-slice -> GS -> TS -> AS path
  from the plan tree stored in the database into an in-memory, never-persisted
  view. This module is read-only: it issues SELECT statements only against the
  step and paragraph tables and never INSERTs, UPDATEs, or DELETEs.
  """

  import uuid
  from dataclasses import dataclass

  import psycopg

  from plan_manager.domain.step import Step
  from plan_manager.domain.paragraph import Paragraph


  class BranchResolutionError(ValueError):
      """Raised when a branch address does not resolve.

      The exception message names the failing path element: for a missing
      step, the level and step_id that could not be found; for a step that
      does not belong to its addressed parent, the child's step_id and the
      addressed parent's step_id.
      """


  @dataclass
  class Branch:
      """In-memory branch view: one resolved HRS-slice -> GS -> TS -> AS path.

      Never persisted; held only in process memory for the duration of the
      consuming operation (verification or prompt assembly).
      """

      plan_uuid: uuid.UUID
      gs: Step
      ts: Step
      atomic: Step
      hrs_slice: list[Paragraph]


  def _get_step_row(
      conn: psycopg.Connection, plan_uuid: uuid.UUID, level: int, step_id: str
  ) -> Step:
      """Fetch one step row by plan, level, and step_id.

      Executes exactly:
          SELECT uuid, plan_uuid, parent_step_uuid, level, step_id, slug,
                 fields, depends_on, concepts, status
          FROM step
          WHERE plan_uuid = %s AND level = %s AND step_id = %s;

      Parameters
      ----------
      conn: psycopg.Connection
          Open database connection. Only SELECT is issued.
      plan_uuid: uuid.UUID
          Identity of the plan owning the step.
      level: int
          Step level to match (3 for GS, 4 for TS, 5 for AS).
      step_id: str
          Step identifier to match, e.g. "G-002", "T-001", "A-001".

      Returns
      -------
      Step
          Constructed from the fetched row. depends_on and concepts are the
          row's array columns converted with list(...); when a column
          value is NULL, an empty list [] is used instead.

      Raises
      ------
      BranchResolutionError
          If no row matches; the message names the level and step_id that
          could not be found.
      """
      row = conn.execute(
          "SELECT uuid, plan_uuid, parent_step_uuid, level, step_id, slug, "
          "fields, depends_on, concepts, status FROM step "
          "WHERE plan_uuid = %s AND level = %s AND step_id = %s;",
          (plan_uuid, level, step_id),
      ).fetchone()
      if row is None:
          raise BranchResolutionError(
              f"no step found at level {level} with step_id {step_id!r}"
          )
      (
          row_uuid,
          row_plan_uuid,
          parent_step_uuid,
          row_level,
          row_step_id,
          slug,
          fields,
          depends_on,
          concepts,
          status,
      ) = row
      return Step(
          uuid=row_uuid,
          plan_uuid=row_plan_uuid,
          parent_step_uuid=parent_step_uuid,
          level=row_level,
          step_id=row_step_id,
          slug=slug,
          fields=fields,
          depends_on=list(depends_on) if depends_on is not None else [],
          concepts=list(concepts) if concepts is not None else [],
          status=status,
      )
  ```

  Change this file to the following full content: add one import line
  (`from plan_manager.domain.paragraph_store import list_paragraphs`, in
  the same first-party import group as the `plan_manager.domain.step` and
  `plan_manager.domain.paragraph` imports, added as the third line of that
  group), and append a new function `resolve_branch` after `_get_step_row`.
  The resulting full file content must be exactly:

  ```python
  """Branch derived view (C-008): resolves one HRS-slice -> GS -> TS -> AS path
  from the plan tree stored in the database into an in-memory, never-persisted
  view. This module is read-only: it issues SELECT statements only against the
  step and paragraph tables and never INSERTs, UPDATEs, or DELETEs.
  """

  import uuid
  from dataclasses import dataclass

  import psycopg

  from plan_manager.domain.step import Step
  from plan_manager.domain.paragraph import Paragraph
  from plan_manager.domain.paragraph_store import list_paragraphs


  class BranchResolutionError(ValueError):
      """Raised when a branch address does not resolve.

      The exception message names the failing path element: for a missing
      step, the level and step_id that could not be found; for a step that
      does not belong to its addressed parent, the child's step_id and the
      addressed parent's step_id.
      """


  @dataclass
  class Branch:
      """In-memory branch view: one resolved HRS-slice -> GS -> TS -> AS path.

      Never persisted; held only in process memory for the duration of the
      consuming operation (verification or prompt assembly).
      """

      plan_uuid: uuid.UUID
      gs: Step
      ts: Step
      atomic: Step
      hrs_slice: list[Paragraph]


  def _get_step_row(
      conn: psycopg.Connection, plan_uuid: uuid.UUID, level: int, step_id: str
  ) -> Step:
      """Fetch one step row by plan, level, and step_id.

      Executes exactly:
          SELECT uuid, plan_uuid, parent_step_uuid, level, step_id, slug,
                 fields, depends_on, concepts, status
          FROM step
          WHERE plan_uuid = %s AND level = %s AND step_id = %s;

      Parameters
      ----------
      conn: psycopg.Connection
          Open database connection. Only SELECT is issued.
      plan_uuid: uuid.UUID
          Identity of the plan owning the step.
      level: int
          Step level to match (3 for GS, 4 for TS, 5 for AS).
      step_id: str
          Step identifier to match, e.g. "G-002", "T-001", "A-001".

      Returns
      -------
      Step
          Constructed from the fetched row. depends_on and concepts are the
          row's array columns converted with list(...); when a column
          value is NULL, an empty list [] is used instead.

      Raises
      ------
      BranchResolutionError
          If no row matches; the message names the level and step_id that
          could not be found.
      """
      row = conn.execute(
          "SELECT uuid, plan_uuid, parent_step_uuid, level, step_id, slug, "
          "fields, depends_on, concepts, status FROM step "
          "WHERE plan_uuid = %s AND level = %s AND step_id = %s;",
          (plan_uuid, level, step_id),
      ).fetchone()
      if row is None:
          raise BranchResolutionError(
              f"no step found at level {level} with step_id {step_id!r}"
          )
      (
          row_uuid,
          row_plan_uuid,
          parent_step_uuid,
          row_level,
          row_step_id,
          slug,
          fields,
          depends_on,
          concepts,
          status,
      ) = row
      return Step(
          uuid=row_uuid,
          plan_uuid=row_plan_uuid,
          parent_step_uuid=parent_step_uuid,
          level=row_level,
          step_id=row_step_id,
          slug=slug,
          fields=fields,
          depends_on=list(depends_on) if depends_on is not None else [],
          concepts=list(concepts) if concepts is not None else [],
          status=status,
      )


  def resolve_branch(
      conn: psycopg.Connection,
      plan_uuid: uuid.UUID,
      gs_step_id: str,
      ts_step_id: str,
      as_step_id: str,
  ) -> Branch:
      """Resolve one branch address to its in-memory Branch view.

      Fetches the global step (level 3), tactical step (level 4), and
      atomic step (level 5) identified by gs_step_id, ts_step_id, and
      as_step_id within the given plan, verifies parentage, and computes
      the HRS slice.

      Parameters
      ----------
      conn: psycopg.Connection
          Open database connection. Only SELECT statements are issued,
          directly and via _get_step_row and list_paragraphs.
      plan_uuid: uuid.UUID
          Identity of the plan owning the branch.
      gs_step_id: str
          step_id of the global step (level 3), e.g. "G-002".
      ts_step_id: str
          step_id of the tactical step (level 4), e.g. "T-001".
      as_step_id: str
          step_id of the atomic step (level 5), e.g. "A-001".

      Returns
      -------
      Branch
          plan_uuid, the fetched gs/ts/atomic Step rows, and hrs_slice: the
          list of Paragraph rows whose label (braces stripped) appears in
          the global step's fields["source_labels"] list, in document
          order as returned by list_paragraphs.

      Raises
      ------
      BranchResolutionError
          If gs, ts, or atomic cannot be found (raised inside
          _get_step_row, naming the missing level and step_id); if
          ts.parent_step_uuid does not equal gs.uuid (message names
          ts_step_id as the child and gs_step_id as the addressed parent);
          if atomic.parent_step_uuid does not equal ts.uuid (message names
          as_step_id as the child and ts_step_id as the addressed parent).
      """
      gs = _get_step_row(conn, plan_uuid, 3, gs_step_id)
      ts = _get_step_row(conn, plan_uuid, 4, ts_step_id)
      atomic = _get_step_row(conn, plan_uuid, 5, as_step_id)
      if ts.parent_step_uuid != gs.uuid:
          raise BranchResolutionError(
              f"step {ts_step_id!r} does not belong to addressed parent {gs_step_id!r}"
          )
      if atomic.parent_step_uuid != ts.uuid:
          raise BranchResolutionError(
              f"step {as_step_id!r} does not belong to addressed parent {ts_step_id!r}"
          )
      source_labels = gs.fields.get("source_labels", [])
      bare = {label[1:-1] for label in source_labels}
      hrs_slice = [
          paragraph
          for paragraph in list_paragraphs(conn, plan_uuid)
          if paragraph.label is not None and paragraph.label in bare
      ]
      return Branch(
          plan_uuid=plan_uuid, gs=gs, ts=ts, atomic=atomic, hrs_slice=hrs_slice
      )
  ```

  Imports used, with exact module paths:
  `from plan_manager.domain.paragraph_store import list_paragraphs` — the
  sibling module `plan_manager/domain/paragraph_store.py` defines exactly:
  `def list_paragraphs(conn: psycopg.Connection, plan_uuid: uuid.UUID) -> list[Paragraph]`,
  documented as returning all stored binding paragraphs of the plan
  ordered by position. `uuid`, `psycopg`, `Step`, `Paragraph`,
  `BranchResolutionError`, `Branch`, and `_get_step_row` used in this
  function are already imported/defined earlier in the same file, as shown
  in the target content above.

  Exact function signature being created:
  `def resolve_branch(conn: psycopg.Connection, plan_uuid: uuid.UUID, gs_step_id: str, ts_step_id: str, as_step_id: str) -> Branch:`

  The sibling module `plan_manager/domain/step.py` defines exactly:
  `@dataclass class Step` with fields, in order: `uuid: uuid.UUID`,
  `plan_uuid: uuid.UUID`, `parent_step_uuid: uuid.UUID | None`,
  `level: int`, `step_id: str`, `slug: str`, `fields: dict`,
  `depends_on: list[str]`, `concepts: list[str]`, `status: str`.
  The sibling module `plan_manager/domain/paragraph.py` defines exactly:
  `@dataclass class Paragraph` with fields, in order:
  `label: Optional[str]`, `text: str`, `position: int`.

  Constraints:

  - Do not modify the module docstring, `BranchResolutionError`, `Branch`,
    or `_get_step_row`; copy them through unchanged exactly as shown.
  - Fetch order is fixed: gs first via `_get_step_row(conn, plan_uuid, 3, gs_step_id)`,
    then ts via `_get_step_row(conn, plan_uuid, 4, ts_step_id)`, then atomic
    via `_get_step_row(conn, plan_uuid, 5, as_step_id)`.
  - Parentage checks happen after all three fetches succeed, in this order:
    first `ts.parent_step_uuid != gs.uuid`, then
    `atomic.parent_step_uuid != ts.uuid`. Each raises
    `BranchResolutionError` with the exact message text shown above (using
    `!r` on both step_id values) before any further code runs.
  - `source_labels` is read via `gs.fields.get("source_labels", [])` —
    never `gs.fields["source_labels"]` directly, since the key may be
    absent.
  - Each entry of `source_labels` is a string with surrounding curly
    braces, e.g. `"{k2p7}"`; the bare label is `label[1:-1]` (braces
    stripped). `bare` is the set comprehension
    `{label[1:-1] for label in source_labels}` exactly as shown.
  - `hrs_slice` is built by calling `list_paragraphs(conn, plan_uuid)`
    exactly once and filtering with a list comprehension that keeps a
    paragraph only when `paragraph.label is not None and paragraph.label in bare`,
    preserving the order returned by `list_paragraphs` (already document
    order) — no re-sorting.
  - The returned `Branch` is constructed with all five fields as keyword
    arguments: `plan_uuid=plan_uuid, gs=gs, ts=ts, atomic=atomic, hrs_slice=hrs_slice`.
  - Only SELECT statements are issued, transitively through
    `_get_step_row` and `list_paragraphs`; `resolve_branch` itself issues
    no direct SQL.
  - This is the last function in the file; do not add anything after it.

  MRS excerpt (concepts relevant to this AS):

  ```yaml
  - concept_id: C-008
    name: Branch
    definition: Addressable derived view of one path HRS-slice -> GS -> TS -> AS; the unit of verification and prompt assembly.
    properties:
    - never materialized as a stored artifact
    - a mechanically clean branch is measurable while siblings are broken

  - concept_id: C-005
    name: Step
    definition: Single stored entity for plan levels 3-5 with level, step id, slug, level-specific fields, and status.
    properties:
    - uniform operations across levels; per-level required fields are declarative schema

  - concept_id: C-002
    name: Paragraph
    definition: Addressable binding unit of the HRS text carrying a stable four-character base36 label.
    properties:
    - label is position-independent, unique within one HRS, survives edits
    - non-binding blocks are excluded from labeling and coverage
  ```

  Relevant MRS relations:

  ```yaml
  - { from_concept: C-008, to_concept: C-005, type: uses }
  - { from_concept: C-008, to_concept: C-002, type: uses }
  ```

  `resolve_branch` is the top-level realisation of Branch (C-008)
  resolution: it locates the three Step (C-005) rows of one addressed
  path, verifies each child belongs to its addressed parent (levels 3, 4,
  5 are enforced structurally by the fixed level arguments passed to
  `_get_step_row`, so a step existing at the wrong level surfaces as "not
  found" via the missing-row branch of `_get_step_row`, not as a separate
  error path), and computes the HRS slice of binding Paragraph (C-002)
  rows claimed by the global step's source labels.
verification:
  type: manual
  target: plan_manager.views.branch.resolve_branch
  expected: >
    Manual review confirms resolve_branch fetches gs (level 3), ts (level
    4), and atomic (level 5) via _get_step_row in that order; raises
    BranchResolutionError naming ts_step_id and gs_step_id when
    ts.parent_step_uuid != gs.uuid; raises BranchResolutionError naming
    as_step_id and ts_step_id when atomic.parent_step_uuid != ts.uuid; and
    on success returns a Branch whose hrs_slice contains exactly the
    paragraphs from list_paragraphs whose label, without braces, appears in
    gs.fields["source_labels"], preserving list_paragraphs's document
    order.
status: done

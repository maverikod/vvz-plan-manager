step_id: A-001
parent_tactical_step: T-001
name: Define BranchResolutionError exception class
target_file: plan_manager/views/branch.py
operation: create_file
priority: 1
depends_on: []
concepts: [C-008]
prompt: >
  Create a new Python source file at the exact project-relative path
  `plan_manager/views/branch.py`.

  This file does not exist yet: operation is create_file. Do not assume any
  pre-existing content.

  Write exactly the following file content (module docstring, one blank
  line, then the class definition):

  ```python
  """Branch derived view (C-008): resolves one HRS-slice -> GS -> TS -> AS path
  from the plan tree stored in the database into an in-memory, never-persisted
  view. This module is read-only: it issues SELECT statements only against the
  step and paragraph tables and never INSERTs, UPDATEs, or DELETEs.
  """


  class BranchResolutionError(ValueError):
      """Raised when a branch address does not resolve.

      The exception message names the failing path element: for a missing
      step, the level and step_id that could not be found; for a step that
      does not belong to its addressed parent, the child's step_id and the
      addressed parent's step_id.
      """
  ```

  Constraints:

  - No imports in this file yet. `BranchResolutionError` subclasses the
    built-in `ValueError` directly; `ValueError` requires no import.
  - The class body contains only the docstring shown above — no `pass`,
    no additional statements, no `__init__` override.
  - Do not add any code beyond what is shown above. Later work (not part of
    this task) will extend this same file with additional classes and
    functions; do not anticipate or stub them.
  - Preserve exact docstring wording and line wrapping as shown.

  MRS excerpt (concept relevant to this AS):

  ```yaml
  - concept_id: C-008
    name: Branch
    definition: Addressable derived view of one path HRS-slice -> GS -> TS -> AS; the unit of verification and prompt assembly.
    properties:
    - never materialized as a stored artifact
    - a mechanically clean branch is measurable while siblings are broken
  ```

  `BranchResolutionError` is the deterministic error type raised when
  resolving a Branch (C-008) address fails; it is part of realising C-008's
  read-only resolution semantics.
verification:
  type: import
  target: plan_manager.views.branch.BranchResolutionError
  expected: >
    The module plan_manager.views.branch imports without error, and
    BranchResolutionError is defined as a subclass of the built-in
    ValueError with no additional attributes or overridden methods.
status: done

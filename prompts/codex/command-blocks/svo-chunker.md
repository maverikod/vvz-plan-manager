# Block ID: svo-chunker

Server:
- `svo-chunker-vvz`

Use for:
- Semantic chunking and file-to-chunk processing

Confirmed command group:
- `chunk`
- `file`
- `file_chunk`
- `health`
- `info`

Confirmed facts:
- `chunk` and `file_chunk` are queue-backed
- `file_chunk` returns outbound transfer artifacts
- health reports vectorization dependency status
